import type { Question } from '../types';

export const BANK_AZ204: Question[] = [
  // ── Develop Azure Compute Solutions ──────────────────────────────────────
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'A developer needs to run background tasks that process items from an Azure Storage Queue with automatic scaling. Which Azure service is MOST appropriate?',
    opts: [
      'Azure App Service with manual scaling',
      'Azure Functions with a Queue Storage trigger',
      'Azure Container Instances with a custom polling loop',
      'Azure Virtual Machines with a scheduled task',
    ],
    a: 1,
    exp: 'Azure Functions with a Queue Storage trigger automatically scales based on queue depth, invoking a function for each message without requiring infrastructure management. Container Instances and VMs require manual scaling and polling logic.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'An App Service web app currently serves production traffic. You need to deploy a new version with zero downtime and the ability to instantly roll back. Which feature should you use?',
    opts: [
      'Deploy directly to the production slot and use Traffic Manager for rollback',
      'Use a deployment slot with auto-swap disabled, then perform a manual slot swap',
      'Enable continuous deployment and rely on deployment history for rollback',
      'Use Azure Container Instances alongside the App Service for blue-green',
    ],
    a: 1,
    exp: 'Deployment slots let you deploy to a staging slot, warm it up, then swap to production. Keeping auto-swap disabled means you control when the swap happens and can swap back instantly if issues arise. Traffic Manager is a DNS-level solution that is slower and more complex for this purpose.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'A Durable Functions orchestrator calls three activity functions in parallel and waits for all to complete. Which pattern does this represent?',
    opts: [
      'Function chaining',
      'Fan-out/fan-in',
      'Async HTTP API (polling)',
      'Monitor',
    ],
    a: 1,
    exp: 'Fan-out/fan-in starts multiple activity functions concurrently (fan-out) and uses Task.WhenAll to aggregate results (fan-in). Function chaining is sequential. The async HTTP API pattern is for polling long-running work. Monitor is for flexible recurring workflows.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You have an Azure Function that must run within a virtual network and needs access to resources that are only accessible from that VNet. The function has unpredictable traffic spikes. Which hosting plan should you choose?',
    opts: [
      'Consumption plan with a VNet integration enabled by default',
      'Premium plan with VNet integration',
      'Dedicated (App Service) plan with no VNet integration',
      'Docker container running in Azure Container Instances',
    ],
    a: 1,
    exp: 'The Premium plan supports VNet integration and can scale to zero while still handling spikes. The Consumption plan does not support VNet integration. The Dedicated plan does support VNet integration but does not auto-scale on a per-execution basis in the same elastic manner as Premium.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You need an App Service deployment slot to automatically swap to production once health checks pass after deployment. Which setting achieves this?',
    opts: [
      'Enable continuous integration in the deployment center',
      'Configure auto-swap on the staging slot targeting the production slot',
      'Set up a release gate in Azure Pipelines that calls the swap API',
      'Enable Always On and Health Check in the production slot settings',
    ],
    a: 1,
    exp: 'Auto-swap is a slot-level setting that triggers an automatic swap to the target slot (production) after the app has warmed up successfully. CI/CD and release gates are pipeline-level constructs, not slot-level automatic behaviors.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'A Durable Functions solution must implement a process that waits for an external event (such as a human approval) before continuing. Which Durable Functions API should the orchestrator call?',
    opts: [
      'context.df.callActivity()',
      'context.df.waitForExternalEvent()',
      'context.df.createTimer()',
      'context.df.callSubOrchestrator()',
    ],
    a: 1,
    exp: 'waitForExternalEvent() suspends the orchestrator until a named event is raised via the Durable Task client, enabling human-in-the-loop workflows. callActivity() invokes a function, createTimer() sets a durable timer, and callSubOrchestrator() starts a child orchestration.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You are deploying an Azure Container App. After deploying a new revision, you want to send 10% of traffic to the new revision and 90% to the old one for canary testing. Which feature do you use?',
    opts: [
      'Revision-level ingress weight configuration',
      'Azure Traffic Manager with weighted routing',
      'A custom Nginx sidecar container to split traffic',
      'Azure API Management rate-limiting policy',
    ],
    a: 0,
    exp: 'Azure Container Apps supports revision-based traffic splitting where you assign percentage weights to different revisions directly in the ingress configuration. This is a built-in feature and does not require external services like Traffic Manager or custom sidecars.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'A Bicep deployment uses the `what-if` operation before the actual deployment. What is the primary purpose of `what-if`?',
    opts: [
      'It validates the Bicep file syntax and type-checks all parameters',
      'It shows the changes that would be made to resources without actually deploying',
      'It rolls back a previous deployment to a known-good state',
      'It generates ARM JSON from the Bicep file for review',
    ],
    a: 1,
    exp: 'The what-if operation analyzes the current state of resources and predicts what changes would result from the deployment, without making any changes. It is different from validate, which only checks syntax. Compilation to ARM JSON is a separate az bicep build command.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You need to run a long-running background job in an Azure App Service without using Azure Functions. The job must start when a file is uploaded to blob storage. Which feature should you use?',
    opts: [
      'WebJobs with a BlobTrigger binding',
      'Azure Logic Apps connected to the App Service',
      'A continuously running WebJob that polls blob storage',
      'Azure Scheduler linked to an App Service API endpoint',
    ],
    a: 0,
    exp: 'WebJobs in App Service support triggered and continuous modes. A triggered WebJob with a BlobTrigger (via the Azure WebJobs SDK) fires automatically when a blob is created, without polling. A continuously running WebJob that polls is less efficient and more complex to implement correctly.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'An AKS pod needs to read a Kubernetes Secret that stores a database connection string. The connection string was base64-encoded and stored in a Secret manifest. How does the pod access the secret value?',
    opts: [
      'Kubernetes decodes it automatically; the pod reads it as plain text via an environment variable or volume mount',
      'The pod must call the Kubernetes API with a service account token to decode the base64 value at runtime',
      'The pod reads the raw base64 string and must decode it using application code',
      'Secrets in AKS are always encrypted by Key Vault and require a CSI driver to access',
    ],
    a: 0,
    exp: 'Kubernetes automatically base64-decodes Secret values when injecting them as environment variables or mounting them as files. The application receives plain text. Base64 is encoding, not encryption. Key Vault CSI driver is optional for additional security.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You need an Azure Functions HTTP trigger to scale based on CPU utilization rather than the number of concurrent HTTP requests. Which plan supports custom KEDA-based scaling rules?',
    opts: [
      'Consumption plan',
      'Premium plan',
      'Dedicated App Service plan with autoscale',
      'Docker container in Azure Container Instances',
    ],
    a: 1,
    exp: 'The Premium plan uses KEDA (Kubernetes-based Event Driven Autoscaling) under the hood and supports custom scaling rules including CPU/memory metrics. The Consumption plan scales only based on event volume. The Dedicated plan uses App Service autoscale rules, not KEDA.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'A Durable Entity function is used to manage state for each shopping cart. Which is the correct way to signal a durable entity without waiting for its result?',
    opts: [
      'await client.callEntityAsync(entityId, operationName)',
      'client.signalEntity(entityId, operationName)',
      'await context.df.callActivity("EntityProxy", entityId)',
      'context.df.waitForExternalEvent(entityId.ToString())',
    ],
    a: 1,
    exp: 'signalEntity() sends a one-way signal (fire-and-forget) to an entity, scheduling the operation without waiting for a response. callEntityAsync() is a two-way call that awaits a response. Entities are not activities and cannot be called via callActivity().',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You configure a Horizontal Pod Autoscaler (HPA) in AKS targeting 70% CPU utilization. The deployment currently has 3 replicas and CPU is at 140%. How many replicas will the HPA scale to?',
    opts: [
      '4 replicas',
      '5 replicas',
      '6 replicas',
      '7 replicas',
    ],
    a: 2,
    exp: 'HPA formula: desiredReplicas = ceil(currentReplicas * (currentMetric / desiredMetric)) = ceil(3 * (140 / 70)) = ceil(3 * 2) = 6 replicas. The ceiling function ensures enough capacity.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'An Azure Container App environment is configured with Dapr. A service needs to invoke another service using Dapr service invocation. What is the correct Dapr sidecar HTTP endpoint format?',
    opts: [
      'http://localhost:3500/v1.0/invoke/{appId}/method/{methodName}',
      'http://{appId}.internal/v1.0/invoke/{methodName}',
      'http://dapr-sidecar:80/invoke/{appId}/{methodName}',
      'http://localhost:80/{appId}/{methodName}',
    ],
    a: 0,
    exp: 'Dapr sidecar listens on port 3500 (by default) on localhost. The service invocation API path is /v1.0/invoke/{appId}/method/{methodName}. Services communicate via the sidecar, not directly to other service endpoints.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'A Bicep module is defined in a separate file and called from a main template. Which syntax correctly references a module output named `storageAccountId`?',
    opts: [
      'module.storageAccountId',
      'storageModule.outputs.storageAccountId',
      'reference(storageModule).storageAccountId',
      'outputs.storageModule.storageAccountId',
    ],
    a: 1,
    exp: 'Bicep module outputs are accessed via the pattern {moduleName}.outputs.{outputName}. The `reference()` function is ARM template syntax. Bicep provides strongly-typed access through the outputs property of the module symbolic name.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You need to route 20% of production traffic to a new App Service slot for A/B testing while ensuring session affinity so users always go to the same slot. Which configuration achieves this?',
    opts: [
      'Set the routing percentage to 20 on the testing slot and enable x-ms-routing-name cookie',
      'Use Azure Traffic Manager with weighted routing and enable session affinity at the Traffic Manager level',
      'Configure Azure Front Door with a canary rule and sticky sessions',
      'Manually set cookies in application code and use URL rewriting in IIS',
    ],
    a: 0,
    exp: 'App Service traffic routing allows percentage-based routing to named slots. When a user is assigned to a slot, the x-ms-routing-name cookie is set, ensuring subsequent requests go to the same slot (session affinity). Traffic Manager operates at DNS level and does not provide slot-level affinity.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You need to configure an Azure Container Registry to serve images to an AKS cluster without storing registry credentials as Kubernetes Secrets. What is the recommended approach?',
    opts: [
      'Create a Docker config secret with the ACR credentials and reference it in the pod spec imagePullSecrets',
      'Attach the AKS cluster to the ACR using the az aks update --attach-acr command',
      'Configure the ACR with public access enabled so no authentication is needed',
      'Use a DaemonSet to pull and cache images on each node before pod scheduling',
    ],
    a: 1,
    exp: 'The az aks update --attach-acr command grants the AKS kubelet managed identity the AcrPull role on the ACR, enabling passwordless image pulls without storing credentials as Secrets. Storing credentials as Docker config secrets is a valid but less secure alternative.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'A Durable Functions Monitor pattern polls an external job status every 5 minutes and completes when the job succeeds or times out after 24 hours. Which Durable Functions primitives are used together?',
    opts: [
      'callActivity() in a loop with Thread.Sleep() for the delay',
      'waitForExternalEvent() with a timer to re-check periodically',
      'createTimer() combined with a conditional loop calling callActivity()',
      'callSubOrchestrator() with fan-out to multiple polling activities',
    ],
    a: 2,
    exp: 'The Monitor pattern uses a durable timer (createTimer()) to sleep between polling intervals and callActivity() to check the external status. The orchestrator loops until the success condition or timeout is met. waitForExternalEvent() waits passively for an external signal rather than actively polling.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'An ARM template has a `dependsOn` property removed from a resource that previously depended on a storage account. What is the effect when this template is deployed?',
    opts: [
      'The deployment fails because ARM requires explicit dependencies',
      'ARM may deploy resources in parallel, potentially causing the dependent resource to fail if the storage account is not ready',
      'ARM automatically detects all dependencies through resource IDs in properties and adds implicit dependencies',
      'The storage account is deleted because removing dependsOn signals it is no longer needed',
    ],
    a: 2,
    exp: 'ARM automatically detects implicit dependencies when a resource references another resource\'s properties (e.g., via resourceId() or reference()). Removing an explicit dependsOn only matters when the dependency is not expressed through property references. ARM does not delete resources when dependsOn is removed.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You need an Azure Function to be triggered by messages on an Azure Service Bus topic subscription. Which trigger configuration is correct?',
    opts: [
      'ServiceBusTrigger with connection string, topicName, and subscriptionName attributes',
      'EventHubTrigger with the Service Bus namespace connection string',
      'QueueTrigger with the topic name as the queue name',
      'TimerTrigger combined with Service Bus SDK code to read messages',
    ],
    a: 0,
    exp: 'The ServiceBusTrigger binding for topics requires the topicName and subscriptionName parameters in addition to the connection string. QueueTrigger is for Storage Queues, not Service Bus. EventHubTrigger is for Event Hubs.',
  },
  // ── Develop for Azure Storage ─────────────────────────────────────────────
  {
    domain: 'Develop for Azure Storage',
    q: 'A developer needs to generate a SAS token in .NET code that allows only read access to a specific blob for 1 hour without using the storage account key. Which approach should they use?',
    opts: [
      'Account SAS generated with StorageSharedKeyCredential',
      'Service SAS generated with BlobSasBuilder using the storage account key',
      'User delegation SAS generated with BlobSasBuilder using a UserDelegationKey obtained from DefaultAzureCredential',
      'Anonymous access enabled on the container with a time-based URL query string',
    ],
    a: 2,
    exp: 'A user delegation SAS uses Azure AD credentials (via DefaultAzureCredential or similar) to obtain a UserDelegationKey, then signs the SAS. This avoids using the storage account key, which is a security best practice. Account SAS and Service SAS require the storage account key.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'You need to move blobs that have not been accessed for 30 days to the Cool tier automatically. Which Azure Blob Storage feature implements this?',
    opts: [
      'Object replication policy',
      'Lifecycle management policy with a tierToCool action and daysAfterLastAccessTimeGreaterThan condition',
      'Azure Data Factory pipeline with a scheduled trigger',
      'Azure Functions timer trigger that calls SetAccessTier() on each blob daily',
    ],
    a: 1,
    exp: 'Azure Blob Storage lifecycle management policies support actions like tierToCool, tierToArchive, and delete, with conditions including daysAfterLastAccessTimeGreaterThan. This is a native, serverless, and cost-efficient approach. Azure Functions timer trigger requires custom code and is less efficient at scale.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'A developer uses the Azure Cosmos DB .NET SDK to read an item by its ID. The operation returns a 404 status. What does this indicate in the SDK context?',
    opts: [
      'The Cosmos DB account is unavailable',
      'The item does not exist in the specified container and partition key',
      'The request exceeded the provisioned RU/s and was throttled',
      'The SDK version is incompatible with the Cosmos DB API version',
    ],
    a: 1,
    exp: 'A 404 from Cosmos DB means the item with the specified ID and partition key was not found. 429 indicates throttling (RU exceeded). 503 or 408 indicates availability issues. Version incompatibilities typically manifest as different error types.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'Which Cosmos DB consistency level provides the strongest guarantees while being available across multiple regions?',
    opts: [
      'Strong',
      'Bounded Staleness',
      'Session',
      'Consistent Prefix',
    ],
    a: 0,
    exp: 'Strong consistency guarantees linearizability — reads always return the most recent committed write. However, in multi-region deployments, Strong consistency has higher latency. Bounded Staleness is the next strongest, guaranteeing reads lag behind writes by at most K versions or T seconds.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'A Cosmos DB container has a partition key of `/customerId`. A query filters on `productId` only without specifying `customerId`. What is the performance implication?',
    opts: [
      'The query is optimized because Cosmos DB indexes all properties by default',
      'The query executes as a cross-partition fan-out query, scanning all partitions and consuming more RUs',
      'The query fails because Cosmos DB requires the partition key in all queries',
      'The query uses the change feed to locate the correct partition',
    ],
    a: 1,
    exp: 'Without the partition key, Cosmos DB cannot route the query to a single partition and must fan out to all partitions, increasing RU consumption and latency. Cosmos DB does not fail on cross-partition queries by default, but they are expensive at scale.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'You need to implement a cache-aside pattern using Azure Cache for Redis with the StackExchange.Redis library. What is the correct sequence of operations?',
    opts: [
      'Always write to Redis first, then write to the database to keep cache warm',
      'Check Redis for the key; if found return it (cache hit); if not found fetch from database, write to Redis with a TTL, then return the value',
      'Subscribe to Redis keyspace notifications and update the database whenever a key expires',
      'Use Redis as the primary store and replicate to the database using Redis streams',
    ],
    a: 1,
    exp: 'Cache-aside (lazy loading) checks the cache first. On a miss, it fetches from the source of truth (database), populates the cache with a TTL, and returns the value. Setting TTL prevents stale data. Writing to Redis first risks inconsistency if the database write fails.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'A developer needs to retrieve blob metadata without downloading the blob content. Which BlobClient method should be used?',
    opts: [
      'DownloadAsync() and read the metadata from the stream headers',
      'GetPropertiesAsync() which returns BlobProperties including metadata',
      'GetTagsAsync() which returns all blob metadata and tags',
      'ExistsAsync() which returns metadata as part of the response',
    ],
    a: 1,
    exp: 'GetPropertiesAsync() retrieves the blob\'s system properties and user-defined metadata without downloading the blob content. GetTagsAsync() retrieves blob index tags (a different feature from metadata). ExistsAsync() only returns whether the blob exists.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'A Cosmos DB stored procedure needs to insert multiple documents atomically. What constraint must you design around?',
    opts: [
      'Stored procedures can span multiple containers but only one partition per container',
      'Stored procedures execute within a single logical partition, so all documents must share the same partition key value',
      'Stored procedures support multi-partition transactions using the two-phase commit protocol',
      'Stored procedures are limited to 100 document operations per execution',
    ],
    a: 1,
    exp: 'Cosmos DB stored procedures are scoped to a single logical partition. All operations within the stored procedure must target documents with the same partition key value. Cross-partition transactions are not supported in Cosmos DB stored procedures.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'You need to set a blob\'s access tier to Archive. Which statement about the Archive tier is TRUE?',
    opts: [
      'Archived blobs can be read immediately without any rehydration delay',
      'To read an archived blob, it must first be rehydrated to Hot or Cool tier, which can take hours',
      'Archive tier is only available for block blobs in LRS storage accounts',
      'Setting a blob to Archive tier deletes it after 180 days automatically',
    ],
    a: 1,
    exp: 'Archive tier blobs are offline and cannot be read directly. They must be rehydrated to Hot or Cool tier first, which takes up to 15 hours for standard priority or can be expedited at higher cost. Archive is available for GRS accounts as well as LRS.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'You are using StackExchange.Redis and need to increment a counter atomically and return the new value. Which command should you use?',
    opts: [
      'db.StringGet() followed by db.StringSet() with the incremented value',
      'db.StringIncrement(key)',
      'db.HashIncrement(key, field)',
      'db.Execute("INCR", key)',
    ],
    a: 1,
    exp: 'StringIncrement() maps to the Redis INCR/INCRBY command, which atomically increments the value and returns the new value in a single operation. Using StringGet() + StringSet() is a read-modify-write race condition. HashIncrement() is for hash fields.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'A developer needs to read all changes to items in a Cosmos DB container in the order they were written, to feed a downstream event-driven system. Which feature should they use?',
    opts: [
      'Cosmos DB query with ORDER BY _ts',
      'Azure Cosmos DB Change Feed',
      'Azure Event Grid integration with Cosmos DB',
      'Cosmos DB stored procedure that queries recent documents',
    ],
    a: 1,
    exp: 'The Change Feed provides an ordered, persistent log of changes to documents in a Cosmos DB container. It is designed for event-driven architectures and stream processing. A query ordered by _ts is not guaranteed to catch all changes and does not scale efficiently for continuous processing.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'An Azure Table Storage query filters on PartitionKey and RowKey. What type of query is this and what is the performance characteristic?',
    opts: [
      'A partition scan — it scans all rows in the partition',
      'A full table scan — it scans all partitions',
      'A point query — it directly retrieves a single entity in O(1) time',
      'A range query — it scans rows within a RowKey range',
    ],
    a: 2,
    exp: 'Specifying both PartitionKey and RowKey is a point query, which locates the exact entity without scanning. It is the most efficient query type in Azure Table Storage. A partition scan specifies only PartitionKey, and a full table scan uses neither.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'You need to upload a 50 GB file to Azure Blob Storage efficiently using the SDK. Which upload method should you choose and why?',
    opts: [
      'UploadAsync() because it handles all sizes uniformly',
      'UploadBlobAsync() on BlobContainerClient because it is the newest API',
      'OpenWriteAsync() to write a stream, allowing the SDK to handle chunking automatically',
      'Use the REST API directly with a single PUT Blob request for maximum control',
    ],
    a: 2,
    exp: 'For large files, OpenWriteAsync() returns a writable stream that the SDK uses to upload in parallel chunks (block blobs with multiple Put Block calls). UploadAsync() also handles large files via parallel block uploads, but OpenWriteAsync() is the streaming-friendly approach. A single PUT Blob request is limited to 5,000 MiB.',
  },
  // ── Implement Azure Security ──────────────────────────────────────────────
  {
    domain: 'Implement Azure Security',
    q: 'A native mobile app needs to authenticate users against Azure AD and call a protected API. Which OAuth 2.0 flow is recommended for this scenario?',
    opts: [
      'Client Credentials flow because the app has its own identity',
      'Implicit flow because it does not require a client secret',
      'Authorization Code flow with PKCE because it is secure for public clients without a client secret',
      'Resource Owner Password Credentials flow because it is simple to implement',
    ],
    a: 2,
    exp: 'Authorization Code flow with PKCE (Proof Key for Code Exchange) is the recommended flow for public clients (mobile, SPA) that cannot securely store a client secret. PKCE mitigates the authorization code interception attack. The Implicit flow is deprecated. ROPC is not recommended as it requires handling user credentials directly.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'A developer needs to retrieve a secret from Azure Key Vault in a .NET application running in Azure App Service without storing any credentials in configuration. What is the correct approach?',
    opts: [
      'Store the client secret in app settings and use ClientSecretCredential with SecretClient',
      'Enable a system-assigned managed identity on the App Service and use DefaultAzureCredential with SecretClient',
      'Use the Key Vault REST API with a storage account key as the authentication credential',
      'Store the Key Vault URI and access key in environment variables at deploy time',
    ],
    a: 1,
    exp: 'A system-assigned managed identity eliminates the need to store credentials. DefaultAzureCredential automatically detects the managed identity and acquires a token for Key Vault access. This is the zero-credential approach. Storing client secrets or keys in configuration is a security anti-pattern.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'You have a user-assigned managed identity attached to multiple Azure Virtual Machines. When using DefaultAzureCredential in code, what additional step is needed to ensure the user-assigned identity is used?',
    opts: [
      'Nothing; DefaultAzureCredential automatically selects the user-assigned identity',
      'Set the AZURE_CLIENT_ID environment variable to the client ID of the user-assigned identity',
      'Use ManagedIdentityCredential with the resource ID of the identity',
      'Both B and C are valid approaches',
    ],
    a: 3,
    exp: 'When multiple managed identities exist on a resource, you must disambiguate. DefaultAzureCredential respects the AZURE_CLIENT_ID environment variable. ManagedIdentityCredential accepts both clientId and resourceId parameters. Both approaches work to target a specific user-assigned identity.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'A JWT token returned by Azure AD contains the `scp` claim with the value `Files.Read Files.Write`. What does this claim represent?',
    opts: [
      'The roles assigned to the application in Azure AD',
      'The delegated permissions (scopes) granted to the application on behalf of the signed-in user',
      'The application permissions consented by an administrator',
      'The token signing certificate thumbprint',
    ],
    a: 1,
    exp: 'The `scp` (scope) claim contains space-separated delegated permissions granted on behalf of the user. Application permissions (app roles) appear in the `roles` claim. The `scp` claim is only present in delegated permission tokens, not application-only tokens.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'A developer registers an app in Azure AD and needs to call Microsoft Graph to read all users in the tenant without a signed-in user. Which permission type and consent is required?',
    opts: [
      'Delegated permission User.Read with user consent',
      'Application permission User.Read.All with admin consent',
      'Delegated permission User.ReadBasic.All with user consent',
      'Application permission User.Read with user consent',
    ],
    a: 1,
    exp: 'Reading all users without a signed-in user requires the Client Credentials flow with an Application permission (not delegated). User.Read.All is the application permission for reading all user profiles and requires administrator consent because it grants broad access.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'You need to generate an Account SAS that grants access to both Blob and Queue services with write permissions, expiring in 8 hours. Which class in the .NET SDK is used?',
    opts: [
      'BlobSasBuilder',
      'AccountSasBuilder',
      'StorageSharedKeyCredential with manual signature generation',
      'UserDelegationSasBuilder',
    ],
    a: 1,
    exp: 'AccountSasBuilder generates Account SAS tokens that can span multiple services (Blob, Queue, Table, File) and resource types. BlobSasBuilder generates Service SAS scoped to Blob service resources only. There is no UserDelegationSasBuilder class; user delegation is a parameter of BlobSasBuilder.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'An API protected by Azure AD receives a Bearer token. The API must validate the token. Which claim must be validated to ensure the token was issued for this specific API?',
    opts: [
      'iss — the issuer of the token',
      'aud — the audience, which must match the API\'s Application ID URI or client ID',
      'sub — the subject identifier of the user',
      'iat — the issued-at time',
    ],
    a: 1,
    exp: 'The `aud` (audience) claim identifies the intended recipient of the token. If the aud does not match the API\'s registered Application ID URI or client ID, the token should be rejected to prevent confused deputy attacks. The iss claim validates the token origin, not the intended audience.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'A Key Vault access policy grants a managed identity `Get` and `List` permissions on Secrets. The application calls SecretClient.GetSecretAsync("mySecret"). What is returned?',
    opts: [
      'The latest enabled version of the secret with its value',
      'A list of all versions of the secret',
      'The secret metadata only, not the secret value',
      'An error because List permission is required to Get a specific secret',
    ],
    a: 0,
    exp: 'GetSecretAsync() with a name but no version retrieves the latest enabled version of the secret, including its value, when the Get permission is granted. List permission is only needed for listing secrets by name. The Get permission alone is sufficient to retrieve a specific secret\'s value.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'You need to allow an Azure Function to access a Key Vault using MSAL.NET in a daemon application. After acquiring a token using the Client Credentials flow, which MSAL method is used?',
    opts: [
      'app.AcquireTokenInteractive(scopes).ExecuteAsync()',
      'app.AcquireTokenSilent(scopes, account).ExecuteAsync()',
      'app.AcquireTokenForClient(scopes).ExecuteAsync()',
      'app.AcquireTokenByUsernamePassword(scopes, username, password).ExecuteAsync()',
    ],
    a: 2,
    exp: 'AcquireTokenForClient() implements the Client Credentials flow for daemon/service applications with no user interaction. AcquireTokenInteractive() requires a user, AcquireTokenSilent() retrieves cached tokens for a user, and AcquireTokenByUsernamePassword() uses ROPC flow.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'A Service SAS token grants access to a specific blob but the storage account\'s key is rotated after the SAS was generated. What happens to the SAS token?',
    opts: [
      'The SAS token continues to work until its expiry time because it was already signed',
      'The SAS token is immediately revoked because it was signed with the old key',
      'The SAS token works for 24 hours after key rotation as a grace period',
      'The SAS token must be regenerated using the new key to remain valid',
    ],
    a: 1,
    exp: 'Service SAS and Account SAS tokens are signed using the storage account key. When the key is rotated, all SAS tokens signed with the old key are immediately invalidated. User delegation SAS tokens are signed with a UserDelegationKey and are not affected by storage key rotation.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'An application uses the Device Code flow to authenticate. In which scenario is this flow appropriate?',
    opts: [
      'A web app running on a server needs to call the Microsoft Graph API without user interaction',
      'A CLI tool or IoT device with no browser needs a user to authenticate via another device',
      'A native mobile app needs to authenticate using the system browser',
      'A background service needs to acquire a token using a certificate',
    ],
    a: 1,
    exp: 'The Device Code flow is for input-constrained devices (smart TVs, CLIs, IoT) where the user authenticates on a separate device with a browser. A server-side app without user interaction uses Client Credentials. Mobile apps use Authorization Code + PKCE. Certificate-based auth uses Client Credentials with certificate assertion.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'You need to sign data using a key stored in Azure Key Vault without the private key material ever leaving Key Vault. Which KeyClient/CryptographyClient method should you use?',
    opts: [
      'KeyClient.GetKeyAsync() to download the private key, then sign locally',
      'CryptographyClient.SignAsync() which performs the signing operation within Key Vault',
      'SecretClient.GetSecretAsync() to retrieve the key stored as a secret, then sign locally',
      'KeyClient.WrapKeyAsync() to protect the signing key before use',
    ],
    a: 1,
    exp: 'CryptographyClient.SignAsync() performs the cryptographic signing operation inside Key Vault HSM. The private key never leaves the vault. KeyClient.GetKeyAsync() only returns the public key for asymmetric keys, not the private key material. WrapKeyAsync() is for key wrapping, not signing.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'You want to use a client certificate instead of a client secret for an Azure AD app registration. What advantage does a certificate provide?',
    opts: [
      'Certificates do not expire, unlike client secrets',
      'Certificates are asymmetric — only the public key is stored in Azure AD, and the private key never leaves the client, reducing credential exposure risk',
      'Certificates bypass the need for admin consent for application permissions',
      'Certificates automatically rotate when they approach expiration',
    ],
    a: 1,
    exp: 'Client certificates use asymmetric cryptography: Azure AD stores only the public key (thumbprint/certificate), and the private key stays with the application. This means no shared secret is transmitted. Certificates do expire and must be manually rotated unless automation is configured.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'A developer needs to call the Microsoft Graph API to send an email on behalf of a user who is signed in to the application. Which permission and token type is required?',
    opts: [
      'Application permission Mail.Send with client credentials token',
      'Delegated permission Mail.Send with a user access token',
      'Application permission Mail.ReadWrite with admin consent token',
      'Delegated permission Mail.Read with a user access token',
    ],
    a: 1,
    exp: 'Sending email on behalf of a signed-in user requires a delegated permission (Mail.Send) and a user access token obtained through an interactive or silent token acquisition on behalf of the user. Application permission Mail.Send allows sending as any user but does not represent the signed-in user.',
  },
  // ── Monitor, Troubleshoot & Optimize ──────────────────────────────────────
  {
    domain: 'Monitor, Troubleshoot & Optimize',
    q: 'A developer wants to track custom business metrics (such as orders processed) in Application Insights from a .NET application. Which API should they use?',
    opts: [
      'telemetryClient.TrackEvent("OrderProcessed")',
      'telemetryClient.TrackMetric("OrdersProcessed", value)',
      'telemetryClient.TrackTrace("OrderProcessed: " + orderId)',
      'telemetryClient.TrackDependency("Order", "Process", ...)',
    ],
    a: 1,
    exp: 'TrackMetric() records custom numeric measurements (like a counter or gauge) that can be aggregated and alerted on. TrackEvent() records named events without numeric aggregation. TrackTrace() is for diagnostic messages. TrackDependency() is for tracking outbound calls.',
  },
  {
    domain: 'Monitor, Troubleshoot & Optimize',
    q: 'An Application Insights resource is configured with adaptive sampling. What is the effect of adaptive sampling on telemetry?',
    opts: [
      'All telemetry is collected but compressed before transmission to reduce bandwidth',
      'A representative subset of telemetry items is collected to control data volume while preserving statistical accuracy',
      'Telemetry is batched and sent at fixed intervals, reducing the number of HTTP calls',
      'Low-priority telemetry (traces) is discarded while exceptions and requests are always sent',
    ],
    a: 1,
    exp: 'Adaptive sampling dynamically adjusts the sampling rate to target a desired telemetry volume. Items within the same operation are sampled together (sampled in or out as a group) to preserve end-to-end traces. This reduces cost while maintaining statistical accuracy. Compression is a separate transport concern.',
  },
  {
    domain: 'Monitor, Troubleshoot & Optimize',
    q: 'You need to write a KQL query in Log Analytics that returns all failed HTTP requests (result code >= 400) to your App Service in the last hour, grouped by URL path. Which table should you query?',
    opts: [
      'AppRequests | where ResultCode >= 400',
      'requests | where resultCode >= "400" | summarize count() by url',
      'AzureDiagnostics | where Category == "FunctionAppLogs"',
      'AppServiceHTTPLogs | where ScStatus >= 400 | summarize count() by CsUriStem',
    ],
    a: 3,
    exp: 'AppServiceHTTPLogs contains the IIS-style HTTP access logs for Azure App Service with ScStatus (status code) and CsUriStem (URL stem). The `requests` table is Application Insights-specific (workspace-based). AzureDiagnostics.FunctionAppLogs is for function app application logs.',
  },
  {
    domain: 'Monitor, Troubleshoot & Optimize',
    q: 'Application Insights Snapshot Debugger captures a snapshot when an exception occurs. What information does a snapshot contain?',
    opts: [
      'A heap dump with variable values at the time of the exception, viewable in the Azure portal',
      'A full memory dump of the process including all threads',
      'A stack trace only, identical to the exception telemetry',
      'A replay of the HTTP request that caused the exception',
    ],
    a: 0,
    exp: 'The Snapshot Debugger captures a mini-dump (snapshots of local variables, parameters, and call stack) at the point of exception. These are viewable in the Azure portal or Visual Studio. It is not a full process memory dump and does not replay requests.',
  },
  {
    domain: 'Monitor, Troubleshoot & Optimize',
    q: 'A distributed application spans multiple services instrumented with Application Insights. You need to correlate telemetry across services for a single user request. Which mechanism links these telemetry items?',
    opts: [
      'Each service uses the same Application Insights instrumentation key',
      'The operation_Id (correlation ID) is propagated in HTTP headers (traceparent/Request-Id) and links all telemetry for the same operation',
      'Azure Monitor automatically joins telemetry from different services by timestamp',
      'A shared Log Analytics workspace de-duplicates telemetry by user session ID',
    ],
    a: 1,
    exp: 'Distributed tracing uses W3C TraceContext (traceparent header) or legacy Request-Id headers to propagate a shared operation ID across service boundaries. All telemetry items for the same operation share the operation_Id, enabling end-to-end trace visualization in Application Insights.',
  },
  {
    domain: 'Monitor, Troubleshoot & Optimize',
    q: 'You want to receive an alert when the average response time of your App Service exceeds 2 seconds over a 5-minute window. Which Azure Monitor resource do you create?',
    opts: [
      'A Log Analytics query alert that runs every 5 minutes',
      'A metric alert with the Http Response Time metric, aggregation Average, threshold 2s, over a 5-minute window',
      'An Activity Log alert for App Service response time events',
      'An Application Insights Smart Detection alert for performance degradation',
    ],
    a: 1,
    exp: 'A metric alert on the Http Response Time metric with Average aggregation and a 5-minute evaluation window is the correct approach for threshold-based alerting on a numeric metric. Log Analytics alerts are for log-based scenarios. Activity Log alerts track control-plane events. Smart Detection is automatic and not threshold-configurable.',
  },
  {
    domain: 'Monitor, Troubleshoot & Optimize',
    q: 'Redis cache is experiencing high latency. You suspect connections are not being reused. Which StackExchange.Redis best practice addresses connection reuse?',
    opts: [
      'Create a new ConnectionMultiplexer for each request to avoid connection state issues',
      'Use a single static ConnectionMultiplexer instance shared across the application lifetime',
      'Use connection pooling with a maximum of 10 connections configured in the connection string',
      'Enable SSL on each individual request connection to isolate secure sessions',
    ],
    a: 1,
    exp: 'ConnectionMultiplexer is designed to be shared. Creating a new instance per request causes connection churn, exhausts sockets, and significantly increases latency. StackExchange.Redis manages an internal connection pool automatically when a single multiplexer is used.',
  },
  {
    domain: 'Monitor, Troubleshoot & Optimize',
    q: 'You deploy Application Insights availability tests. Which test type sends a synthetic request to your application from multiple worldwide locations every 5 minutes?',
    opts: [
      'Custom TrackAvailability() test running in your application code',
      'Standard (URL ping) availability test configured in the Azure portal',
      'Multi-step web test using Selenium scripts',
      'Health check probe configured on the App Service plan',
    ],
    a: 1,
    exp: 'Standard availability tests (formerly URL ping tests) are configured in Application Insights and send HTTP requests from up to 5 Azure regions on a configurable interval (minimum 5 minutes). They do not require any application code changes. TrackAvailability() is for custom availability tracking from your own infrastructure.',
  },
  // ── Connect & Consume Azure Services ──────────────────────────────────────
  {
    domain: 'Connect & Consume Azure Services',
    q: 'An Azure API Management policy needs to limit each subscriber to 100 calls per minute. Which policy element achieves this?',
    opts: [
      '<rate-limit calls="100" renewal-period="60" />',
      '<quota calls="100" renewal-period="60" />',
      '<throttle-requests calls-per-minute="100" />',
      '<ip-filter action="allow" calls="100" />',
    ],
    a: 0,
    exp: 'The rate-limit policy enforces a per-subscription call rate within a sliding window (renewal-period in seconds). The quota policy enforces a long-term cumulative limit (e.g., per month). There is no throttle-requests or ip-filter policy with call counting in APIM.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'An Azure Event Grid subscription needs to handle events that cannot be delivered after all retry attempts. Where are these events sent?',
    opts: [
      'They are permanently deleted to prevent storage costs',
      'They are sent to the dead-letter destination (a storage blob container) if configured',
      'They are re-queued in the Event Grid topic for future delivery',
      'They trigger an Azure Monitor alert but are not stored',
    ],
    a: 1,
    exp: 'Event Grid supports dead-lettering: undeliverable events (after exhausting retry policy) are stored in a configured Azure Blob Storage container. Dead-lettering must be explicitly configured on the event subscription. Without a dead-letter destination, undeliverable events are dropped.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'An Azure Event Hubs consumer needs to ensure it resumes reading from where it left off after a restart. Which mechanism provides this capability?',
    opts: [
      'Setting the ConsumerGroup offset to "latest" in the connection string',
      'Using EventProcessorClient, which checkpoints the partition offset to Azure Blob Storage',
      'Storing the sequence number in application memory and re-reading from that position on restart',
      'Configuring the Event Hub to retain messages for 7 days with guaranteed redelivery',
    ],
    a: 1,
    exp: 'EventProcessorClient (the recommended client) checkpoints the last processed event\'s offset to Azure Blob Storage. On restart, it reads the checkpoint and resumes from where it left off. In-memory storage loses state on restart, and message retention alone does not provide automatic offset tracking.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'You need to process Azure Service Bus messages in order, with all messages for a given customer processed sequentially. Which feature of Service Bus enables this?',
    opts: [
      'Message deduplication with a duplicate detection window',
      'Message sessions with a session ID set to the customer ID',
      'Topic subscriptions with a correlation filter on the customer ID',
      'FIFO ordering at the queue level without any additional configuration',
    ],
    a: 1,
    exp: 'Service Bus sessions (enabled per queue/topic) allow messages with the same session ID to be locked and processed by a single receiver at a time, guaranteeing ordered processing per session. Deduplication prevents duplicate messages. Correlation filters route messages but do not enforce ordering.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'An Azure Service Bus message fails processing and is moved to the dead-letter queue. What must the receiving application do to process dead-letter messages?',
    opts: [
      'Enable the dead-letter queue in the Service Bus namespace settings and messages are automatically retried',
      'Connect to a sub-queue path in the format {queueName}/$DeadLetterQueue and receive messages from it',
      'Dead-letter messages are automatically forwarded to an Azure Event Hub for reprocessing',
      'Restart the Service Bus namespace to replay dead-letter messages',
    ],
    a: 1,
    exp: 'The dead-letter queue is a sub-queue accessible via the path {queueName}/$DeadLetterQueue. A receiver must explicitly connect to this sub-queue path to read and process dead-letter messages. They are not retried automatically and require explicit handling.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'You need to configure an Azure API Management policy to cache the response of a GET endpoint for 60 seconds. Which policy and section is correct?',
    opts: [
      'Add <cache-store duration="60" /> in the inbound section',
      'Add <cache-lookup /> in the inbound section and <cache-store duration="60" /> in the outbound section',
      'Add <cache-store duration="60" /> in the outbound section only',
      'Configure caching in the backend service and set Cache-Control headers',
    ],
    a: 1,
    exp: 'APIM response caching requires both <cache-lookup /> in the inbound section (to check the cache before calling the backend) and <cache-store duration="60" /> in the outbound section (to cache the response after receiving it). Using cache-store alone in the outbound section stores the response but never serves it from cache.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'A producer publishes events to Azure Event Hubs using a partition key equal to the device ID. What does this guarantee?',
    opts: [
      'All events from the same device are routed to the same partition, preserving order per device',
      'Events are distributed evenly across all partitions for load balancing',
      'The event is deduplicated if sent more than once with the same key',
      'Only one consumer group can read events with that partition key',
    ],
    a: 0,
    exp: 'Using a consistent partition key causes Event Hubs to route all messages with that key to the same partition. Since partitions are ordered, consumers reading a single partition see events from each device in order. Different keys may land on the same partition, but the reverse is not true.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'You import an OpenAPI specification into Azure API Management. What happens to the operations defined in the OpenAPI spec?',
    opts: [
      'APIM validates the spec but you must manually create each operation',
      'APIM automatically creates API operations, sets URL paths, HTTP methods, and request/response schemas from the spec',
      'APIM creates a passthrough proxy but does not apply any schema validation',
      'APIM converts the OpenAPI spec to a WSDL file for SOAP compatibility',
    ],
    a: 1,
    exp: 'Importing an OpenAPI (Swagger) spec into APIM automatically creates all API operations with their paths, HTTP methods, parameters, request bodies, and response schemas. Policies can then be applied to these auto-created operations. Schema validation is available as an optional policy.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'An Azure Function is the handler for an Azure Event Grid system topic subscription on a storage account. The function receives a blob created event. Where is the event data in the function parameter?',
    opts: [
      'In a BlobTrigger binding parameter as a Stream object',
      'In an EventGridTrigger binding parameter as an EventGridEvent object containing the blob event data in the Data property',
      'In a QueueTrigger binding parameter as a JSON string',
      'In an HttpTrigger binding parameter as the request body',
    ],
    a: 1,
    exp: 'When an Azure Function uses the EventGridTrigger binding, it receives an EventGridEvent (or CloudEvent) object. The event-specific data (such as blob URL, size) is in the Data property and must be deserialized to the appropriate type (e.g., StorageBlobCreatedEventData).',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'An Azure Service Bus queue has duplicate detection enabled with a 10-minute window. A producer sends a message with MessageId "order-123", and 5 minutes later sends the same MessageId again. What happens?',
    opts: [
      'The second message is delivered normally because 5 minutes is within the window, so it is not a duplicate',
      'The second message is silently discarded because its MessageId was seen within the duplicate detection window',
      'The second message is sent to the dead-letter queue as a duplicate',
      'The second message overwrites the first message in the queue',
    ],
    a: 1,
    exp: 'Service Bus duplicate detection tracks MessageIds within the configured time window (10 minutes here). If the same MessageId is submitted within the window, the subsequent message is silently dropped. This prevents double-processing due to producer retries. The message is not dead-lettered or stored.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'You need to send push notifications to iOS and Android devices from a backend service using Azure Notification Hubs. What does Notification Hubs abstract away?',
    opts: [
      'The need to manage device registration tokens from the mobile app',
      'The platform-specific push notification services (APNs for iOS, FCM for Android), allowing a single API call to reach multiple platforms',
      'The need to use HTTPS for notification delivery',
      'Authentication between the mobile device and the backend service',
    ],
    a: 1,
    exp: 'Azure Notification Hubs abstracts the platform notification systems (PNS): APNs (Apple), FCM (Google), WNS (Windows), ADM (Amazon), etc. You send a single notification to Notification Hubs and it fans out to the appropriate PNS for each registered device.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'An APIM policy needs to transform the JSON response from a backend and add a custom header. In which policy section should the transformation be placed?',
    opts: [
      'inbound — because it runs before the backend call',
      'outbound — because it runs after receiving the backend response',
      'backend — because it runs during the backend call',
      'on-error — because header addition is an error-handling operation',
    ],
    a: 1,
    exp: 'The outbound section runs after the backend response is received and before the response is sent to the client. This is where response transformation (JSON manipulation, header addition, set-body) policies belong. The inbound section is for request transformation before forwarding to the backend.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'You need to route Service Bus topic messages to two different subscriptions: one for premium customers and one for standard customers. Which Service Bus feature handles this routing?',
    opts: [
      'Queue sessions with different session IDs for each customer type',
      'Topic subscription filters — a SQL filter on a message property like "CustomerType = \'Premium\'"',
      'Dead-letter queue routing based on message content',
      'Event Grid integration to re-route messages to different queues',
    ],
    a: 1,
    exp: 'Topic subscriptions can have SQL filters that evaluate message properties. You create two subscriptions each with a different SQL filter (e.g., CustomerType = \'Premium\' and CustomerType = \'Standard\'). Only matching messages are routed to each subscription.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'An Event Hubs Capture feature is enabled on a namespace. Where are the captured events stored and in what format?',
    opts: [
      'Azure SQL Database in a pre-defined schema',
      'Azure Blob Storage or Azure Data Lake Storage in Avro format',
      'Azure Cosmos DB in JSON format',
      'Azure Table Storage in OData format',
    ],
    a: 1,
    exp: 'Event Hubs Capture stores events in Azure Blob Storage or Azure Data Lake Storage Gen2 in Apache Avro format. The capture window is time-based and/or size-based. Avro is a compact binary format with embedded schema.',
  },
  // Additional questions to reach 65+
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You need to pass configuration values to an AKS pod that are non-sensitive (such as the environment name and feature flags). Which Kubernetes resource is appropriate?',
    opts: [
      'Kubernetes Secret with stringData',
      'Kubernetes ConfigMap with data key-value pairs',
      'Azure Key Vault Secret accessed via CSI driver',
      'Pod environment variables hardcoded in the Deployment manifest',
    ],
    a: 1,
    exp: 'ConfigMaps store non-sensitive configuration data as key-value pairs and can be injected into pods as environment variables or mounted as files. Secrets are for sensitive data. Hardcoding values in the Deployment manifest makes them difficult to change without redeploying.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'A Bicep template deploys a storage account and then a web app that needs the storage connection string. How should you pass the connection string from the storage account module to the web app resource?',
    opts: [
      'Hardcode the connection string in the Bicep template as a variable',
      'Use the storage account module\'s output and reference it in the web app resource properties',
      'Store the connection string in a parameter file and pass it to both modules',
      'Deploy the storage account first in a separate pipeline and then pass the value as a pipeline variable',
    ],
    a: 1,
    exp: 'Bicep modules expose outputs that can be referenced by other resources in the same template. The connection string can be built from the storage account module\'s outputs (e.g., using listKeys()). This avoids hardcoding and keeps the template self-contained.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'You need to copy a blob from one container to another within the same storage account using the .NET SDK without downloading and re-uploading the data. Which method should you use?',
    opts: [
      'BlobClient.DownloadAsync() followed by destinationBlobClient.UploadAsync()',
      'BlobClient.StartCopyFromUriAsync() with the source blob URI',
      'BlobContainerClient.CreateAsync() and BlobClient.SetMetadataAsync()',
      'StorageManagementClient.BlobContainers.CreateAsync() with copy flag',
    ],
    a: 1,
    exp: 'StartCopyFromUriAsync() initiates a server-side copy operation within Azure storage without transferring data through the client. This is efficient for large blobs. DownloadAsync() + UploadAsync() unnecessarily downloads and re-uploads data through the client machine.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'You have a Key Vault with soft-delete enabled. An application accidentally deletes a secret. What is the maximum retention period for recovering a soft-deleted secret?',
    opts: [
      '7 days',
      '30 days',
      '90 days',
      '180 days',
    ],
    a: 2,
    exp: 'Azure Key Vault soft-delete retains deleted secrets, keys, and certificates for a configurable period between 7 and 90 days (default is 90 days). During the retention period, the deleted item can be recovered. After the period, it is permanently purged.',
  },
  {
    domain: 'Monitor, Troubleshoot & Optimize',
    q: 'You need to track the number of active user sessions in your application using Application Insights. Which telemetry type is MOST appropriate for storing and visualizing this as a time-series metric?',
    opts: [
      'TrackEvent("SessionStarted") and count events in the portal',
      'TrackMetric("ActiveSessions", currentCount) sent periodically',
      'TrackTrace("Active sessions: " + count) at each interval',
      'TrackRequest() for each session heartbeat',
    ],
    a: 1,
    exp: 'TrackMetric() sends a numeric measurement that Application Insights aggregates into time-series data, suitable for charts and alerts. TrackEvent() creates event records that can be counted but are not native numeric metrics. TrackTrace() is for log messages, not metrics.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'An Azure Function uses an Event Hub trigger. The function processes events in batches. Which binding configuration property controls the maximum batch size?',
    opts: [
      'maxBatchSize in host.json under extensions.eventHubs',
      'batchSize in the function.json binding configuration',
      'maxConcurrentCalls in host.json',
      'prefetchCount in the EventProcessorClient configuration',
    ],
    a: 0,
    exp: 'The maxBatchSize property in host.json under extensions.eventHubs controls the maximum number of events received per batch. The default varies by SDK version. batchSize is not a standard Event Hub trigger binding property in function.json.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'A Cosmos DB container uses partition key `/tenantId`. You need to calculate the approximate RU charge for a point read of a 1 KB item. What is the standard RU cost?',
    opts: [
      '0.1 RUs',
      '1 RU',
      '5 RUs',
      '10 RUs',
    ],
    a: 1,
    exp: 'A point read (ReadItem by ID + partition key) of a 1 KB item costs 1 RU in Azure Cosmos DB. This is the most efficient read operation. Queries cost more RUs because they require index lookups. Write operations typically cost more than reads.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'You publish a custom event to an Azure Event Grid custom topic. The event payload uses the CloudEvents 1.0 schema instead of the Event Grid schema. What HTTP header must be set on the publish request?',
    opts: [
      'Content-Type: application/json',
      'Content-Type: application/cloudevents+json',
      'Content-Type: application/cloudevents-batch+json',
      'Aeg-Event-Type: CloudEvent',
    ],
    a: 1,
    exp: 'When publishing a single CloudEvent to an Event Grid topic, the Content-Type header must be application/cloudevents+json. For batches, use application/cloudevents-batch+json. Using application/json signals the Event Grid native schema format.',
  },
  {
    domain: 'Monitor, Troubleshoot & Optimize',
    q: 'A KQL query needs to find all requests to a specific URL path that took more than 1 second in the last 24 hours. Which query is correct?',
    opts: [
      'requests | where url contains "/api/orders" and duration > 1000 | where timestamp > ago(24h)',
      'requests | where timestamp > ago(24h) | where url contains "/api/orders" | where duration > 1000',
      'AppRequests | where Url contains "/api/orders" | where DurationMs > 1000 | where TimeGenerated > ago(24h)',
      'Both B and C are correct depending on whether you are using Application Insights or workspace-based queries',
    ],
    a: 3,
    exp: 'Application Insights classic tables use lowercase names (requests, exceptions) with timestamp and duration (in ms). Workspace-based Application Insights uses AppRequests with TimeGenerated and DurationMs. Both query structures are valid for their respective table schemas. The order of where clauses does not affect results but can affect performance.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'An MSAL.NET application acquires a token and then makes a second request for the same resource 30 minutes later. MSAL attempts silent token acquisition first. What does this mean?',
    opts: [
      'MSAL silently re-authenticates the user without displaying any prompt',
      'MSAL checks its token cache and returns the cached access token if it is not expired; otherwise it uses the refresh token to get a new access token',
      'MSAL makes a background network request to Azure AD without caching',
      'MSAL prompts the user with a consent dialog in a hidden browser window',
    ],
    a: 1,
    exp: 'Silent token acquisition first checks the in-memory token cache. If the access token is still valid, it is returned directly. If expired, MSAL uses the cached refresh token to acquire a new access token from Azure AD without user interaction. This is the recommended approach to avoid unnecessary interactive prompts.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You need to expose an Azure Container App to the internet on a custom domain with TLS termination. Which Azure Container Apps feature handles this?',
    opts: [
      'Configure ingress with external traffic type and add a custom domain with a managed or customer-provided certificate',
      'Deploy an Azure Application Gateway in front of the Container Apps environment',
      'Enable Public IP on the Container Apps environment and configure DNS manually',
      'Use Azure Front Door CDN with origin set to the Container App internal URL',
    ],
    a: 0,
    exp: 'Azure Container Apps supports custom domains with TLS termination directly through the ingress configuration. You can use Azure-managed certificates (auto-provisioned) or bring your own certificate. External ingress makes the app internet-accessible. Application Gateway and Front Door are additional layers that may be used for advanced scenarios but are not required.',
  },
  {
    domain: 'Develop for Azure Storage',
    q: 'You are using the Cosmos DB SDK and need to upsert a document. Which SDK method should you use to insert the document if it does not exist, or replace it if it does?',
    opts: [
      'container.CreateItemAsync<T>(item, partitionKey)',
      'container.UpsertItemAsync<T>(item, partitionKey)',
      'container.ReplaceItemAsync<T>(item, id, partitionKey)',
      'container.PatchItemAsync(id, partitionKey, patchOperations)',
    ],
    a: 1,
    exp: 'UpsertItemAsync() performs an insert-or-replace operation atomically. If the item exists (matching ID and partition key), it is replaced; otherwise, it is created. CreateItemAsync() fails if the item exists. ReplaceItemAsync() fails if the item does not exist.',
  },
  {
    domain: 'Connect & Consume Azure Services',
    q: 'Azure Event Grid uses an exponential back-off retry policy. After the maximum retry attempts are exhausted (default 30 attempts over 24 hours), what happens to the event if no dead-letter destination is configured?',
    opts: [
      'The event is stored in Azure Table Storage for manual review',
      'The event is permanently dropped',
      'The event is sent back to the Event Grid topic for reprocessing',
      'The event triggers an Azure Monitor alert and is held for 7 days',
    ],
    a: 1,
    exp: 'Without a dead-letter destination, events that exhaust all retry attempts are permanently dropped. This is why configuring a dead-letter storage container is recommended for production event subscriptions to avoid losing events.',
  },
  {
    domain: 'Monitor, Troubleshoot & Optimize',
    q: 'You need to run Application Insights Profiler to capture CPU call stacks for an App Service app. What must be enabled on the App Service for Profiler to work?',
    opts: [
      'The App Service must be on an Isolated (ASE) plan',
      'The Application Insights Profiler site extension must be enabled and Always On must be turned on',
      'Remote debugging must be enabled in the App Service configuration',
      'The app must be deployed as a Docker container, not code',
    ],
    a: 1,
    exp: 'Application Insights Profiler requires the Profiler site extension to be enabled in the Application Insights blade for the App Service. Always On should be enabled to prevent the app from going idle, which would prevent profiler sessions from starting. It works with both code and container deployments.',
  },
  {
    domain: 'Develop Azure Compute Solutions',
    q: 'You need to build a Durable Functions solution that runs a sequence of activities where each activity\'s output is the next activity\'s input. Which pattern is this?',
    opts: [
      'Fan-out/fan-in',
      'Function chaining',
      'Async HTTP API',
      'Human interaction',
    ],
    a: 1,
    exp: 'Function chaining is the sequential execution of functions where the output of one function is passed as input to the next. This is the simplest Durable Functions pattern. Fan-out/fan-in runs functions in parallel. Async HTTP API is for polling long-running operations via HTTP.',
  },
  {
    domain: 'Implement Azure Security',
    q: 'An Azure AD app registration is configured as a multi-tenant app. A user from an external tenant tries to sign in. Which issuer value should the API validate against to accept multi-tenant tokens?',
    opts: [
      'https://login.microsoftonline.com/{your-tenant-id}/v2.0',
      'https://login.microsoftonline.com/common/v2.0',
      'https://login.microsoftonline.com/{organizations}/v2.0',
      'The API should validate the `tid` (tenant ID) claim is in an allowed-tenant list rather than a fixed issuer',
    ],
    a: 3,
    exp: 'For multi-tenant apps, the issuer contains the caller\'s tenant ID, which varies per caller. Validating against a fixed issuer URL would reject tokens from external tenants. The correct approach is to validate the `tid` claim against an allowlist of authorized tenants, combined with the standard signature and audience validation.',
  },
];
