import type { Question } from '../types';

export const BANK_DOP: Question[] = [
  // ── SDLC Automation (~17 questions) ──────────────────────────────────────

  {
    domain: 'SDLC Automation',
    q: 'A development team uses AWS CodeCommit and follows a Gitflow branching strategy. They want to prevent developers from pushing directly to the "main" branch and require pull requests to be reviewed by at least one approver before merging. Which CodeCommit feature should they configure?',
    opts: [
      'Enable branch-level IAM policies that deny the codecommit:GitPush action on the main branch and create an approval rule template requiring one approver.',
      'Create a CodeBuild project that validates commits and blocks merges using a webhook trigger.',
      'Use AWS Organizations SCPs to restrict direct pushes to the main branch for all IAM users.',
      'Configure a CodePipeline stage with a manual approval action before the merge step.',
    ],
    a: 0,
    exp: 'CodeCommit supports branch-level IAM condition keys (codecommit:References) to deny direct pushes, and approval rule templates enforce the minimum number of reviewers on pull requests. This is the native CodeCommit mechanism designed exactly for this Gitflow governance requirement.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A team\'s CodeBuild project builds a Node.js application and takes 12 minutes per run due to npm install downloading hundreds of packages. Build logs show the cache is never being used. Which buildspec change will MOST effectively reduce build time?',
    opts: [
      'Add a cache section specifying paths: ["/root/.npm/**/*"] and set the cache type to S3 in the project settings.',
      'Increase the compute type to compute:BUILD_GENERAL1_LARGE to improve download speed.',
      'Add a pre_build phase that runs "npm ci --prefer-offline" before the build phase.',
      'Store the node_modules directory in an EFS volume mounted to the CodeBuild environment.',
    ],
    a: 0,
    exp: 'CodeBuild\'s S3 caching feature stores specified directories between builds; caching the npm cache directory (/root/.npm) means packages are restored from S3 rather than re-downloaded from the internet on each run. This is the standard, low-overhead solution for dependency caching in CodeBuild.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A CodeBuild project needs to build a Docker image and push it to ECR, but the build must also run integration tests against a private RDS database that is not publicly accessible. What is the correct approach?',
    opts: [
      'Place the CodeBuild project in a VPC with a subnet that has a route to the RDS instance, and configure appropriate security group rules.',
      'Use a NAT Gateway to expose the RDS database to the public internet temporarily during the build.',
      'Create a VPC peering connection between the default CodeBuild VPC and the RDS VPC.',
      'Use an EC2 bastion host as a proxy; configure CodeBuild to tunnel through it using SSH port forwarding.',
    ],
    a: 0,
    exp: 'CodeBuild supports VPC configuration, which places build containers inside a specified VPC subnet. This allows the build to reach private resources like RDS using standard VPC routing and security groups without exposing those resources to the internet.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A company uses CodeDeploy to deploy to an Auto Scaling group. During a deployment, a new application version causes errors and needs to be rolled back immediately. The team wants future deployments to automatically roll back when CloudWatch alarms trigger. Which configuration achieves this?',
    opts: [
      'In the CodeDeploy deployment group, enable automatic rollbacks and specify the CloudWatch alarm names that should trigger a rollback.',
      'Create a Lambda function subscribed to the CodeDeploy SNS topic that calls the StopDeployment API when alarm state changes.',
      'Use a CodePipeline manual approval action so operators can approve or reject before production deployment.',
      'Configure the CodeDeploy appspec.yml to include a ValidateService hook that checks alarm state and exits with code 1 on failure.',
    ],
    a: 0,
    exp: 'CodeDeploy deployment groups have a built-in "Automatic Rollback" feature where you specify CloudWatch alarm names; when any listed alarm enters ALARM state during deployment, CodeDeploy automatically rolls back to the previous revision. This is the native, lowest-friction mechanism for alarm-triggered rollback.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A DevOps engineer is configuring a CodeDeploy appspec.yml for an EC2 in-place deployment. They need to stop the application server before files are copied, copy files, then start the server. In which lifecycle event hooks should these actions be placed?',
    opts: [
      'ApplicationStop for stopping the server, and ApplicationStart for starting it after files are copied.',
      'BeforeInstall for stopping the server, and AfterInstall for starting it.',
      'ValidateService for stopping the server, and AfterAllowTraffic for starting it.',
      'DownloadBundle for stopping the server, and Install for starting it.',
    ],
    a: 0,
    exp: 'The ApplicationStop hook runs before the new revision is downloaded and is the correct place to stop the running application. ApplicationStart runs after Install completes and is the correct hook to restart the application server. BeforeInstall and AfterInstall are also valid but ApplicationStop/ApplicationStart map most semantically to start/stop lifecycle actions.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A company wants to implement a Blue/Green deployment strategy using CodeDeploy for their ECS Fargate service. During the deployment, 10% of traffic should shift to the new (green) task set immediately, then 100% should shift after 10 minutes if no alarms fire. Which deployment configuration should they use?',
    opts: [
      'CodeDeployDefault.ECSCanary10Percent5Minutes',
      'CodeDeployDefault.ECSLinear10PercentEvery1Minutes',
      'CodeDeployDefault.ECSAllAtOnce',
      'CodeDeployDefault.ECSCanary10Percent10Minutes',
    ],
    a: 3,
    exp: 'The ECSCanary10Percent10Minutes configuration shifts 10% of traffic immediately to the new task set, waits 10 minutes, then shifts the remaining 90% — matching the described requirement exactly. The 5-minute variant would shift remaining traffic after only 5 minutes.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A development team wants to set up a CodePipeline that deploys to a staging account and then, after manual approval, deploys to the production account. Both accounts are in the same AWS Organization. What is the recommended approach for the cross-account deployment?',
    opts: [
      'Create a cross-account IAM role in the production account that the pipeline role in the staging account can assume, and use that role in the Deploy action.',
      'Share the CodePipeline artifact S3 bucket using S3 bucket ACLs so the production account can read artifacts directly.',
      'Use AWS Resource Access Manager (RAM) to share the CodePipeline pipeline resource across accounts.',
      'Duplicate the pipeline in the production account and trigger it via an EventBridge cross-account event rule.',
    ],
    a: 0,
    exp: 'Cross-account CodePipeline deployments use IAM role chaining: a deployment role in the target (production) account trusts the pipeline\'s execution role in the source account. The pipeline assumes this role to perform deployment actions in the production account, with the artifact S3 bucket and KMS key also configured for cross-account access.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A pipeline has three independent test stages (unit tests, security scan, and integration tests) that each take about 10 minutes. They currently run sequentially, making the pipeline 30+ minutes long. How can a DevOps engineer run all three simultaneously to reduce pipeline duration?',
    opts: [
      'Create three separate CodePipeline pipelines, one for each test type, and trigger them all from the same source event.',
      'Place all three test actions within the same CodePipeline stage so they execute in parallel.',
      'Use a CodeBuild batch build with the BUILD_LIST batch type to run the three buildspecs concurrently.',
      'Add a Lambda action before the tests that invokes all three CodeBuild projects asynchronously.',
    ],
    a: 1,
    exp: 'In CodePipeline, actions within the same stage run in parallel by default. By placing the unit test, security scan, and integration test actions in a single stage, all three execute concurrently, reducing the total time from ~30 minutes to ~10 minutes.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A company stores private npm packages in AWS CodeArtifact. Developers\' machines need to authenticate with CodeArtifact to install packages. The authentication token expires every 12 hours. What is the recommended approach to automate re-authentication in CI/CD pipelines?',
    opts: [
      'Store a long-lived CodeArtifact token in AWS Secrets Manager and retrieve it in the buildspec pre_build phase.',
      'Use the "aws codeartifact login" command in the CodeBuild pre_build phase, which automatically retrieves a fresh token using the IAM role.',
      'Create an IAM user with CodeArtifact permissions and embed its access keys in the buildspec environment variables.',
      'Configure an API Gateway endpoint that proxies requests to CodeArtifact with a static API key for authentication.',
    ],
    a: 1,
    exp: 'The "aws codeartifact login" command obtains a fresh authentication token using the environment\'s IAM credentials (the CodeBuild service role) and configures the package manager automatically. Running this in the pre_build phase ensures a valid token for every build without storing long-lived secrets.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A team wants to implement feature flags in their AWS-based application so that new features can be toggled on or off without deploying new code. Which AWS service is purpose-built for this use case?',
    opts: [
      'AWS AppConfig with a feature flag configuration profile',
      'AWS Systems Manager Parameter Store with SecureString parameters',
      'Amazon DynamoDB with a configuration table polled by the application',
      'AWS Secrets Manager with rotation disabled for static flag values',
    ],
    a: 0,
    exp: 'AWS AppConfig is purpose-built for application configuration management, including feature flags. It supports feature flag configuration profiles with validators, staged rollouts, and automatic rollback on CloudWatch alarm thresholds — all without requiring a new code deployment.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A DevOps engineer needs to add load testing to a CodePipeline. The load test should run after deployment to a staging environment and fail the pipeline if p99 latency exceeds 500ms. Which approach is most operationally efficient?',
    opts: [
      'Add a CodeBuild action that runs a load testing tool (e.g., k6 or Artillery), then parses the results and exits with a non-zero code if the p99 threshold is exceeded.',
      'Add a manual approval action where a QA engineer manually reviews load test results before approving.',
      'Deploy to a dedicated load-testing EC2 instance, run tests there, and use SNS to notify the pipeline of results.',
      'Use AWS X-Ray to measure p99 latency during the deployment and fail the deployment group if thresholds are exceeded.',
    ],
    a: 0,
    exp: 'A CodeBuild action can run any load testing tool, capture the output, programmatically evaluate the p99 latency against the threshold, and exit with a non-zero code to fail the pipeline stage automatically. This is the most automated and scalable approach for continuous load testing in a pipeline.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A company has a monorepo in CodeCommit with three microservices. They want CodePipeline to trigger only the relevant service\'s pipeline when files in that service\'s directory change. How should this be implemented?',
    opts: [
      'Use an EventBridge rule with a codecommit:referenceUpdated event and a filter on the "referenceFullName" field.',
      'Configure an EventBridge rule that triggers a Lambda function to check which directories changed; the Lambda then conditionally starts the appropriate pipelines using the CodePipeline StartPipelineExecution API.',
      'Create a single pipeline with three parallel stages, each checking if its service directory was modified before proceeding.',
      'Use CodeCommit triggers with AWS Lambda to analyze the diff and invoke the appropriate pipeline.',
    ],
    a: 1,
    exp: 'EventBridge CodeCommit events do not natively filter on changed file paths. The correct approach is to trigger a Lambda on commit events; the Lambda uses the CodeCommit GetDifferences API to determine which directories changed, then calls StartPipelineExecution only for affected pipelines.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A CodeBuild project builds a Docker image and needs to access secrets (database credentials) during the build. The secrets must not appear in build logs. What is the most secure way to provide these secrets to the build?',
    opts: [
      'Pass the secrets as plaintext environment variables in the CodeBuild project configuration.',
      'Reference the secrets using the parameter-store or secrets-manager environment variable type in the buildspec or project configuration so CodeBuild resolves them at runtime without logging their values.',
      'Encode the secrets in Base64 and store them as standard environment variables, then decode them in the buildspec.',
      'Create a custom Docker image with the secrets baked in and use that as the CodeBuild environment image.',
    ],
    a: 1,
    exp: 'CodeBuild supports environment variables backed by Parameter Store (SecureString) or Secrets Manager, resolved at build time. These values are never echoed to build logs, satisfying the security requirement. Plaintext environment variables and Base64 encoding both expose secrets in logs or configuration.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A team uses CodePipeline with a GitHub source action. They notice the pipeline sometimes fails because multiple rapid commits trigger concurrent pipeline executions that conflict during deployment. How should they prevent this?',
    opts: [
      'Set the pipeline execution mode to SUPERSEDED so that newer executions replace older ones that are still in progress.',
      'Add a wait step at the beginning of each pipeline that checks for running executions and sleeps for 60 seconds.',
      'Configure CodePipeline with a Lambda function that cancels any existing execution before starting a new one.',
      'Use CodeCommit instead of GitHub, as CodeCommit prevents concurrent pipeline executions natively.',
    ],
    a: 0,
    exp: 'CodePipeline\'s SUPERSEDED execution mode (the default) automatically queues new executions and supersedes in-progress ones with the latest commit, preventing conflicting concurrent deployments. Switching to QUEUED mode would execute them serially; SUPERSEDED drops intermediate commits and always runs the latest.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A company wants to enforce that all CodeBuild projects use an approved list of Docker base images to prevent supply-chain attacks. Which combination of services best enforces this at pipeline time?',
    opts: [
      'Use AWS Config to detect non-compliant CodeBuild projects and send SNS notifications to developers.',
      'Integrate Amazon Inspector into the CodePipeline to scan images and fail the pipeline if unapproved base images are detected.',
      'Add a CodeBuild step that uses AWS CLI to check the buildspec environment image against an approved list in Parameter Store and fails the build if it is not approved.',
      'Use ECR image scanning with enhanced scanning and block pulls of images with critical vulnerabilities.',
    ],
    a: 2,
    exp: 'Adding a validation CodeBuild step that reads an approved image list from Parameter Store and compares it to the project\'s environment image provides a pipeline-time enforcement gate. This fails the pipeline immediately for non-compliant images, preventing unapproved base images from ever being built or deployed.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A DevOps engineer needs to configure a CodeDeploy deployment for EC2 instances that requires at least 75% of instances to remain in service at all times during deployment. Which deployment configuration should they create?',
    opts: [
      'A custom deployment configuration with MinimumHealthyHosts type FLEET_PERCENT set to 75.',
      'A custom deployment configuration with MinimumHealthyHosts type HOST_COUNT set to 75.',
      'Use CodeDeployDefault.HalfAtATime, which keeps 50% healthy during deployment.',
      'Use CodeDeployDefault.OneAtATime, which keeps all instances except one healthy.',
    ],
    a: 0,
    exp: 'Custom CodeDeploy deployment configurations support MinimumHealthyHosts with type FLEET_PERCENT, which specifies the percentage of instances that must remain healthy. Setting it to 75 ensures at least 75% of instances stay in service during the rolling update.',
  },
  {
    domain: 'SDLC Automation',
    q: 'A team wants to run unit tests in CodeBuild and publish test results so they can be viewed in the AWS console alongside build history. What must be added to the buildspec.yml to enable this?',
    opts: [
      'A reports section in buildspec.yml specifying the test report group name and the path to the test result files in a supported format (e.g., JUnit XML).',
      'An artifacts section that uploads the test XML files to S3, then a separate Lambda that parses and displays them.',
      'A post_build phase that calls "aws codebuild put-resource-policy" to publish test results.',
      'A CloudWatch Logs metric filter that extracts pass/fail counts from build log output.',
    ],
    a: 0,
    exp: 'CodeBuild\'s native test reporting feature is configured via the "reports" section in buildspec.yml, where you specify a report group and the location of test output files in formats like JUnit XML or Cucumber JSON. CodeBuild then parses and displays these results in the console as a test report.',
  },

  // ── Configuration Management & IaC (~13 questions) ──────────────────────

  {
    domain: 'Configuration Management & IaC',
    q: 'A company manages infrastructure with CloudFormation and wants to deploy a stack set to all accounts in an AWS Organization\'s OU. They need new accounts added to the OU to automatically receive the stack. Which StackSets feature enables this?',
    opts: [
      'Enable automatic deployment with account filtering set to the target OU; new accounts in the OU will receive the stack automatically.',
      'Create an EventBridge rule that detects new account creation events and calls the CloudFormation CreateStackInstances API.',
      'Use an AWS Config rule to detect accounts without the stack and trigger an SSM Automation runbook to deploy it.',
      'Configure a CloudFormation custom resource that periodically lists accounts in the OU and creates stack instances for new ones.',
    ],
    a: 0,
    exp: 'CloudFormation StackSets with service-managed permissions support "Automatic Deployment," which is a setting on the stack set that automatically creates stack instances in new accounts as they join a targeted OU. This eliminates the need for any custom automation.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'A CloudFormation stack update is failing because a resource is in a state that prevents update. A DevOps engineer wants to detect which resources have drifted from the stack\'s expected configuration before troubleshooting. What should they do?',
    opts: [
      'Run "aws cloudformation detect-stack-drift" and then "aws cloudformation describe-stack-resource-drifts" to see which resources have drifted and what properties changed.',
      'Delete and recreate the stack from the template to reset all resources to their expected state.',
      'Use AWS Config to query resource configuration history and compare it with the CloudFormation template.',
      'Enable CloudTrail and search for API calls that modified stack resources since the last successful update.',
    ],
    a: 0,
    exp: 'CloudFormation drift detection (DetectStackDrift) compares actual resource configurations against the stack template and reports differences per-resource. DescribeStackResourceDrifts shows exactly which properties differ, making it the direct native tool for this investigation.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'A team is using CloudFormation with a custom resource backed by Lambda to provision a third-party SaaS account during stack creation. After testing, they notice that if the Lambda function times out, the stack hangs indefinitely. What should the Lambda function always do to prevent this?',
    opts: [
      'Set a CloudFormation stack creation timeout of 30 minutes so the stack eventually fails.',
      'Send a response to the CloudFormation pre-signed S3 URL (ResponseURL) with a SUCCESS or FAILED status in all code paths, including exception handlers.',
      'Configure the Lambda with a Destinations (on-failure) target that sends a FAILED response via SNS.',
      'Use cfn-response module only; do not use custom response logic.',
    ],
    a: 1,
    exp: 'CloudFormation custom resources wait for the Lambda to PUT a response to the pre-signed S3 ResponseURL. If the Lambda times out or throws an unhandled exception without sending this response, the stack hangs until the stack operation timeout. Every code path — including exception handlers — must send a SUCCESS or FAILED response to the ResponseURL.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'A company is deploying a multi-tier application using CloudFormation nested stacks. The root stack creates a networking stack (VPC, subnets) and an application stack that needs the VPC ID from the networking stack. How should the VPC ID be passed to the application stack?',
    opts: [
      'Use the Fn::ImportValue function in the application stack to import the VPC ID exported by the networking stack.',
      'In the root stack, use Fn::GetAtt to retrieve the VPC ID output from the networking nested stack resource, then pass it as a parameter to the application nested stack.',
      'Store the VPC ID in SSM Parameter Store in the networking stack and read it from Parameter Store in the application stack.',
      'Hard-code the VPC ID in the application stack template after the networking stack is created.',
    ],
    a: 1,
    exp: 'In nested stack patterns, the root stack orchestrates data flow: it retrieves outputs from the networking nested stack using Fn::GetAtt (e.g., NetworkingStack.Outputs.VpcId) and passes them as parameters to the application nested stack. This keeps the dependency within the root stack and avoids cross-stack export coupling.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'A team wants CloudFormation to wait for EC2 instances to complete bootstrapping (installing software, running health checks) before marking the stack as CREATE_COMPLETE. Which CloudFormation features enable this?',
    opts: [
      'Use cfn-init to install software and cfn-signal to send a success signal; configure a CreationPolicy with ResourceSignal on the EC2 resource specifying Count and Timeout.',
      'Add a WaitCondition resource with a 5-minute DependsOn on the EC2 instance.',
      'Use a Lambda-backed custom resource that polls the EC2 instance status before returning SUCCESS.',
      'Enable CloudFormation rollback triggers with a CloudWatch alarm on EC2 StatusCheckFailed.',
    ],
    a: 0,
    exp: 'The cfn-init helper installs packages and runs commands defined in the CloudFormation metadata, and cfn-signal sends a completion signal to CloudFormation. The CreationPolicy with ResourceSignal on the EC2 resource causes CloudFormation to wait for the specified number of signals before proceeding, ensuring bootstrapping completes before the stack is marked complete.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'A team migrating to AWS CDK wants to enforce that all S3 buckets created across all CDK apps in the organization have versioning enabled and server-side encryption configured. What CDK feature allows them to apply this rule across all constructs in a CDK app?',
    opts: [
      'CDK Aspects, which implement the IAspect interface and are applied to a construct scope, visiting every child construct to validate or mutate properties.',
      'CDK Constructs Level 2 (L2), which automatically enable versioning and encryption by default.',
      'CDK Context values stored in cdk.json that set default bucket properties for all instantiations.',
      'A CDK pipeline stage with a custom validation step that checks bucket properties before deployment.',
    ],
    a: 0,
    exp: 'CDK Aspects are the mechanism for cross-cutting concerns in CDK: they implement IAspect with a visit() method and are applied to a scope. CDK calls visit() on every construct in the scope tree, allowing you to enforce, mutate, or validate any property — including adding versioning and encryption to every S3 bucket.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'An AWS Config rule detects that S3 buckets lack server-side encryption. The team wants non-compliant buckets to be automatically remediated. Which approach should be used?',
    opts: [
      'Configure an AWS Config remediation action linked to an SSM Automation document that enables default encryption on the non-compliant bucket.',
      'Create a CloudWatch Events rule that triggers a Lambda function to enable encryption whenever a bucket is found non-compliant.',
      'Use an SCP to prevent creation of unencrypted buckets going forward, and manually fix existing non-compliant buckets.',
      'Enable AWS Security Hub to automatically remediate S3 encryption findings using its built-in response playbooks.',
    ],
    a: 0,
    exp: 'AWS Config supports remediation actions that are directly associated with a Config rule. When triggered, the remediation executes an SSM Automation document (such as AWS-EnableS3BucketEncryption) with the non-compliant resource as input. This is the native Config remediation path without additional event orchestration.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'A company uses Systems Manager State Manager to ensure EC2 instances always have the latest CrowdStrike agent installed. The association should run every 30 minutes and only on instances tagged Environment=Production. How should the association be configured?',
    opts: [
      'Create an SSM Association with the target as "tag:Environment=Production", the document specifying the install command, and a schedule expression of "rate(30 minutes)".',
      'Create a CloudWatch Events rule on a 30-minute schedule that triggers a Lambda to call SSM SendCommand on tagged instances.',
      'Use AWS Config with a custom rule that checks for the agent and triggers an SSM Run Command via remediation.',
      'Deploy an EC2 user data script that runs a cron job every 30 minutes to update the agent.',
    ],
    a: 0,
    exp: 'State Manager Associations support tag-based targeting (using the "tag:Key=Value" target type) and schedule expressions including rate expressions. This ensures the association runs every 30 minutes exclusively on Production-tagged instances without any additional orchestration.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'A DevOps engineer uses an SSM Automation document to patch EC2 instances during a maintenance window. After patching, instances should be rebooted only if required. Which SSM document is designed for this workflow?',
    opts: [
      'AWS-RunPatchBaseline with the operation parameter set to "Install" — this document patches instances and reboots only when required by the patch.',
      'AWS-ApplyPatchBaseline with the rebootOption parameter set to "RebootIfNeeded".',
      'AWS-InstallWindowsUpdates with the RebootOption set to "NoAutoReboot".',
      'AWS-RunShellScript with a custom script that checks if a reboot is required and conditionally reboots.',
    ],
    a: 0,
    exp: 'AWS-RunPatchBaseline is the standard SSM document for patch management, supporting both Scan and Install operations. When set to Install, it patches instances according to the patch baseline and handles conditional reboots based on whether the installed patches require it.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'A team wants to use CloudFormation UpdatePolicy on an Auto Scaling group to perform a rolling update when the launch template changes. They want updates to replace only 25% of instances at a time. Which UpdatePolicy configuration achieves this?',
    opts: [
      'AutoScalingRollingUpdate with MaxBatchSize set to 25% of the desired capacity and MinInstancesInService set to 75% of desired capacity.',
      'AutoScalingReplacingUpdate with WillReplace set to true.',
      'AutoScalingScheduledAction with a 25% batch size schedule.',
      'AutoScalingRollingUpdate with PauseTime of PT25M and SuspendProcesses for 75% of instances.',
    ],
    a: 0,
    exp: 'AutoScalingRollingUpdate UpdatePolicy controls rolling replacements of ASG instances. MaxBatchSize limits how many instances are replaced at once, and MinInstancesInService ensures a minimum number remain in service during the update, together achieving a 25%-at-a-time rolling replacement.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'A company has 50 CloudFormation stacks across 10 AWS accounts. They want to detect configuration drift across all stacks simultaneously. What is the most efficient approach?',
    opts: [
      'Use CloudFormation StackSets drift detection, which can detect drift across all stack instances in multiple accounts simultaneously.',
      'Write a script that iterates over all 50 stacks in all accounts and calls DetectStackDrift sequentially.',
      'Use AWS Config to compare resource configurations against expected values from the templates.',
      'Use AWS Trusted Advisor to identify resources that have drifted from their CloudFormation-managed state.',
    ],
    a: 0,
    exp: 'CloudFormation StackSets supports drift detection at the stack set level, launching drift detection operations concurrently across all stack instances in all target accounts. This is far more efficient than scripting sequential per-stack drift detection calls.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'A CDK application defines three stacks: NetworkStack, DatabaseStack (depends on NetworkStack), and AppStack (depends on both). A developer wants to deploy only AppStack in isolation for testing. What CDK CLI command should they use?',
    opts: [
      '"cdk deploy AppStack" — CDK will deploy only AppStack but will fail if its dependencies are not already deployed.',
      '"cdk deploy AppStack --exclusively" — this flag deploys only the specified stack without deploying dependency stacks.',
      '"cdk deploy AppStack --force" — this bypasses dependency checks and deploys only AppStack.',
      '"cdk synth AppStack | aws cloudformation deploy" — pipe the synthesized template directly to CloudFormation.',
    ],
    a: 1,
    exp: 'The "--exclusively" flag in CDK deploy skips automatic deployment of dependency stacks and deploys only the specified stack. Without this flag, CDK would also deploy NetworkStack and DatabaseStack if they are not yet deployed.',
  },
  {
    domain: 'Configuration Management & IaC',
    q: 'A team manages EC2 instances with AWS OpsWorks Stacks. They want to run a Chef recipe on all instances in a layer whenever a new application version is deployed. Which OpsWorks lifecycle event should the recipe be assigned to?',
    opts: [
      'Setup — runs when an instance finishes booting.',
      'Configure — runs on all instances in the stack when an instance enters or leaves the stack.',
      'Deploy — runs on all instances when you deploy an app.',
      'Undeploy — runs when you delete an app from a set of instances.',
    ],
    a: 2,
    exp: 'The OpsWorks Deploy lifecycle event is triggered when you deploy an application and runs the assigned Chef recipes on all instances in the layer. This is the correct event for application-version-specific configuration or file deployment tasks.',
  },

  // ── Resilient Cloud Solutions (~11 questions) ────────────────────────────

  {
    domain: 'Resilient Cloud Solutions',
    q: 'A company requires an RTO of less than 1 minute and an RPO of near-zero for their critical database. Which disaster recovery strategy best meets these requirements?',
    opts: [
      'Backup and Restore: take nightly snapshots to S3 and restore in the DR region when needed.',
      'Pilot Light: replicate data continuously to a DR region with minimal resources running; scale up when needed.',
      'Warm Standby: run a scaled-down but fully functional environment in the DR region with continuous replication.',
      'Multi-Site Active-Active: run identical environments in multiple regions simultaneously with live traffic distribution.',
    ],
    a: 3,
    exp: 'Active-Active multi-site architectures have both regions serving live traffic simultaneously, so failover requires only a DNS or routing change (RTO < 1 minute). Continuous traffic and data replication achieves near-zero RPO. The other strategies all require some recovery time that would exceed a 1-minute RTO.',
  },
  {
    domain: 'Resilient Cloud Solutions',
    q: 'A company uses Aurora MySQL in us-east-1 and needs to add a read replica in eu-west-1 to serve European users and enable fast failover if us-east-1 becomes unavailable. Which Aurora feature should they use?',
    opts: [
      'Aurora Multi-AZ deployment with a cross-region read replica.',
      'Aurora Global Database, which provides cross-region replication with a typical replication lag under 1 second and managed cross-region failover.',
      'RDS Database replication using binlog from the Aurora primary to an Aurora instance in eu-west-1.',
      'DMS ongoing replication task from the Aurora cluster to an Aurora cluster in eu-west-1.',
    ],
    a: 1,
    exp: 'Aurora Global Database is the correct feature: it replicates data to up to five secondary regions with typical lag under one second and supports managed cross-region failover that promotes a secondary region to primary in under a minute. It is purpose-built for this multi-region read scaling and DR use case.',
  },
  {
    domain: 'Resilient Cloud Solutions',
    q: 'A company wants to enable DynamoDB Global Tables for their order-management application across us-east-1 and ap-southeast-1. What is a key consistency consideration they must account for?',
    opts: [
      'Global Tables use synchronous replication, so writes to one region block until all other regions confirm the write.',
      'Global Tables use eventual consistency for cross-region replication; applications must handle the possibility of reading stale data in non-primary regions shortly after a write.',
      'Global Tables require a designated primary region; only the primary region accepts writes, and other regions are read-only.',
      'Global Tables do not support DynamoDB Streams, so replication must be handled by a custom Lambda function.',
    ],
    a: 1,
    exp: 'DynamoDB Global Tables use asynchronous replication between regions, meaning there is a brief window (typically under one second) where a region may serve slightly stale data after a write occurs in another region. Applications must be designed with this eventual consistency in mind, especially for scenarios requiring read-your-writes across regions.',
  },
  {
    domain: 'Resilient Cloud Solutions',
    q: 'A company uses Route 53 to route traffic between two regions using an active-passive failover policy. The primary region (us-east-1) becomes unhealthy. How should Route 53 be configured to detect this and failover automatically?',
    opts: [
      'Create a health check that monitors the primary endpoint; associate it with the primary record set using Failover routing policy set to PRIMARY. The secondary record set is set to SECONDARY. Route 53 will automatically route to the secondary when the health check fails.',
      'Use a latency-based routing policy; Route 53 will automatically route to the lower-latency region if the primary becomes slow.',
      'Create a CloudWatch alarm on the primary region\'s ALB and use EventBridge to trigger a Lambda that updates the Route 53 record.',
      'Enable Route 53 Resolver and configure conditional forwarding rules to redirect traffic to the secondary region.',
    ],
    a: 0,
    exp: 'Route 53 Failover routing policy works by associating health checks with record sets: the PRIMARY record is served when healthy, and Route 53 automatically serves the SECONDARY record when the PRIMARY health check fails. This is the native Route 53 mechanism for active-passive failover.',
  },
  {
    domain: 'Resilient Cloud Solutions',
    q: 'A DevOps team wants to perform chaos engineering on their ECS-based microservices to validate their resilience. They want to inject CPU stress on specific containers for a defined duration. Which AWS service supports this natively?',
    opts: [
      'AWS Fault Injection Service (FIS) with an ECS StressTask experiment action targeting containers by ECS cluster and service.',
      'AWS Systems Manager Run Command with a custom stress-ng script targeting ECS container instances.',
      'Amazon CloudWatch Synthetics canaries configured to generate artificial load on the services.',
      'AWS CodeBuild with a custom buildspec that runs stress tests against the ECS endpoints.',
    ],
    a: 0,
    exp: 'AWS Fault Injection Service (FIS) is AWS\'s purpose-built chaos engineering service. It includes ECS-specific experiment actions like cpu stress injection on tasks, allowing teams to validate resilience under realistic failure conditions with safety controls like stop conditions.',
  },
  {
    domain: 'Resilient Cloud Solutions',
    q: 'A company has S3 buckets in us-east-1 containing critical data that must be replicated to eu-central-1 for compliance. Replication must begin within 15 minutes for all objects. Which S3 feature guarantees this SLA?',
    opts: [
      'S3 Cross-Region Replication (CRR) with S3 Replication Time Control (RTC) enabled, which provides a 15-minute SLA for 99.99% of objects.',
      'S3 Standard Cross-Region Replication without RTC; it typically replicates objects within 15 minutes.',
      'S3 Batch Replication scheduled every 15 minutes to replicate new objects.',
      'S3 Transfer Acceleration combined with a cross-region Lambda trigger on S3 PUT events.',
    ],
    a: 0,
    exp: 'S3 Replication Time Control (RTC) adds an SLA guarantee of replicating 99.99% of objects within 15 minutes. Standard CRR replicates objects eventually but does not provide a guaranteed SLA. RTC is the correct choice when a 15-minute replication guarantee is required for compliance.',
  },
  {
    domain: 'Resilient Cloud Solutions',
    q: 'An application deployed across two regions uses an active-active architecture. A developer asks how to ensure that a user in Asia who writes data in ap-southeast-1 will read their own data from that region without seeing stale data from another region. What design pattern addresses this?',
    opts: [
      'Use a single DynamoDB Global Table and configure the application with a strong consistency read to always read from the primary region.',
      'Use a session-affinity (sticky) approach where a user\'s requests are always routed to the same region, ensuring they read from the region they wrote to.',
      'Use Route 53 latency routing to direct all writes and reads to the globally lowest-latency region.',
      'Use DynamoDB Accelerator (DAX) in each region to cache writes and ensure consistency.',
    ],
    a: 1,
    exp: 'Session affinity (geographic stickiness) ensures that a user\'s reads and writes always go to the same region, avoiding the cross-region eventual consistency problem. As long as the user\'s session is pinned to ap-southeast-1, they will always read their own latest writes from that region.',
  },
  {
    domain: 'Resilient Cloud Solutions',
    q: 'A company uses an Application Load Balancer (ALB) in us-west-2 and needs to failover to us-east-1 if the primary region becomes unavailable. The failover must be automatic and complete within 60 seconds. What is the recommended architecture?',
    opts: [
      'Create Route 53 health checks monitoring the us-west-2 ALB; configure a Failover routing policy with the us-west-2 record as PRIMARY and the us-east-1 record as SECONDARY. Reduce health check intervals to 10 seconds with a failure threshold of 3.',
      'Use CloudFront with an origin group containing both ALBs; CloudFront will automatically failover to the secondary origin if the primary returns errors.',
      'Configure Global Accelerator with both ALBs as endpoints; Global Accelerator routes traffic to the healthy endpoint based on health checks.',
      'Both B and C are valid approaches; the choice depends on whether caching is needed.',
    ],
    a: 3,
    exp: 'Both CloudFront origin failover (fast, edge-based) and Global Accelerator (Anycast routing with health checks) provide sub-60-second automatic failover between regional ALBs. Route 53 health check failover can also work but depends on DNS TTL and health check timing. CloudFront and Global Accelerator are generally faster for failover.',
  },
  {
    domain: 'Resilient Cloud Solutions',
    q: 'A company is designing a disaster recovery plan and wants to define RTOs and RPOs for different tiers of applications. For Tier 1 (mission-critical), they require RTO = 1 hour and RPO = 15 minutes. Which DR strategy is the MOST cost-effective that meets these requirements?',
    opts: [
      'Backup and Restore: costs are minimal, but RTO can be many hours depending on backup size.',
      'Pilot Light: minimal resources run in DR region with continuous data replication; scale-up takes 15-30 minutes.',
      'Warm Standby: a scaled-down version runs in the DR region, reducing scale-up time to minutes.',
      'Multi-Site Active-Active: highest cost but provides near-zero RTO and RPO.',
    ],
    a: 2,
    exp: 'Warm Standby keeps a scaled-down but functional environment running in the DR region with continuous replication (achieving ~15-minute RPO). Scale-up to full capacity takes minutes, meeting a 1-hour RTO. Pilot Light could also work but requires more scale-up time. Active-Active exceeds the requirements at significantly higher cost.',
  },
  {
    domain: 'Resilient Cloud Solutions',
    q: 'A company runs a stateless web application on EC2 instances behind an ALB. They want to test the application\'s resilience to instance failures by randomly terminating EC2 instances during business hours. Which AWS FIS action achieves this?',
    opts: [
      'aws:ec2:terminate-instances targeting instances by Auto Scaling group tag.',
      'aws:ec2:stop-instances with a 100% action target percentage.',
      'aws:ssm:send-command running a custom shutdown script on the instances.',
      'aws:autoscaling:terminate-instance targeting the ASG directly.',
    ],
    a: 0,
    exp: 'AWS FIS includes the aws:ec2:terminate-instances action, which terminates EC2 instances matching the specified target (by tag, resource ID, or ASG). This directly simulates instance failure for resilience testing, validating that the ALB and ASG replace terminated instances correctly.',
  },
  {
    domain: 'Resilient Cloud Solutions',
    q: 'A DevOps engineer needs to ensure that RDS automated backups are replicated to a different AWS region for DR. The replication must be set up without manual intervention and must support point-in-time recovery (PITR) in the target region. Which feature should they enable?',
    opts: [
      'RDS Cross-Region Automated Backups, which replicates automated backup snapshots and transaction logs to the target region, enabling PITR in that region.',
      'RDS Read Replica in the target region promoted to standalone instance during DR.',
      'AWS Backup with a cross-region copy rule in the backup plan.',
      'Enable RDS Multi-AZ in the target region to automatically receive replicated backups.',
    ],
    a: 0,
    exp: 'RDS Cross-Region Automated Backups (enabled via the RDS console or API) continuously replicate automated backups and transaction logs to a specified destination region, enabling full point-in-time recovery in that region. This is the native RDS feature for cross-region PITR without manual snapshot copying.',
  },

  // ── Monitoring & Logging (~11 questions) ─────────────────────────────────

  {
    domain: 'Monitoring & Logging',
    q: 'A team wants to collect memory utilization metrics from EC2 instances running Amazon Linux. CloudWatch does not collect memory metrics by default. What is the recommended solution?',
    opts: [
      'Install the CloudWatch unified agent on EC2 instances and configure it to collect mem_used_percent from the mem plugin in the agent configuration file.',
      'Create a custom Lambda function that connects to each EC2 instance via SSM Session Manager and reads /proc/meminfo, then publishes to CloudWatch.',
      'Use CloudWatch Container Insights, which automatically collects memory metrics from EC2 instances.',
      'Enable Enhanced Monitoring on the EC2 instance, which includes memory utilization as a standard metric.',
    ],
    a: 0,
    exp: 'The CloudWatch unified agent (amazon-cloudwatch-agent) can be configured to collect OS-level metrics including memory utilization via the mem plugin. This is the AWS-recommended, low-overhead approach for collecting custom metrics not available from the CloudWatch hypervisor-level monitoring.',
  },
  {
    domain: 'Monitoring & Logging',
    q: 'A Lambda function processes millions of requests per day and the team wants to publish custom business metrics (e.g., orders processed, revenue) without adding significant latency. Which approach should they use?',
    opts: [
      'Call CloudWatch PutMetricData API synchronously in the Lambda handler for each request.',
      'Use the Embedded Metric Format (EMF) to write structured JSON logs to stdout; CloudWatch Logs extracts the metrics asynchronously without added Lambda latency.',
      'Write metrics to a DynamoDB table and use a separate Lambda to aggregate and publish to CloudWatch every minute.',
      'Use CloudWatch Synthetics canaries to measure business metrics from the Lambda function output.',
    ],
    a: 1,
    exp: 'EMF allows Lambda functions to emit custom metrics as structured JSON to stdout (which goes to CloudWatch Logs). CloudWatch Logs asynchronously extracts the metrics and publishes them to CloudWatch Metrics with no added Lambda invocation latency and no synchronous API calls.',
  },
  {
    domain: 'Monitoring & Logging',
    q: 'A company uses CloudWatch Synthetics to monitor their API endpoints. A canary runs every 5 minutes but the team wants it to test a complex multi-step user workflow (login, search, add to cart, checkout). Which canary blueprint should they use?',
    opts: [
      'Heartbeat Monitor blueprint — tests a single URL availability.',
      'API Canary blueprint — tests REST API endpoints by making HTTP requests.',
      'Broken Link Checker blueprint — finds broken links on a webpage.',
      'Playwright blueprint or Node.js script blueprint — supports multi-step workflows with Playwright or Puppeteer scripting.',
    ],
    a: 3,
    exp: 'CloudWatch Synthetics supports custom Node.js scripts using Puppeteer or Playwright (syn-nodejs-puppeteer runtime), which enable multi-step browser workflows such as login, navigation, form submission, and checkout. The pre-built blueprints (Heartbeat, API Canary) are for simpler single-step tests.',
  },
  {
    domain: 'Monitoring & Logging',
    q: 'A company is using CloudWatch Evidently to run an A/B test on a new checkout UI. 20% of users should see the new UI. After launching, they want to measure the impact on conversion rate using a custom metric. Which Evidently feature collects this?',
    opts: [
      'Configure an Evidently metric with the goal of maximizing the conversion event; send conversion events from the application using the PutProjectEvents API.',
      'Use a CloudWatch alarm on a custom metric for conversion rate and associate it with the Evidently feature.',
      'Create a CloudWatch Logs metric filter to extract conversion events and link it to the Evidently experiment.',
      'Use CloudWatch Synthetics to simulate user checkout flows and measure conversion rate.',
    ],
    a: 0,
    exp: 'CloudWatch Evidently supports custom metrics defined within an experiment. The application sends feature evaluation events and metric events using the Evidently PutProjectEvents API. Evidently then analyzes the metric data split by feature variation to determine the A/B test impact on conversion.',
  },
  {
    domain: 'Monitoring & Logging',
    q: 'An application team wants to use X-Ray to sample only 5% of requests to their high-volume API, but always sample requests that result in errors. Which X-Ray feature allows this tiered sampling configuration?',
    opts: [
      'Modify the X-Ray daemon sampling configuration file to set the default sampling rate to 5%.',
      'Configure X-Ray sampling rules using the X-Ray console or API: set a default rule for 5% sampling and add a higher-priority rule matching on HTTP status codes 5xx with a 100% fixed rate.',
      'Use CloudWatch Logs metric filters to selectively forward only 5% of X-Ray trace segments to X-Ray.',
      'Set reservoir size to 5 and fixed rate to 5% in the X-Ray sampling rule; error sampling is automatic.',
    ],
    a: 1,
    exp: 'X-Ray sampling rules allow defining multiple rules with priorities. A rule matching HTTP 5xx responses with 100% fixed rate will always sample errors, while a lower-priority default rule samples 5% of normal requests. X-Ray evaluates rules by priority (lowest number = highest priority) and applies the first matching rule.',
  },
  {
    domain: 'Monitoring & Logging',
    q: 'A company has microservices instrumented with X-Ray. The service map shows unusually high response times between Service A and Service B. How should the DevOps engineer investigate the root cause?',
    opts: [
      'Navigate to the X-Ray service map, select the edge between Service A and Service B, and filter traces by response time to identify the slowest individual requests and their trace details.',
      'Enable CloudWatch Container Insights to get CPU and memory metrics for Services A and B.',
      'Look at CloudWatch Logs for Services A and B and search for timeout errors.',
      'Create a CloudWatch alarm on the X-Ray latency metric for the A-to-B edge and wait for it to fire again.',
    ],
    a: 0,
    exp: 'The X-Ray service map visually shows latency between services. By selecting the edge between two services and filtering traces by latency, engineers can drill into individual slow traces to see exactly which downstream calls, database queries, or external dependencies are causing the latency.',
  },
  {
    domain: 'Monitoring & Logging',
    q: 'A company wants to centralize CloudWatch Logs from 15 AWS accounts into a single central logging account for security analysis with OpenSearch. What is the recommended architecture?',
    opts: [
      'In each source account, create a Kinesis Data Firehose delivery stream that sends logs to OpenSearch in the central account.',
      'Configure CloudWatch Logs subscription filters in each source account to send log events to a Kinesis Data Stream or Kinesis Data Firehose in the central account; the Firehose delivers to OpenSearch.',
      'Use AWS CloudTrail with S3 cross-account delivery to the central account and index logs from S3 into OpenSearch.',
      'Install a custom Fluentd agent on all EC2 instances that ships logs directly to OpenSearch in the central account.',
    ],
    a: 1,
    exp: 'CloudWatch Logs subscription filters support cross-account destinations via Kinesis Data Streams or Firehose. By setting the destination to a Kinesis resource in the central account (with an appropriate resource-based policy), each source account streams log events to the central account, where Firehose delivers them to OpenSearch.',
  },
  {
    domain: 'Monitoring & Logging',
    q: 'A DevOps engineer wants to create a CloudWatch dashboard that shows metrics from multiple AWS accounts and regions in a single view. Which CloudWatch feature enables this?',
    opts: [
      'Cross-account cross-region CloudWatch dashboards, enabled by sharing monitoring data from source accounts to a monitoring account using CloudWatch cross-account observability.',
      'AWS Health Dashboard, which provides a unified view of account health across the organization.',
      'AWS Systems Manager Explorer, which aggregates operational data including CloudWatch metrics across accounts.',
      'Amazon Managed Grafana with CloudWatch as a data source, configured with cross-account IAM roles.',
    ],
    a: 0,
    exp: 'CloudWatch cross-account observability allows a central monitoring account to view and interact with CloudWatch metrics, logs, and traces from source accounts. Cross-account, cross-region dashboards are then built natively in CloudWatch, showing data from multiple accounts and regions in one place.',
  },
  {
    domain: 'Monitoring & Logging',
    q: 'A company wants to monitor the health of their API endpoints every minute and get alerted within 2 minutes of an endpoint returning HTTP 500 errors. CloudWatch alarms currently have a 5-minute evaluation period. How should they reduce the detection time?',
    opts: [
      'Enable CloudWatch high-resolution custom metrics and create a high-resolution alarm with a 10-second or 30-second evaluation period.',
      'Decrease the CloudWatch alarm evaluation period to 1 minute using the standard metrics resolution.',
      'Use CloudWatch Synthetics canaries running every minute; configure the canary to trigger an SNS alert if it detects HTTP 500 responses.',
      'Enable CloudWatch Anomaly Detection to catch unexpected 500 error spikes more quickly.',
    ],
    a: 2,
    exp: 'CloudWatch Synthetics canaries can run as frequently as every minute and immediately detect HTTP 500 responses from API endpoints. Combined with an SNS alarm action on the canary\'s SuccessPercent metric, this provides near-real-time alerting within 1-2 minutes of endpoint failure.',
  },
  {
    domain: 'Monitoring & Logging',
    q: 'A company uses CloudWatch Logs Insights to query application logs. Their queries run slowly because the log group contains years of data. What is the most effective way to optimize query performance without deleting historical data?',
    opts: [
      'Split logs into separate log groups by time period (e.g., monthly) and query only the relevant log group.',
      'Use CloudWatch Logs Insights with a specific time range filter to limit the amount of data scanned.',
      'Export historical logs to S3 and query them using Amazon Athena, reserving CloudWatch Logs Insights for recent data.',
      'Enable CloudWatch Logs compression to reduce data volume and improve query speed.',
    ],
    a: 2,
    exp: 'Exporting historical logs to S3 and querying them with Athena is the optimal long-term architecture: Athena is designed for querying large datasets in S3 cost-effectively, while CloudWatch Logs Insights remains fast and relevant for querying recent operational data. This separation by access pattern reduces both cost and query latency.',
  },
  {
    domain: 'Monitoring & Logging',
    q: 'A DevOps engineer needs to create a CloudWatch alarm that triggers when the p99 latency of their API exceeds 1 second. The API publishes standard latency metrics to CloudWatch. What type of alarm statistic should be used?',
    opts: [
      'Average — this calculates the mean latency across all requests.',
      'Maximum — this catches the single highest latency value in the period.',
      'p99 (percentile statistic) — this measures the value below which 99% of latency data points fall.',
      'SampleCount — this counts the number of requests, not latency values.',
    ],
    a: 2,
    exp: 'CloudWatch supports percentile statistics (p10, p50, p90, p99, etc.) for alarms. Using p99 ensures the alarm triggers when 99% of requests complete faster than 1 second, which accurately measures tail latency without being skewed by a few extreme outliers or averaged-out by the majority of fast requests.',
  },

  // ── Incident & Event Response (~11 questions) ────────────────────────────

  {
    domain: 'Incident & Event Response',
    q: 'A security team wants to automatically isolate an EC2 instance when GuardDuty detects a cryptocurrency mining finding (UnauthorizedAccess:EC2/CryptoCurrencyMiningActivity). What is the recommended automated response architecture?',
    opts: [
      'Create an EventBridge rule matching the GuardDuty finding type; trigger a Lambda function that removes the instance from its security groups and adds it to an isolated security group with no outbound access.',
      'Enable GuardDuty automated response, which will terminate the EC2 instance when a high-severity finding is generated.',
      'Use AWS Security Hub automated response to apply a "quarantine" tag to the instance, which triggers an SCP to deny all actions on tagged instances.',
      'Create a CloudWatch alarm on GuardDuty findings and use SSM Run Command to stop the instance when the alarm triggers.',
    ],
    a: 0,
    exp: 'EventBridge natively receives GuardDuty findings as events and can trigger Lambda functions for automated response. The Lambda isolates the instance by modifying its security group to one that blocks all traffic, which is the standard IR isolation technique. GuardDuty does not have a built-in automated terminate or quarantine action.',
  },
  {
    domain: 'Incident & Event Response',
    q: 'A company uses CloudTrail in an AWS Organization and wants to detect unusually high numbers of IAM API calls (potential credential stuffing) and alert the security team. Which CloudTrail feature is purpose-built for detecting unusual API activity patterns?',
    opts: [
      'CloudTrail Event History filtering by event source = iam.amazonaws.com.',
      'CloudTrail Insights, which detects unusual write API call volumes compared to baseline and publishes insight events to EventBridge.',
      'CloudWatch Logs metric filter on the CloudTrail log group counting IAM API calls per minute.',
      'AWS Config rule that evaluates IAM API call frequency against a defined threshold.',
    ],
    a: 1,
    exp: 'CloudTrail Insights analyzes management API call volumes against a 7-day baseline and generates Insight events when unusual activity is detected (e.g., a spike in IAM write calls). These insight events are delivered to EventBridge for automated alerting or response, making it purpose-built for anomaly detection in API activity.',
  },
  {
    domain: 'Incident & Event Response',
    q: 'A DevOps team uses Systems Manager Incident Manager for incident management. When a CloudWatch alarm triggers an incident, the on-call engineer receives a PagerDuty page. The team wants to automatically create a Systems Manager OpsItem and run a diagnostic runbook when the incident opens. How should this be configured in Incident Manager?',
    opts: [
      'In the Incident Manager response plan, configure the runbook as an automation step and enable OpsItem creation as part of the incident response plan settings.',
      'Create an EventBridge rule that matches Incident Manager incident creation events and triggers a Lambda to create an OpsItem and start the SSM Automation.',
      'Configure the CloudWatch alarm to directly trigger an SSM Automation document; SSM Automation creates the OpsItem as a final step.',
      'Use SSM Incident Manager with AWS Config remediation to automatically run the runbook when the incident is created.',
    ],
    a: 0,
    exp: 'Incident Manager response plans support runbooks (SSM Automation documents) that run automatically when an incident is created, and they can be configured to create OpsItems in Systems Manager OpsCenter. This native integration requires no additional EventBridge rules or Lambda functions.',
  },
  {
    domain: 'Incident & Event Response',
    q: 'A company wants to use EventBridge to detect when an EC2 instance is stopped and automatically restart it (for instances tagged with "AutoRestart=true"). Which architecture achieves this with the least operational overhead?',
    opts: [
      'Create an EventBridge rule matching EC2 state-change events with state=stopped; use an SSM Automation document (or Lambda) as the target that checks the AutoRestart tag and starts the instance if the tag is present.',
      'Create a CloudWatch alarm on EC2 StatusCheckFailed metric and use an EC2 action to restart the instance.',
      'Use EC2 Auto Recovery to automatically restart stopped instances with the AutoRestart tag.',
      'Configure an EC2 instance scheduled event using EC2 Maintenance events to detect stopped instances.',
    ],
    a: 0,
    exp: 'EventBridge receives EC2 state-change notifications in near real-time. An SSM Automation document target can check the AutoRestart tag using GetTagsForResource and call EC2 StartInstances conditionally. This is fully serverless, event-driven, and requires no polling or additional infrastructure.',
  },
  {
    domain: 'Incident & Event Response',
    q: 'A company wants to ensure that when an SQS-triggered Lambda function fails to process a message after 3 attempts, the message is preserved for debugging. What configuration achieves this?',
    opts: [
      'Configure a Dead-Letter Queue (DLQ) on the SQS queue; failed messages are automatically sent to the DLQ after the maximum receive count (3) is exceeded.',
      'Add a try/catch block in the Lambda that sends failed messages to S3 for debugging.',
      'Increase the SQS message visibility timeout to give Lambda more time to process messages.',
      'Configure Lambda event source mapping with a BisectBatchOnFunctionError set to true.',
    ],
    a: 0,
    exp: 'SQS Dead-Letter Queues (DLQ) automatically receive messages that exceed the MaxReceiveCount (set to 3 here) without successful processing. The DLQ preserves the failed messages indefinitely (up to the DLQ retention period) for debugging, without requiring any application code changes.',
  },
  {
    domain: 'Incident & Event Response',
    q: 'A DevOps team wants to be notified by SNS when any resource in their account is tagged incorrectly (missing required tags). Which combination of services best implements real-time tag compliance monitoring?',
    opts: [
      'AWS Config with a required-tags managed rule; configure a Config remediation action that sends an SNS notification when non-compliant resources are detected.',
      'Create an EventBridge rule on all resource tag-change events; trigger a Lambda that validates tags and publishes to SNS if tags are incorrect.',
      'Use AWS Tag Editor to schedule daily tag compliance reports sent to an SNS topic.',
      'Use AWS Trusted Advisor tag compliance checks with weekly email notifications.',
    ],
    a: 1,
    exp: 'EventBridge captures tag-change events (e.g., "Tag Change on Resource" events from various services) in real-time. A Lambda target evaluates the tags against required tag policies and sends SNS notifications for violations immediately. AWS Config is also valid but evaluates periodically, making EventBridge more real-time for this use case.',
  },
  {
    domain: 'Incident & Event Response',
    q: 'An operations team uses SSM Automation runbooks to respond to incidents. They want to create a runbook that automatically restores an RDS instance from its latest snapshot when a "DatabaseCorruption" OpsItem is created. Which SSM Automation document action type should they use to restore the RDS instance?',
    opts: [
      'aws:runCommand — runs a shell command on EC2 instances.',
      'aws:executeAwsApi — calls an AWS API action such as RDS RestoreDBInstanceFromDBSnapshot.',
      'aws:invokeLambdaFunction — invokes a Lambda function that calls the RDS restore API.',
      'aws:executeScript — runs a Python or PowerShell script inline.',
    ],
    a: 1,
    exp: 'The aws:executeAwsApi action in SSM Automation documents calls any AWS API directly, including RDS:RestoreDBInstanceFromDBSnapshot. This is the most direct approach for calling AWS APIs within a runbook without requiring Lambda functions or custom scripts.',
  },
  {
    domain: 'Incident & Event Response',
    q: 'A company wants to automatically remediate the finding "S3 bucket is publicly accessible" from AWS Security Hub. What is the recommended automated response approach?',
    opts: [
      'Use Security Hub custom actions to trigger an EventBridge rule, which invokes a Lambda function that calls s3:PutPublicAccessBlock on the non-compliant bucket.',
      'Enable Security Hub automated response with the built-in "Block S3 Public Access" remediation.',
      'Create a GuardDuty suppression rule to hide the finding until it can be manually remediated.',
      'Configure an S3 Block Public Access setting at the account level and suppress all future Security Hub findings.',
    ],
    a: 0,
    exp: 'Security Hub custom actions send findings to EventBridge, which can trigger Lambda for automated remediation. The Lambda calls s3:PutPublicAccessBlock to remove public access. While account-level S3 Block Public Access prevents future violations, it does not remediate existing findings or provide an automated feedback loop through Security Hub.',
  },
  {
    domain: 'Incident & Event Response',
    q: 'An organization wants to automatically page the on-call team via PagerDuty when a CloudWatch alarm enters ALARM state, and automatically resolve the PagerDuty incident when the alarm returns to OK state. What is the recommended integration approach?',
    opts: [
      'Create two SNS topics (one for ALARM, one for OK) with PagerDuty HTTPS subscription endpoints; configure the CloudWatch alarm to send to the ALARM topic on state change to ALARM and to the OK topic on state change to OK.',
      'Create a single SNS topic subscribed to PagerDuty; CloudWatch sends all alarm state changes to this topic, and PagerDuty uses the newStateValue field to trigger or resolve incidents.',
      'Create a Lambda function triggered by CloudWatch alarm state changes that calls the PagerDuty Events API v2 to trigger or resolve incidents based on the alarm state.',
      'Use Amazon DevOps Guru to detect anomalies and integrate with PagerDuty via its native integration.',
    ],
    a: 2,
    exp: 'A Lambda function provides the most flexible integration: it receives CloudWatch alarm state change notifications via SNS, evaluates the newStateValue field, and calls the PagerDuty Events API v2 to trigger a new incident (ALARM state) or resolve an existing incident (OK state). This bidirectional integration ensures PagerDuty incidents stay in sync with alarm state.',
  },
  {
    domain: 'Incident & Event Response',
    q: 'A company\'s Lambda functions are experiencing sudden spikes in throttling errors. The on-call engineer wants to be alerted and have the system automatically increase the reserved concurrency. What EventBridge-based architecture supports this?',
    opts: [
      'Create a CloudWatch alarm on Lambda Throttles metric; when it enters ALARM state, trigger a Lambda function that calls UpdateFunctionConfiguration to increase reserved concurrency.',
      'Create an EventBridge rule on Lambda throttle events; trigger an SSM Automation to update the concurrency.',
      'Use AWS Auto Scaling to automatically scale Lambda concurrency based on throttle metrics.',
      'Enable Lambda Provisioned Concurrency and set it to automatically scale using Application Auto Scaling.',
    ],
    a: 0,
    exp: 'CloudWatch alarms on the Throttles metric detect throttling spikes. An alarm action can trigger an SNS topic, which invokes a Lambda remediation function. The remediation Lambda calls the Lambda UpdateFunctionConfiguration API to increase reserved concurrency. This creates a feedback loop that automatically responds to throttling events.',
  },
  {
    domain: 'Incident & Event Response',
    q: 'A team uses a runbook documented in Systems Manager OpsCenter. During an incident, an operator needs to run the runbook but must not be able to approve their own OpsItem. What IAM configuration enforces this?',
    opts: [
      'Add a condition in the operator\'s IAM policy denying ssm:UpdateOpsItem when the aws:RequestedRegion matches their home region.',
      'Use SSM OpsCenter approval rules that require a second approver who is different from the OpsItem creator, enforced through the Incident Manager response plan.',
      'Configure an IAM permission boundary that prevents operators from approving OpsItems they created.',
      'Enable AWS Organizations SCPs that restrict ssm:UpdateOpsItem to users in the manager group only.',
    ],
    a: 1,
    exp: 'Systems Manager Incident Manager response plans support runbook approval steps where you can require a second approver distinct from the initiator. This is the native mechanism for enforcing four-eyes (dual-control) approval within SSM runbooks without complex IAM condition expressions.',
  },

  // ── Security & Compliance (~13 questions) ────────────────────────────────

  {
    domain: 'Security & Compliance',
    q: 'A company uses AWS Organizations and wants to prevent all accounts in the "Development" OU from creating resources in regions outside us-east-1 and eu-west-1. Which control should they implement?',
    opts: [
      'Apply an IAM permission boundary to all IAM roles in development accounts that deny actions outside the allowed regions.',
      'Create an SCP attached to the Development OU with a Deny statement for all actions where aws:RequestedRegion is not in [us-east-1, eu-west-1].',
      'Use AWS Config rules in each development account to detect resources in unapproved regions and auto-remediate.',
      'Enable AWS Control Tower guardrails in the Development OU to restrict to specific regions.',
    ],
    a: 1,
    exp: 'Service Control Policies (SCPs) are the correct tool for organization-wide preventive controls. An SCP with a Deny statement using the "aws:RequestedRegion" condition key and NotAction to exclude global services effectively prevents resource creation in non-approved regions for all accounts in the OU.',
  },
  {
    domain: 'Security & Compliance',
    q: 'A developer\'s IAM role needs to be able to deploy CloudFormation stacks but must not be able to create IAM roles directly. However, the CloudFormation stacks they deploy may need to create IAM roles. How should this be configured?',
    opts: [
      'Give the developer\'s role AdministratorAccess and rely on CloudFormation to enforce IAM boundaries.',
      'Use a CloudFormation service role with IAM permissions, and grant the developer\'s role cloudformation:* but not iam:CreateRole. CloudFormation will use the service role for IAM operations during stack deployment.',
      'Add a permission boundary to the developer\'s role that denies iam:CreateRole.',
      'Create a separate IAM user with iam:CreateRole for the developer to use only during CloudFormation deployments.',
    ],
    a: 1,
    exp: 'CloudFormation supports a service role (PassRole model) where the stack operations assume a designated IAM role with the necessary permissions (including IAM). The developer\'s role needs only cloudformation:* and iam:PassRole on the service role. This separates the developer\'s direct permissions from what CloudFormation can do on their behalf.',
  },
  {
    domain: 'Security & Compliance',
    q: 'A company stores RDS database credentials in AWS Secrets Manager. The secret is configured for automatic rotation every 30 days. Which Lambda function behavior is critical to ensure zero-downtime rotation?',
    opts: [
      'The rotation Lambda must immediately delete the old secret version and create a new one.',
      'The rotation Lambda must implement all four rotation steps: createSecret, setSecret, testSecret, and finishSecret, ensuring the new credentials work before the old ones are invalidated.',
      'The rotation Lambda must update the secret and immediately notify all applications via SNS to refresh their cached credentials.',
      'The rotation Lambda must pause RDS connections during rotation and resume them only after the new secret is validated.',
    ],
    a: 1,
    exp: 'Secrets Manager rotation follows a four-step Lambda lifecycle: createSecret (create new version), setSecret (set credentials in the service), testSecret (verify new credentials work), finishSecret (mark new version as AWSCURRENT). This staged approach ensures the new credentials are fully functional before the old ones (AWSPREVIOUS) are deprecated, enabling zero-downtime rotation.',
  },
  {
    domain: 'Security & Compliance',
    q: 'A company must comply with a policy that all S3 bucket keys encrypted with KMS must use a customer-managed key (CMK) with annual key rotation enabled. They want to automatically detect non-compliant buckets. Which approach achieves this with the least operational overhead?',
    opts: [
      'Create a custom AWS Config rule backed by Lambda that checks if the S3 bucket uses a CMK and verifies that the CMK has key rotation enabled.',
      'Run a daily Lambda function that lists all S3 buckets, checks their encryption settings, and sends non-compliant findings to Security Hub.',
      'Use AWS Trusted Advisor to check S3 encryption compliance and send daily reports.',
      'Create a CloudTrail event rule to detect when S3 buckets are created without CMK encryption.',
    ],
    a: 0,
    exp: 'A custom AWS Config rule provides continuous compliance evaluation: whenever an S3 bucket or KMS key configuration changes, the rule Lambda is invoked to check both the S3 encryption configuration and the KMS key rotation setting. Non-compliant resources appear in the Config compliance dashboard automatically.',
  },
  {
    domain: 'Security & Compliance',
    q: 'A DevOps team wants to integrate AWS Inspector into their CodePipeline to scan Docker images for vulnerabilities before pushing to ECR. The pipeline should fail if any CRITICAL vulnerabilities are found. How should this be implemented?',
    opts: [
      'Add a CodeBuild step that pushes the image to a staging ECR repository, waits for Inspector enhanced scanning to complete, then calls the Inspector ListFindings API and fails the build if critical findings exist.',
      'Enable Inspector on the ECR repository and add a manual approval step where a security engineer reviews Inspector findings before approving.',
      'Use ECR basic scanning in a CodeBuild step and fail the pipeline if the scan returns any vulnerabilities.',
      'Add a Lambda action in CodePipeline that triggers Inspector scanning and polls for results every 30 seconds.',
    ],
    a: 0,
    exp: 'Inspector enhanced scanning for ECR automatically scans images on push. By pushing to a staging ECR repo, waiting for the scan to complete (polling DescribeImageScanFindings), and using the Inspector ListFindings API to check for CRITICAL severity findings, a CodeBuild step can gate the pipeline on vulnerability results programmatically.',
  },
  {
    domain: 'Security & Compliance',
    q: 'A company wants to implement least-privilege IAM by ensuring that no IAM role has permissions it hasn\'t used in 90 days. Which IAM feature helps identify these unused permissions?',
    opts: [
      'IAM Access Analyzer policy validation, which identifies overly permissive policies.',
      'IAM Last Accessed data (service last accessed and action last accessed), which shows when each service or action was last used by a role.',
      'AWS Trusted Advisor IAM service limits checks, which report on IAM usage.',
      'AWS Config managed rule "iam-user-unused-credentials-check" to find idle credentials.',
    ],
    a: 1,
    exp: 'IAM Last Accessed data shows the last time each AWS service (and at the action level for some services) was accessed by a principal. By reviewing which services and actions haven\'t been used in 90+ days, teams can safely remove those permissions to enforce least privilege over time.',
  },
  {
    domain: 'Security & Compliance',
    q: 'A company wants to enforce that all new IAM roles created by developers can only assume up to a maximum privilege level defined by a permission boundary. How should this be implemented?',
    opts: [
      'Attach an SCP to the account that limits what actions IAM roles can perform.',
      'Create a permission boundary policy; modify the developer\'s IAM role policy to include a condition on iam:CreateRole that requires iam:PermissionsBoundary to be set to the approved boundary ARN.',
      'Use IAM Access Analyzer to automatically downscope any newly created roles to minimum required permissions.',
      'Configure CloudTrail to alert when roles are created without a permission boundary and have a security engineer manually add it.',
    ],
    a: 1,
    exp: 'IAM condition keys allow restricting iam:CreateRole and iam:PutRolePermissionsBoundary calls by requiring specific permission boundary ARNs. By conditioning the developer\'s CreateRole permission on "iam:PermissionsBoundary" matching the approved policy ARN, all roles they create will be constrained by that boundary.',
  },
  {
    domain: 'Security & Compliance',
    q: 'An organization wants to run static application security testing (SAST) in their CI/CD pipeline to detect vulnerabilities in code before deployment. Which integration point in CodePipeline is most appropriate for SAST?',
    opts: [
      'In a CodeBuild stage immediately after the Source stage, before compilation and testing, to detect vulnerabilities as early as possible.',
      'In a CodeDeploy pre-deployment hook to scan code on the target EC2 instances before deployment.',
      'After deployment to staging, in a separate testing stage that uses dynamic scanning (DAST) tools.',
      'In the Source stage itself, using a CodeCommit trigger that runs the SAST scan before the commit is accepted.',
    ],
    a: 0,
    exp: 'SAST analyzes source code without executing it, so it should run as early as possible in the pipeline — immediately after the source is available, before build and deployment stages. This "shift left" approach catches vulnerabilities at the lowest remediation cost and prevents them from progressing further in the pipeline.',
  },
  {
    domain: 'Security & Compliance',
    q: 'A company uses KMS customer-managed keys to encrypt S3 buckets and wants to ensure that KMS keys are rotated annually. What is the most operationally efficient way to enforce and verify this at scale across multiple accounts?',
    opts: [
      'Manually enable key rotation on each CMK through the KMS console in each account.',
      'Use the AWS Config managed rule "cmk-backing-key-rotation-enabled" deployed via StackSets across all accounts; this continuously evaluates all CMKs and flags non-compliant keys.',
      'Create a Lambda function in each account that weekly checks KMS key rotation settings and sends a report to SNS.',
      'Enable AWS Security Hub with the CIS AWS Foundations Benchmark standard, which includes a check for KMS key rotation.',
    ],
    a: 1,
    exp: 'The "cmk-backing-key-rotation-enabled" AWS Config managed rule checks all CMKs for rotation enablement continuously. Deploying it via CloudFormation StackSets to all accounts in the organization provides centralized, automated compliance monitoring without per-account manual setup or custom Lambda functions.',
  },
  {
    domain: 'Security & Compliance',
    q: 'A company wants to implement compliance-as-code using AWS Config. They need to ensure that all EC2 instances are launched only from pre-approved AMIs. Which Config rule type is most appropriate?',
    opts: [
      'AWS Config managed rule "approved-amis-by-id" or "approved-amis-by-tag" that checks whether EC2 instances use only approved AMIs.',
      'A custom Lambda-backed Config rule that queries EC2 DescribeInstances and checks the AMI ID against an approved list in Parameter Store.',
      'An EventBridge rule on EC2 RunInstances events that checks the AMI and terminates non-compliant instances.',
      'An SCP that denies EC2 RunInstances for AMI IDs not in the approved list.',
    ],
    a: 0,
    exp: 'AWS Config managed rules "approved-amis-by-id" and "approved-amis-by-tag" are purpose-built for this use case, evaluating existing and newly launched EC2 instances against an approved AMI list. Managed rules require no custom Lambda code and are directly supported by AWS Config without additional development.',
  },
  {
    domain: 'Security & Compliance',
    q: 'A multi-account organization uses AWS Security Hub as a centralized security findings aggregator. They want findings from all accounts to automatically flow to the management account. What must be configured?',
    opts: [
      'Enable Security Hub in each account and configure cross-account finding aggregation in the Security Hub management account by designating it as the delegated administrator.',
      'Use Amazon EventBridge cross-account event bus to forward Security Hub findings from each account to the management account.',
      'Enable CloudTrail in each account and forward logs to the management account; Security Hub then analyzes the centralized CloudTrail logs.',
      'Create a custom Lambda in each account that queries Security Hub findings and posts them to the management account via API.',
    ],
    a: 0,
    exp: 'Security Hub supports a delegated administrator model where a central account (typically the Organizations management account) is designated as the Security Hub administrator. This automatically aggregates findings from all member accounts into the administrator account without custom event forwarding or Lambda functions.',
  },
  {
    domain: 'Security & Compliance',
    q: 'A company\'s security team discovered that an IAM role used by a third-party vendor has not been used in 6 months. They want to disable access immediately without deleting the role, in case it needs to be re-enabled. What is the recommended approach?',
    opts: [
      'Delete the role\'s IAM policies and wait for the vendor to request access again.',
      'Attach a customer-managed policy to the role that explicitly Denies all actions ("*") with a Deny statement, overriding all Allow policies.',
      'Remove the role from any IAM groups and change its trust policy to prevent the vendor from assuming it.',
      'Enable MFA requirement on the role, which the vendor does not have.',
    ],
    a: 1,
    exp: 'Attaching an explicit Deny policy for all actions to the role immediately revokes all effective permissions (Deny always overrides Allow in IAM). The role itself remains intact with its original policies, making it easy to re-enable by simply detaching the Deny policy, without any reconstruction of the original role configuration.',
  },
  {
    domain: 'Security & Compliance',
    q: 'A DevOps engineer needs to implement DAST (Dynamic Application Security Testing) in a CodePipeline that deploys to a staging environment. Which approach best integrates DAST into the pipeline?',
    opts: [
      'Add a CodeBuild action after the staging deployment stage that runs an OWASP ZAP or Burp Suite scan against the staging endpoint, then parses the results and fails the pipeline if high-severity findings are detected.',
      'Add a manual approval action where a security engineer runs DAST tools manually and approves if no critical findings are found.',
      'Use AWS Inspector for DAST scanning of the deployed application endpoint.',
      'Enable AWS WAF on the staging ALB, which performs dynamic security testing on all incoming traffic.',
    ],
    a: 0,
    exp: 'DAST tools like OWASP ZAP can run as a CodeBuild action against a live staging environment. The buildspec runs the DAST scan, parses the JSON/XML report for high-severity findings, and exits with a non-zero code to fail the pipeline if critical vulnerabilities are detected. This integrates dynamic testing into the automated pipeline flow.',
  },
];
