import type { Question } from '../types';

export const BANK_MLS: Question[] = [
  // ─── DATA ENGINEERING (~14 questions) ───────────────────────────────────────
  {
    domain: 'Data Engineering',
    q: 'A data engineering team needs to ingest streaming clickstream data and make it available for SQL-based ad-hoc queries in Amazon S3 within 60 seconds. Which combination of services best satisfies this requirement?',
    opts: [
      'Amazon Kinesis Data Streams → AWS Lambda → Amazon S3',
      'Amazon Kinesis Data Firehose → Amazon S3 → Amazon Athena',
      'Amazon MSK → Amazon EMR → Amazon Redshift',
      'Amazon SQS → AWS Glue → Amazon DynamoDB',
    ],
    a: 1,
    exp: 'Kinesis Data Firehose is purpose-built for loading streaming data into S3 (and other destinations) with configurable buffering intervals as low as 60 seconds, requiring no custom code. Once in S3, Amazon Athena provides serverless SQL-based ad-hoc querying directly on the files. The other options introduce unnecessary complexity or do not meet the latency requirement out-of-the-box.',
  },
  {
    domain: 'Data Engineering',
    q: 'An ML team stores raw training data in Amazon S3 in CSV format. They need to catalog the schema, transform the data into Parquet, and schedule nightly ETL refreshes—all with minimal infrastructure management. Which AWS service combination is most appropriate?',
    opts: [
      'AWS Glue Crawlers to catalog + AWS Glue ETL jobs to transform + AWS Glue triggers for scheduling',
      'Amazon EMR with Apache Hive to catalog + Apache Spark for transformation + Apache Oozie for scheduling',
      'AWS Lambda to catalog + Amazon Athena CTAS to transform + Amazon EventBridge for scheduling',
      'Amazon Redshift Spectrum to catalog + COPY command to transform + AWS Step Functions for scheduling',
    ],
    a: 0,
    exp: 'AWS Glue provides an integrated, serverless solution: Crawlers automatically infer and register schema into the Glue Data Catalog, ETL jobs convert CSV to Parquet using managed Spark, and Glue Triggers schedule the pipeline—all without provisioning servers. The other options require more infrastructure management or have limitations for this fully managed ETL pattern.',
  },
  {
    domain: 'Data Engineering',
    q: 'A company wants to perform visual data transformations and profiling on a 50 GB dataset stored in S3 without writing code. Which AWS service is specifically designed for this use case?',
    opts: [
      'AWS Glue Studio',
      'AWS Glue DataBrew',
      'Amazon SageMaker Data Wrangler',
      'Amazon EMR Notebooks',
    ],
    a: 1,
    exp: 'AWS Glue DataBrew is a visual data preparation tool that allows analysts to clean and normalize data using over 250 pre-built transformations without writing code. While SageMaker Data Wrangler also provides visual data preparation, it is tightly integrated into the SageMaker ML workflow. Glue DataBrew is the purpose-built service for code-free data profiling and transformation on S3 data.',
  },
  {
    domain: 'Data Engineering',
    q: 'A financial services company needs to process 1 million transactions per second with exactly-once semantics and replay capability for the past 7 days. Which streaming service should they use?',
    opts: [
      'Amazon Kinesis Data Firehose',
      'Amazon Kinesis Data Streams',
      'Amazon SQS FIFO',
      'Amazon SNS with filtering',
    ],
    a: 1,
    exp: 'Amazon Kinesis Data Streams supports high-throughput ingestion, data retention up to 365 days (configurable), and replay capability, making it suitable for replay-based recovery. Kinesis Data Firehose does not support replay and is delivery-focused. SQS FIFO supports exactly-once but has throughput limits and no replay. Kinesis Data Streams is the correct choice for this combination of requirements.',
  },
  {
    domain: 'Data Engineering',
    q: 'An ML team runs Apache Spark jobs on Amazon EMR to preprocess 10 TB of training data. Jobs intermittently fail due to memory errors on executors. Which action is most likely to resolve this issue?',
    opts: [
      'Increase the number of EMR master nodes',
      'Increase executor memory by adjusting spark.executor.memory and repartitioning data to reduce shuffle size',
      'Switch from YARN to Mesos as the cluster manager',
      'Enable EMR managed scaling and let AWS auto-tune Spark parameters',
    ],
    a: 1,
    exp: 'Executor OOM errors in Spark are caused by insufficient memory per executor or excessive data per partition during shuffles. Increasing spark.executor.memory and repartitioning to create more, smaller partitions directly addresses both causes. Adding master nodes does not help executor memory. EMR managed scaling adjusts cluster size but does not tune per-executor memory allocation.',
  },
  {
    domain: 'Data Engineering',
    q: 'A team needs to label 500,000 images for an object detection model as quickly as possible with high quality. They want to use a combination of automated labeling and human review. Which service supports this workflow?',
    opts: [
      'Amazon Rekognition Custom Labels',
      'AWS Mechanical Turk directly via API',
      'Amazon SageMaker Ground Truth with auto-labeling enabled',
      'Amazon Augmented AI (A2I) standalone',
    ],
    a: 2,
    exp: 'SageMaker Ground Truth supports automated labeling, where a model trained on early human labels automatically labels the remaining data with high confidence, sending only uncertain examples to human reviewers. This hybrid approach significantly reduces cost and time for large datasets. Rekognition Custom Labels trains models but does not manage human labeling workflows at scale.',
  },
  {
    domain: 'Data Engineering',
    q: 'A dataset for a churn prediction model has a 98:2 class imbalance (non-churn vs. churn). The team is using scikit-learn. Which technique would most directly address this imbalance during training?',
    opts: [
      'Apply PCA to reduce dimensionality before training',
      'Use SMOTE (Synthetic Minority Over-sampling Technique) to oversample the minority class',
      'Increase the learning rate of the model',
      'Remove 90% of the majority class records from the training set',
    ],
    a: 1,
    exp: 'SMOTE generates synthetic samples for the minority class by interpolating between existing minority samples, directly addressing class imbalance without losing majority-class information. Random undersampling (removing majority records) discards data and can hurt model performance. PCA is a dimensionality reduction technique unrelated to class imbalance. Adjusting learning rate does not address the distribution skew.',
  },
  {
    domain: 'Data Engineering',
    q: 'A company uses AWS Glue to run ETL jobs that read from Amazon RDS MySQL and write to S3. The jobs are slow because they re-process the entire dataset nightly. How should the team optimize this?',
    opts: [
      'Enable AWS Glue job bookmarks to process only new or changed data since the last run',
      'Increase the number of Glue DPUs allocated to the job',
      'Switch to AWS Glue DataBrew for incremental processing',
      'Use Amazon DMS continuous replication instead of nightly batch jobs',
    ],
    a: 0,
    exp: 'AWS Glue job bookmarks track which data has already been processed and ensure that subsequent runs only process new or modified records, eliminating redundant reprocessing. Increasing DPUs speeds up processing of the full dataset but does not reduce the amount of data processed. DMS is a migration service and is not a replacement for ETL transformation logic in Glue.',
  },
  {
    domain: 'Data Engineering',
    q: 'A streaming ML inference pipeline receives sensor readings via Kinesis Data Streams and needs to run SQL-based anomaly detection in real time before storing results in S3. Which service fits this use case?',
    opts: [
      'Amazon Kinesis Data Firehose with a Lambda transformation',
      'Amazon Kinesis Data Analytics (Apache Flink) with SQL or Flink application',
      'AWS Glue Streaming ETL job',
      'Amazon EMR with Spark Structured Streaming',
    ],
    a: 1,
    exp: 'Amazon Kinesis Data Analytics for Apache Flink is purpose-built for real-time SQL and stream processing on data flowing through Kinesis Data Streams, with built-in anomaly detection functions (RANDOM_CUT_FOREST). It requires no server management for this use case. Glue Streaming ETL and EMR Spark Streaming are viable but involve more operational overhead and are not as tightly integrated with Kinesis for real-time SQL analytics.',
  },
  {
    domain: 'Data Engineering',
    q: 'An ML pipeline needs to run data preprocessing, model training, and evaluation as a sequence of steps that can be retried individually on failure. Which AWS service is designed for orchestrating such ML batch workflows?',
    opts: [
      'AWS Step Functions with Lambda tasks',
      'Amazon MWAA (Managed Airflow) with DAGs',
      'AWS Batch with job dependencies',
      'Amazon SageMaker Pipelines',
    ],
    a: 3,
    exp: 'SageMaker Pipelines is a purpose-built CI/CD orchestration service for ML workflows that supports step-level retries, caching, and integration with all SageMaker services (processing, training, evaluation, registration). While Step Functions and Airflow can orchestrate ML steps, SageMaker Pipelines provides native ML step types, lineage tracking, and tighter SageMaker integration without custom glue code.',
  },
  {
    domain: 'Data Engineering',
    q: 'A data scientist needs to normalize numerical features and one-hot encode categorical features as part of a SageMaker training pipeline. Where is the best place to perform these transformations to avoid data leakage?',
    opts: [
      'Compute normalization statistics on the full dataset before splitting into train/validation/test sets',
      'Compute normalization statistics on the training set only, then apply to validation and test sets',
      'Normalize inside the model\'s forward pass using batch normalization layers',
      'Use Amazon SageMaker Autopilot to handle all feature engineering automatically',
    ],
    a: 1,
    exp: 'To avoid data leakage, normalization parameters (mean, standard deviation) must be computed exclusively on the training set. Applying training-set statistics to validation and test sets ensures that information from held-out data does not influence preprocessing. Computing statistics on the full dataset before splitting contaminates the validation and test sets with training information.',
  },
  {
    domain: 'Data Engineering',
    q: 'A company wants to use Amazon S3 as a centralized data lake for ML workloads. They need to enforce fine-grained column-level access control so that only specific IAM roles can access PII columns. Which service provides this capability?',
    opts: [
      'S3 bucket policies with condition keys for specific prefixes',
      'AWS Lake Formation with column-level permissions on the Glue Data Catalog',
      'Amazon Macie with automated PII detection and blocking',
      'S3 Object Lambda with a transformation function that removes PII columns',
    ],
    a: 1,
    exp: 'AWS Lake Formation provides fine-grained access control at the database, table, column, and row level for data registered in the AWS Glue Data Catalog. Column-level permissions can be assigned to specific IAM roles, preventing those roles from querying PII columns via Athena, Glue, or EMR. S3 bucket policies operate at the object/prefix level and cannot restrict access to specific columns within a file.',
  },
  {
    domain: 'Data Engineering',
    q: 'A team has a dataset where 15% of values in a continuous numerical feature are missing. The feature is normally distributed and correlated with the target. Which imputation strategy best preserves the statistical properties for model training?',
    opts: [
      'Drop all rows with missing values in this feature',
      'Replace missing values with the feature mean computed on the training set',
      'Replace missing values with zero',
      'Use multiple imputation by chained equations (MICE) to generate plausible values',
    ],
    a: 3,
    exp: 'MICE (Multiple Imputation by Chained Equations) accounts for the uncertainty of missing values and preserves the correlation structure between features, making it superior for features that are correlated with the target. Mean imputation underestimates variance and can distort correlations. Dropping 15% of rows reduces training data significantly. Replacing with zero introduces a false signal if zero is not a meaningful value.',
  },
  {
    domain: 'Data Engineering',
    q: 'A company runs large-scale ML preprocessing jobs that take 8 hours and need to run once per week. They want to minimize cost. Which EC2 purchasing option should they use for their AWS Batch compute environment?',
    opts: [
      'On-Demand Instances for reliability',
      'Reserved Instances for consistent weekly usage',
      'Spot Instances with AWS Batch managed retries and checkpointing',
      'Dedicated Hosts for compliance requirements',
    ],
    a: 2,
    exp: 'AWS Batch natively supports Spot Instances and handles interruption and retry logic automatically. For batch workloads that run periodically and are fault-tolerant with checkpointing, Spot Instances can reduce compute costs by up to 90% compared to On-Demand. Reserved Instances are cost-effective only for continuously running workloads, which does not apply to once-weekly batch jobs.',
  },

  // ─── EXPLORATORY DATA ANALYSIS (~17 questions) ──────────────────────────────
  {
    domain: 'Exploratory Data Analysis',
    q: 'A data scientist is exploring a dataset and finds that a continuous feature has a long right tail with many extreme high values. Which transformation is most appropriate before feeding this feature into a linear model?',
    opts: [
      'Apply a square root transformation to the feature',
      'Apply a log transformation to compress the right tail',
      'Apply z-score standardization without any prior transformation',
      'Bin the feature into equal-width buckets',
    ],
    a: 1,
    exp: 'Log transformation is the standard technique for compressing right-skewed distributions, pulling extreme values closer to the bulk of the data and making the distribution more approximately normal. This helps linear models, which assume normally distributed residuals, perform better. Z-score standardization changes scale but does not correct skewness. Equal-width binning loses the continuous nature of the feature.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A team uses Amazon Athena to query data stored in S3. Query costs are high because queries scan entire datasets. Which two changes would reduce cost and improve performance? (Choose the best single answer that includes both.)',
    opts: [
      'Convert data to Parquet format and partition by query filter columns (e.g., date)',
      'Increase Athena DPU allocation and use larger instance types',
      'Move data from S3 to Amazon RDS and query via Athena Federated Query',
      'Enable Athena query result caching and increase the result retention period',
    ],
    a: 0,
    exp: 'Athena charges per byte scanned. Converting to columnar formats like Parquet reduces bytes scanned (only queried columns are read) and partitioning by frequently filtered columns (e.g., date) allows Athena to skip irrelevant partitions entirely. These two optimizations can reduce cost and query time by orders of magnitude. Athena does not have DPU allocation like Glue.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'During EDA, a data scientist observes that a feature has a Pearson correlation of 0.95 with another feature. What is the concern, and what action should be taken?',
    opts: [
      'The feature is an outlier; remove rows where the feature is extreme',
      'The features are highly multicollinear; consider removing one or using dimensionality reduction like PCA',
      'The features have the same distribution; apply different normalization to each',
      'The features are perfectly correlated; this confirms they are the same variable and both should be kept for redundancy',
    ],
    a: 1,
    exp: 'A Pearson correlation of 0.95 indicates severe multicollinearity, where one feature can be linearly predicted from the other. This inflates coefficient variance in linear models and makes them unstable. The recommended action is to drop one of the features or apply PCA to create uncorrelated components. Keeping both redundant features does not add information and hurts model interpretability.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A data scientist wants to reduce a 500-feature dataset to 2 dimensions for visualization while preserving local neighborhood structure (clusters). Which technique is most appropriate?',
    opts: [
      'PCA (Principal Component Analysis)',
      't-SNE (t-Distributed Stochastic Neighbor Embedding)',
      'Linear Discriminant Analysis (LDA)',
      'Factor Analysis',
    ],
    a: 1,
    exp: 't-SNE is specifically designed for high-dimensional data visualization by preserving local neighborhood structure, making it excellent for revealing clusters. PCA is a linear method that preserves global variance but often does not reveal cluster structure as clearly in 2D. LDA requires labeled data and maximizes class separability. t-SNE is the standard choice for exploratory cluster visualization.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A machine learning dataset contains a feature called "account_age_days" that was computed using the target variable\'s event timestamp rather than the current date. What problem does this introduce?',
    opts: [
      'Overfitting due to high-cardinality categorical encoding',
      'Data leakage, because future information is embedded in the feature',
      'Multicollinearity with the target variable',
      'Class imbalance in the training set',
    ],
    a: 1,
    exp: 'Data leakage occurs when information that would not be available at prediction time is included in the training features. Computing account_age_days using the event timestamp means the feature implicitly encodes the target event timing, giving the model information it would not have in production. This leads to unrealistically high training performance that does not generalize.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A data scientist is analyzing a feature and discovers that 40% of values are missing. The missingness appears to be related to a specific customer segment (Missing Not at Random). Which imputation approach is most appropriate?',
    opts: [
      'Drop the feature entirely since 40% missingness is too high',
      'Impute with the global mean and add a binary indicator feature for missingness',
      'Model the missingness pattern and use a model-based imputation approach',
      'Impute with the median of the non-missing values',
    ],
    a: 2,
    exp: 'When data is Missing Not at Random (MNAR), the missingness itself carries signal correlated with the target, so simple mean/median imputation or deletion introduces bias. Model-based imputation (e.g., using other features to predict the missing value, or using the missing indicator as a feature in a missingness model) accounts for the non-random pattern. Adding a binary indicator is useful but only partially addresses MNAR.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A team wants to build an interactive business intelligence dashboard on top of data in Amazon S3 and Amazon RDS for non-technical stakeholders. Which AWS service is most appropriate?',
    opts: [
      'Amazon SageMaker Studio notebooks',
      'AWS Glue DataBrew',
      'Amazon QuickSight',
      'Amazon Athena with saved queries',
    ],
    a: 2,
    exp: 'Amazon QuickSight is AWS\'s business intelligence service for building interactive dashboards and visualizations that non-technical users can consume via a web browser. It connects natively to S3, RDS, Athena, and many other sources. SageMaker notebooks and DataBrew require technical users. Athena with saved queries provides SQL access but not a point-and-click dashboard experience.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A data scientist uses a box plot to explore a numerical feature and finds that many data points lie beyond 3 IQR from the quartiles. What is the most likely explanation and appropriate action?',
    opts: [
      'The feature is normally distributed; no action is needed',
      'The feature contains outliers; investigate their cause and consider capping (winsorizing) extreme values',
      'The feature is missing data; apply mean imputation before analysis',
      'The IQR method is only valid for categorical features; switch to z-score detection',
    ],
    a: 1,
    exp: 'Points beyond 1.5×IQR (or more conservatively 3×IQR) are flagged as potential outliers. Outliers can distort model training, especially for distance-based and linear algorithms. Winsorizing (capping at percentile thresholds) is a common approach that reduces outlier influence while retaining the observations. IQR-based outlier detection is a standard method for continuous numerical features.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'During feature selection for a regression model, a data scientist wants to identify features with low predictive power to remove. Which statistical approach is appropriate for continuous features?',
    opts: [
      'Chi-square test between each feature and the continuous target',
      'Pearson correlation coefficient between each feature and the target',
      'ANOVA F-test between each categorical feature and the continuous target',
      'Mutual information between categorical features using label encoding',
    ],
    a: 1,
    exp: 'Pearson correlation measures the linear relationship between two continuous variables, making it appropriate for assessing the relationship between continuous features and a continuous regression target. Features with correlations near zero can be candidates for removal. Chi-square is for categorical-categorical relationships. ANOVA tests categorical predictors against a continuous response, not continuous-continuous pairs.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A data scientist applies PCA to a 200-feature dataset and retains components that explain 95% of cumulative variance. The resulting transformation has 15 components. What is a key trade-off of this approach?',
    opts: [
      'PCA components are highly interpretable and directly map to original features',
      'The 15 components are orthogonal and interpretable, but the remaining 5% variance may include target-relevant signal',
      'PCA requires labeled data, so it introduces supervised bias',
      'PCA can only be applied to categorical features after one-hot encoding',
    ],
    a: 1,
    exp: 'PCA components are linear combinations of original features and are not directly interpretable in domain terms. While retaining 95% variance is a common heuristic, the discarded 5% of variance could contain signal relevant to the target variable. PCA is unsupervised and operates on feature variance, not target correlation, so it may discard predictive information that has low variance.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A team uses Amazon SageMaker Data Wrangler to prepare a dataset. After joining two tables, they notice duplicate rows in the output. What is the most likely cause?',
    opts: [
      'S3 eventual consistency caused duplicate files during the join',
      'The join key has a one-to-many relationship, causing a Cartesian product-like expansion',
      'Data Wrangler automatically duplicates rows to balance class distribution',
      'The Glue Data Catalog schema is mismatched with the actual data format',
    ],
    a: 1,
    exp: 'When joining tables where the join key has a one-to-many relationship in both tables, the resulting join can produce multiple matching rows for each key value, leading to row duplication. This is a common data quality issue in join operations. It is important to validate join keys for uniqueness before performing joins. Data Wrangler does not automatically duplicate rows for balancing.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A data scientist wants to detect whether the distribution of a feature differs significantly between the training set and a new production batch (concept drift). Which statistical test is most appropriate?',
    opts: [
      'Pearson chi-square goodness-of-fit test for continuous distributions',
      'Kolmogorov-Smirnov (K-S) test to compare two sample distributions',
      'Student\'s t-test to compare means of the two samples',
      'F-test to compare variances of the two samples',
    ],
    a: 1,
    exp: 'The two-sample Kolmogorov-Smirnov test compares the cumulative distribution functions of two samples and detects differences in shape, spread, and location—making it ideal for detecting distributional drift between a training set and a production batch. The t-test only compares means and would miss distributional shape changes. The F-test only compares variances. Chi-square for continuous distributions requires binning and loses information.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A machine learning team is performing EDA on a text dataset and wants to understand word importance across documents without building a full model. Which technique provides a TF-IDF style understanding of term relevance?',
    opts: [
      'Count vectorization followed by PCA',
      'Term Frequency-Inverse Document Frequency (TF-IDF) vectorization',
      'Word2Vec embedding trained on the corpus',
      'BERT tokenization with subword encoding',
    ],
    a: 1,
    exp: 'TF-IDF directly quantifies how important a word is to a document relative to its frequency across all documents—terms that appear often in one document but rarely across others get high TF-IDF scores, capturing discriminative words. This is interpretable and computationally efficient for EDA. Word2Vec and BERT produce dense embeddings useful for modeling but are less directly interpretable for term importance analysis.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'During EDA, a data scientist computes a confusion matrix on a multi-class classification problem with 10 classes and finds that class 3 is frequently confused with class 7. What action should be taken?',
    opts: [
      'Increase the regularization parameter to reduce model complexity',
      'Investigate feature overlap between class 3 and class 7, and consider engineering discriminating features',
      'Remove class 3 from the dataset to simplify the problem',
      'Apply PCA to all features to remove multicollinearity between classes',
    ],
    a: 1,
    exp: 'Frequent confusion between two classes indicates they share similar feature representations. The appropriate action is to investigate which features overlap between the two classes and engineer new discriminating features or collect additional data that better separates them. Removing a class changes the problem definition. PCA and regularization do not address class-level feature similarity.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A data scientist is using Amazon SageMaker Data Wrangler and wants to export a feature transformation recipe to be applied in a SageMaker Processing job for production. What does Data Wrangler generate for this export?',
    opts: [
      'A CloudFormation template that deploys a dedicated EC2 preprocessing server',
      'A Jupyter notebook with SageMaker Processing job code that applies the same transformations',
      'An AWS Glue ETL script that replicates the transformations in Apache Spark',
      'A Lambda function that applies transformations to each new S3 object via S3 event notifications',
    ],
    a: 1,
    exp: 'SageMaker Data Wrangler can export transformation flows as Jupyter notebooks containing SageMaker Processing job code, allowing the same transformations to be operationalized at scale. This bridges the gap between interactive data preparation and production ML pipelines. The exported code uses the SageMaker SDK and can be integrated into SageMaker Pipelines for automation.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A data scientist has a dataset with 1,000 features. After applying univariate feature selection, 200 features remain. They then train a Random Forest and extract feature importances. What issue might arise from this two-stage approach?',
    opts: [
      'Random Forest cannot handle more than 100 features at once',
      'Univariate selection can miss features that are only predictive in combination, so the two-stage approach may discard important interaction features',
      'Feature importance from Random Forest is biased toward categorical features',
      'Univariate selection requires normally distributed features to be valid',
    ],
    a: 1,
    exp: 'Univariate feature selection evaluates each feature independently with respect to the target, so it misses features that have low individual correlation but high combined predictive power (interaction effects). This means important features could be discarded in the first stage before the Random Forest ever sees them. Wrapper methods or model-based selection consider feature interactions.',
  },
  {
    domain: 'Exploratory Data Analysis',
    q: 'A team analyzes a time-series dataset of server CPU metrics and notices a clear 24-hour repeating pattern. What type of pattern is this, and which decomposition method should be applied?',
    opts: [
      'This is a trend component; apply linear regression to detrend the series',
      'This is a seasonal component with daily periodicity; apply seasonal decomposition (e.g., STL) to separate trend, seasonality, and residuals',
      'This is autocorrelation; apply differencing to make the series stationary',
      'This is a cyclical component; use ARIMA without seasonal terms',
    ],
    a: 1,
    exp: 'A repeating pattern with a fixed period (24 hours) is a seasonal component. STL (Seasonal and Trend decomposition using Loess) is a robust method for separating time series into trend, seasonal, and residual components. Understanding and removing seasonality is important before feeding time-series data into many ML models. Differencing addresses non-stationarity but does not specifically isolate and explain seasonal patterns.',
  },

  // ─── MODELING (~25 questions) ────────────────────────────────────────────────
  {
    domain: 'Modeling',
    q: 'A data scientist needs to forecast daily sales for 500 individual product SKUs simultaneously, each with 3 years of historical data. Which SageMaker built-in algorithm is most appropriate?',
    opts: [
      'SageMaker Linear Learner with one model per SKU',
      'SageMaker DeepAR, which trains a single global model across all time series',
      'SageMaker K-Means to cluster similar SKUs before forecasting',
      'SageMaker XGBoost with lag features manually engineered for each SKU',
    ],
    a: 1,
    exp: 'SageMaker DeepAR is specifically designed for probabilistic forecasting of many related time series simultaneously. It trains a single RNN-based model on all series, leveraging cross-series patterns to improve accuracy, especially for series with limited history. Training 500 separate Linear Learner models is operationally complex and ignores inter-series relationships. XGBoost requires extensive manual feature engineering for time series.',
  },
  {
    domain: 'Modeling',
    q: 'A team trains an XGBoost model and observes that training accuracy is 99% but validation accuracy is 72%. Which approach is most likely to improve validation performance?',
    opts: [
      'Increase the number of estimators (n_estimators) to train longer',
      'Increase the learning rate to converge faster',
      'Add L1/L2 regularization, reduce max_depth, and increase min_child_weight to reduce overfitting',
      'Switch to a more complex model like a deep neural network',
    ],
    a: 2,
    exp: 'A large gap between training and validation accuracy is a classic sign of overfitting (high variance). For XGBoost, regularization parameters (alpha for L1, lambda for L2), reducing tree depth (max_depth), and increasing min_child_weight all constrain model complexity and reduce overfitting. Increasing n_estimators or learning rate without regularization would worsen overfitting. A more complex model would exacerbate the problem.',
  },
  {
    domain: 'Modeling',
    q: 'A credit risk model must be highly interpretable to satisfy regulatory requirements. The team is choosing between XGBoost and Logistic Regression. Both achieve similar AUC on the test set. Which should they choose and why?',
    opts: [
      'XGBoost, because it always outperforms Logistic Regression on tabular data',
      'Logistic Regression, because its coefficients provide direct, linear, and interpretable feature contributions that satisfy regulatory scrutiny',
      'XGBoost with SHAP values, because SHAP is universally accepted by financial regulators as interpretable',
      'Logistic Regression with polynomial features to capture non-linearity while maintaining interpretability',
    ],
    a: 1,
    exp: 'Logistic Regression provides direct coefficient interpretation: the log-odds change per unit increase in each feature. This linear, additive structure is straightforward to explain to regulators. While SHAP values can explain XGBoost predictions post-hoc, many regulators explicitly require inherently interpretable models. When both achieve similar performance, interpretability requirements favor Logistic Regression.',
  },
  {
    domain: 'Modeling',
    q: 'A medical image classification model achieves 95% accuracy on a dataset where 95% of samples are healthy. What is the most appropriate evaluation metric for this use case?',
    opts: [
      'Accuracy, since it is above 95% threshold',
      'AUC-ROC, to evaluate the model\'s ability to discriminate between classes across all thresholds',
      'Mean Squared Error, to measure prediction distance from the true label',
      'BLEU score, to evaluate text quality of the diagnosis output',
    ],
    a: 1,
    exp: 'With severe class imbalance, a model predicting "healthy" for every sample achieves 95% accuracy without learning anything meaningful. AUC-ROC measures the model\'s ability to discriminate between the positive and negative classes across all decision thresholds, making it robust to class imbalance. BLEU is for text generation tasks. MSE is for regression. Accuracy alone is misleading here.',
  },
  {
    domain: 'Modeling',
    q: 'A data scientist is tuning an XGBoost model with 8 hyperparameters. They have a compute budget for 50 training jobs. Which hyperparameter optimization strategy is most efficient for this scenario?',
    opts: [
      'Grid search over all combinations of hyperparameters',
      'Manual trial-and-error based on intuition',
      'Bayesian optimization using SageMaker Automatic Model Tuning',
      'Random search with 50 random configurations',
    ],
    a: 2,
    exp: 'Bayesian optimization builds a probabilistic model of the objective function and uses it to select the most promising configurations to evaluate next, making it significantly more sample-efficient than grid or random search. SageMaker Automatic Model Tuning implements Bayesian optimization and is designed exactly for this scenario. With 8 hyperparameters and a budget of 50 jobs, grid search is infeasible and random search wastes budget on uninformative regions.',
  },
  {
    domain: 'Modeling',
    q: 'A team uses the SageMaker BlazingText algorithm in Word2Vec mode. What is the primary output of this training job?',
    opts: [
      'A classification model that predicts document categories',
      'Dense vector embeddings for each word in the vocabulary that capture semantic relationships',
      'A language model that generates text token by token',
      'A topic model that assigns latent topics to documents',
    ],
    a: 1,
    exp: 'BlazingText in Word2Vec mode trains word embeddings—dense numerical vector representations of words where semantically similar words have similar vectors (e.g., king - man + woman ≈ queen). These embeddings can be used as features in downstream NLP models. BlazingText also supports text classification mode, but Word2Vec mode outputs embeddings, not a classifier.',
  },
  {
    domain: 'Modeling',
    q: 'A model trained on customer transaction data has high bias (underfitting). The team is using a linear model. Which interventions are most likely to reduce bias?',
    opts: [
      'Increase regularization strength (higher lambda)',
      'Engineer non-linear polynomial features and interaction terms, or switch to a more complex model',
      'Reduce the training dataset size to prevent overfitting',
      'Apply dropout regularization and early stopping',
    ],
    a: 1,
    exp: 'High bias indicates that the model is too simple to capture the patterns in the data. For a linear model, the primary fixes are to enrich the feature space with polynomial and interaction features, or to switch to a more expressive model (e.g., XGBoost, neural network). Increasing regularization, reducing data, or adding dropout all constrain model capacity further, worsening underfitting.',
  },
  {
    domain: 'Modeling',
    q: 'A team is using SageMaker\'s built-in K-Means algorithm to cluster customer purchase patterns. After training, they need to determine the optimal number of clusters. Which approach should they use?',
    opts: [
      'Use the elbow method by plotting within-cluster sum of squares (WCSS) vs. number of clusters',
      'Use cross-validation with k-fold splits on the clustering objective',
      'Select k = sqrt(n/2) where n is the number of data points',
      'The SageMaker K-Means algorithm automatically selects the optimal k',
    ],
    a: 0,
    exp: 'The elbow method plots WCSS (inertia) against different values of k; the optimal k is where the rate of decrease in WCSS slows sharply, forming an "elbow." This is the standard heuristic for selecting k in K-Means. Cross-validation is not directly applicable to unsupervised clustering in the same way as supervised learning. SageMaker K-Means requires the user to specify k.',
  },
  {
    domain: 'Modeling',
    q: 'A company wants to use SageMaker Autopilot to automatically build a binary classification model. What does Autopilot do during its run?',
    opts: [
      'It trains a single pre-specified algorithm with default hyperparameters and returns predictions',
      'It automatically performs feature engineering, algorithm selection, and hyperparameter optimization, producing multiple candidate models with explainability reports',
      'It deploys a model to a SageMaker endpoint without requiring any training data',
      'It only supports neural network architectures for classification tasks',
    ],
    a: 1,
    exp: 'SageMaker Autopilot is an AutoML service that automatically preprocesses data, selects algorithms from a candidate set, runs hyperparameter optimization, and produces a leaderboard of models with performance metrics and explainability reports (using SHAP). It supports multiple algorithm types including XGBoost, linear models, and neural networks, not just one architecture.',
  },
  {
    domain: 'Modeling',
    q: 'A data scientist uses 5-fold cross-validation instead of a single train/test split. What is the primary advantage?',
    opts: [
      'It trains 5 separate production models that can be ensembled automatically',
      'It provides a less biased and lower-variance estimate of model generalization performance by using all data for both training and validation',
      'It eliminates the need for a separate test set since all data is used in cross-validation',
      'It is faster than a single train/test split because each fold uses less data',
    ],
    a: 1,
    exp: 'K-fold cross-validation trains and evaluates the model k times, each time using a different fold as the validation set. This means every observation is used for validation exactly once and for training k-1 times, giving a more reliable estimate of generalization performance with lower variance than a single split. A separate held-out test set is still recommended for final unbiased evaluation; cross-validation does not replace it.',
  },
  {
    domain: 'Modeling',
    q: 'A team uses the SageMaker Seq2Seq built-in algorithm. For which task is this algorithm most appropriate?',
    opts: [
      'Clustering customer segments from transaction logs',
      'Translating text from English to French or summarizing long documents',
      'Detecting objects in images with bounding box predictions',
      'Predicting time-series values for univariate sensor data',
    ],
    a: 1,
    exp: 'SageMaker Seq2Seq implements an encoder-decoder RNN architecture designed for sequence-to-sequence tasks such as machine translation, text summarization, and speech-to-text. It takes a variable-length input sequence and produces a variable-length output sequence. Object detection uses SageMaker\'s Object Detection algorithm. Time-series forecasting uses DeepAR. Clustering uses K-Means or IP Insights.',
  },
  {
    domain: 'Modeling',
    q: 'A SageMaker training job using a custom PyTorch container fails with a CUDA out-of-memory error. The team is using ml.p3.2xlarge (16 GB GPU). Which changes could resolve this issue?',
    opts: [
      'Increase the number of training instances (horizontal scaling)',
      'Reduce the batch size and/or use gradient checkpointing to reduce GPU memory consumption',
      'Switch from GPU to CPU instances to avoid CUDA memory limits',
      'Increase the number of epochs to allow the model to learn with smaller batches',
    ],
    a: 1,
    exp: 'CUDA out-of-memory errors occur when the model, optimizer states, and batch activations exceed GPU VRAM. Reducing batch size decreases the memory required per forward/backward pass. Gradient checkpointing trades compute for memory by recomputing activations during the backward pass. Switching to CPU avoids the VRAM limit but dramatically slows training. Increasing epochs does not reduce memory usage.',
  },
  {
    domain: 'Modeling',
    q: 'A data scientist is using SageMaker Debugger to monitor a training job and receives an alert that the gradient:vanishing rule has triggered. What does this indicate and what is the recommended fix?',
    opts: [
      'The model is overfitting; increase regularization',
      'Gradients in early layers are approaching zero, preventing effective learning; consider using different activation functions (e.g., ReLU instead of sigmoid) or batch normalization',
      'The loss function is not converging; switch from SGD to Adam optimizer',
      'The training data contains NaN values; apply data cleaning before retraining',
    ],
    a: 1,
    exp: 'Vanishing gradients occur when gradients become extremely small as they propagate backward through deep networks, stalling learning in early layers. This is common with sigmoid and tanh activations. Solutions include using ReLU activations (which have non-saturating gradients for positive inputs), batch normalization, or residual connections. SageMaker Debugger\'s built-in vanishing gradient rule detects this automatically during training.',
  },
  {
    domain: 'Modeling',
    q: 'A team needs to train an object detection model on 100,000 annotated images using SageMaker\'s built-in Object Detection algorithm. Which input data format does this algorithm require?',
    opts: [
      'COCO JSON annotation format with a manifest file',
      'RecordIO format with image data and annotation files in the SageMaker augmented manifest format',
      'TFRecord format compatible with TensorFlow\'s data pipeline',
      'CSV with base64-encoded image strings and bounding box coordinates',
    ],
    a: 1,
    exp: 'SageMaker\'s built-in Object Detection algorithm supports RecordIO format (for images packed into binary files for efficient reading) and the augmented manifest format (JSON Lines files referencing S3 image locations with annotations). These formats are optimized for high-throughput training on S3. COCO JSON is a common annotation format but must be converted to one of the supported SageMaker formats.',
  },
  {
    domain: 'Modeling',
    q: 'A company uses SageMaker JumpStart to deploy a pre-trained large language model. What is the primary benefit of using JumpStart compared to training from scratch?',
    opts: [
      'JumpStart models are always smaller and faster than custom-trained models',
      'JumpStart provides one-click access to pre-trained foundation models and fine-tuning notebooks, dramatically reducing time-to-deployment',
      'JumpStart automatically selects the optimal model architecture based on the dataset',
      'JumpStart models are free and do not incur compute charges for training or inference',
    ],
    a: 1,
    exp: 'SageMaker JumpStart provides a model hub with hundreds of pre-trained models (including foundation models from AWS and third parties) along with ready-to-use Jupyter notebooks for fine-tuning and deployment. This eliminates the need to source, package, and configure models from scratch, significantly accelerating development. JumpStart models still incur standard SageMaker compute charges.',
  },
  {
    domain: 'Modeling',
    q: 'A regression model\'s residuals show a clear fan-shaped pattern when plotted against predicted values (residuals increase with the magnitude of predictions). What does this indicate?',
    opts: [
      'Autocorrelation in the residuals due to a time-series structure',
      'Heteroscedasticity—the variance of errors is not constant; consider a log transformation of the target or using weighted least squares',
      'High bias in the model; add more features to reduce underfitting',
      'Multicollinearity between predictors; apply ridge regression',
    ],
    a: 1,
    exp: 'A fan-shaped residual plot (variance increasing with fitted values) is the signature of heteroscedasticity—non-constant error variance—which violates a key assumption of OLS regression. The recommended fixes are transforming the target (e.g., log transformation), using weighted least squares, or using models robust to heteroscedasticity. This is not related to bias, multicollinearity, or autocorrelation.',
  },
  {
    domain: 'Modeling',
    q: 'A SageMaker Linear Learner job is configured for binary classification. The team wants to optimize for maximizing recall (minimizing false negatives) in a fraud detection use case. How should they configure the objective?',
    opts: [
      'Set predictor_type to regressor and post-process the output',
      'Set binary_classifier_model_selection_criteria to recall_at_target_precision with a precision target',
      'Set the loss function to squared_loss to penalize false negatives more',
      'No configuration needed; Linear Learner automatically optimizes for recall in classification tasks',
    ],
    a: 1,
    exp: 'SageMaker Linear Learner supports optimizing for specific business objectives through binary_classifier_model_selection_criteria. Setting it to recall_at_target_precision allows the algorithm to find the model that maximizes recall while maintaining a specified minimum precision, which is ideal for fraud detection where missing fraud (false negatives) is costly. The default objective is accuracy, not recall.',
  },
  {
    domain: 'Modeling',
    q: 'A data scientist is building a semantic segmentation model using SageMaker\'s built-in Semantic Segmentation algorithm. What is the output of this model for a given input image?',
    opts: [
      'A set of bounding boxes with class labels for detected objects',
      'A pixel-wise class label mask assigning a category to every pixel in the image',
      'A single class label for the entire image',
      'A set of keypoints marking object locations in the image',
    ],
    a: 1,
    exp: 'Semantic segmentation assigns a class label to every pixel in an input image, producing a dense label mask of the same spatial dimensions as the input. This differs from object detection (bounding boxes) and image classification (single label per image). SageMaker\'s Semantic Segmentation algorithm uses FCN, PSP, or DeepLab architectures for this pixel-level prediction task.',
  },
  {
    domain: 'Modeling',
    q: 'A model achieves an AUC-ROC of 0.95 on the training set but 0.61 on the test set. Which regularization techniques would be most appropriate to improve test performance?',
    opts: [
      'Increase model depth and add more features to improve training performance',
      'Apply L2 regularization (weight decay), dropout (for neural networks), or reduce model complexity to combat overfitting',
      'Apply L1 regularization only to categorical features',
      'Use data augmentation exclusively on the test set to improve evaluation',
    ],
    a: 1,
    exp: 'A large gap between training AUC (0.95) and test AUC (0.61) indicates severe overfitting. L2 regularization penalizes large weights, reducing model complexity. Dropout prevents co-adaptation of neurons in neural networks. Reducing model complexity (fewer layers, less depth) directly reduces the model\'s capacity to memorize training data. Data augmentation should be applied to training data, not the test set.',
  },
  {
    domain: 'Modeling',
    q: 'A data scientist is evaluating a multi-class classification model. The confusion matrix shows that the model performs well for majority classes but poorly for minority classes. Which metric best captures per-class performance accounting for class imbalance?',
    opts: [
      'Overall accuracy',
      'Macro-averaged F1 score, which computes F1 for each class and averages them equally regardless of class frequency',
      'Weighted-averaged precision, weighted by class support',
      'Area under the ROC curve (AUC-ROC) for the majority class only',
    ],
    a: 1,
    exp: 'Macro-averaged F1 computes the F1 score independently for each class and takes an unweighted average, giving equal importance to every class regardless of its frequency. This surfaces poor performance on minority classes that would be masked by accuracy or weighted averaging. Weighted averaging penalizes minority class errors less because it weights by class frequency.',
  },
  {
    domain: 'Modeling',
    q: 'A team needs to evaluate the quality of a machine translation model that translates English to Spanish. Which evaluation metric is the industry standard for this task?',
    opts: [
      'RMSE between predicted and ground-truth token embeddings',
      'BLEU (Bilingual Evaluation Understudy) score comparing n-gram overlap with reference translations',
      'AUC-ROC treating translation as a binary classification problem',
      'Perplexity of the target-language language model',
    ],
    a: 1,
    exp: 'BLEU score is the standard automatic evaluation metric for machine translation. It measures the n-gram precision of the model\'s output against one or more reference translations, with a brevity penalty for short outputs. Higher BLEU scores indicate closer alignment with human translations. RMSE and AUC-ROC are not applicable to text generation tasks. Perplexity measures language model fluency but not translation accuracy.',
  },
  {
    domain: 'Modeling',
    q: 'During SageMaker HPO (Hyperparameter Optimization), a data scientist notices that later training jobs are not improving on early jobs. The hyperparameter search space is very wide. What adjustment should be made?',
    opts: [
      'Increase the maximum number of training jobs to allow more exploration',
      'Narrow the search space based on the best-performing parameter ranges found so far, and enable warm starting from the previous tuning job',
      'Switch from Bayesian optimization to random search to increase exploration',
      'Reduce the number of hyperparameters to a single parameter to simplify optimization',
    ],
    a: 1,
    exp: 'When early training jobs identify promising regions of the hyperparameter space, narrowing the search space around those regions and enabling warm starting (which initializes the Bayesian model with results from a previous tuning job) allows SageMaker HPO to focus exploration efficiently. Switching to random search abandons the accumulated information. Simply adding more jobs without narrowing the space wastes budget on unproductive regions.',
  },
  {
    domain: 'Modeling',
    q: 'A data scientist trains a Random Forest with 500 trees and evaluates feature importance using mean decrease in impurity (MDI). A concern is that MDI may be biased. What is the known bias of MDI-based feature importance?',
    opts: [
      'MDI overestimates the importance of low-cardinality categorical features',
      'MDI overestimates the importance of high-cardinality features (many unique values) because there are more split opportunities',
      'MDI is biased toward features with many missing values',
      'MDI underestimates the importance of features that are correlated with the target',
    ],
    a: 1,
    exp: 'MDI-based feature importance in tree models is known to be biased toward high-cardinality features (e.g., continuous features or categorical features with many unique values) because such features offer more possible split points, making it statistically easier for a tree to find a useful split. Permutation importance is a less biased alternative that evaluates feature importance by measuring the drop in performance when a feature is randomly shuffled.',
  },
  {
    domain: 'Modeling',
    q: 'A company is building a recommendation system and needs to find similar items based on latent factors from user-item interaction data. Which SageMaker algorithm is designed for this matrix factorization task?',
    opts: [
      'SageMaker K-Nearest Neighbors (KNN)',
      'SageMaker Factorization Machines',
      'SageMaker K-Means',
      'SageMaker Principal Component Analysis (PCA)',
    ],
    a: 1,
    exp: 'SageMaker Factorization Machines are specifically designed for recommendation and collaborative filtering tasks. They model pairwise feature interactions through latent factor decomposition, which is well-suited for sparse user-item interaction matrices. KNN retrieves similar items based on distance but does not perform matrix factorization. K-Means clusters items but does not model user-item interactions.',
  },
  {
    domain: 'Modeling',
    q: 'A text classification model is trained with SageMaker BlazingText in supervised mode. The training data has 50 classes with significant class overlap. The model\'s validation accuracy is poor. Which hyperparameter adjustment is most likely to help?',
    opts: [
      'Increase the wordNgrams parameter to capture longer n-gram context and improve discrimination between similar classes',
      'Reduce the learning_rate to ensure stable convergence',
      'Decrease the vector_dim to force the model to learn more compact representations',
      'Enable subwords mode to handle out-of-vocabulary words',
    ],
    a: 0,
    exp: 'In BlazingText\'s supervised (text classification) mode, increasing wordNgrams (from 1 to 2 or 3) allows the model to consider sequences of consecutive words as features, capturing phrase-level context that helps discriminate between classes with similar individual word distributions. This is particularly effective for classes with overlapping vocabulary but different phrase patterns.',
  },

  // ─── ML IMPLEMENTATION & OPERATIONS (~14 questions) ─────────────────────────
  {
    domain: 'ML Implementation & Operations',
    q: 'A company needs to deploy a SageMaker model for real-time fraud scoring with requirements of p99 latency under 100ms and autoscaling from 0 to 1,000 requests per second. Which deployment option is most appropriate?',
    opts: [
      'SageMaker Batch Transform for daily batch scoring',
      'SageMaker Asynchronous Inference endpoint for queuing requests',
      'SageMaker Real-Time Inference endpoint with Application Auto Scaling',
      'SageMaker Serverless Inference endpoint',
    ],
    a: 2,
    exp: 'SageMaker Real-Time Inference endpoints provide synchronous, low-latency predictions and support Application Auto Scaling based on InvocationsPerInstance metrics, enabling scale from low to high traffic. For sub-100ms p99 latency requirements at sustained high throughput, real-time endpoints with pre-warmed instances are required. Serverless inference has cold-start latency issues. Async inference is for long-running requests, not real-time scoring.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A data science team wants to track model versions, approve them for production, and maintain a lineage of training metrics and artifacts. Which SageMaker feature provides this capability?',
    opts: [
      'SageMaker Experiments for tracking training runs',
      'SageMaker Model Registry for versioning, metadata, and approval workflows',
      'SageMaker Feature Store for storing and versioning feature transformations',
      'Amazon ECR for versioning model container images',
    ],
    a: 1,
    exp: 'SageMaker Model Registry provides a centralized repository for registering model versions with associated metadata (training metrics, artifacts, lineage), and supports approval workflows (Approved/Rejected/PendingManualApproval) for governing model promotion to production. SageMaker Experiments tracks training runs but does not provide deployment approval workflows. Feature Store versions features, not models.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A company deploys a SageMaker model and wants to detect when the distribution of incoming features drifts from the training baseline. Which SageMaker feature provides automated drift detection?',
    opts: [
      'SageMaker Debugger with built-in training rules',
      'SageMaker Model Monitor with a data quality monitoring job',
      'SageMaker Clarify for bias detection',
      'SageMaker Pipelines with a data validation step',
    ],
    a: 1,
    exp: 'SageMaker Model Monitor continuously monitors deployed endpoints by capturing inference data and comparing it against a baseline computed from training data. It detects data drift (feature distribution changes), model quality degradation, bias drift, and feature attribution drift, generating CloudWatch alerts when violations are detected. SageMaker Debugger monitors training jobs, not deployed endpoints.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A ML team wants to run A/B testing between two model versions (Model A and Model B) with 80%/20% traffic split on a single SageMaker endpoint. How should this be configured?',
    opts: [
      'Deploy two separate endpoints and use Route 53 weighted routing policies',
      'Configure a single endpoint with two production variants, each with an InitialVariantWeight that sums to 1',
      'Use SageMaker Pipelines to route 80% of batch data to Model A and 20% to Model B',
      'Use Amazon API Gateway canary deployments in front of a single SageMaker endpoint',
    ],
    a: 1,
    exp: 'SageMaker endpoints natively support production variants—multiple model versions deployed on the same endpoint with configurable traffic weights. Setting InitialVariantWeight to 0.8 for Model A and 0.2 for Model B routes traffic in the desired ratio without external infrastructure. SageMaker also provides per-variant CloudWatch metrics for comparison. Route 53 and API Gateway add unnecessary complexity.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A company wants to reduce the cost of running weekly batch inference on 10 million records. Each batch takes 4 hours to complete. Which deployment mode is most cost-effective?',
    opts: [
      'Real-time endpoint running 24/7 with auto-scaling to zero',
      'SageMaker Batch Transform, which spins up compute, runs inference, and shuts down automatically',
      'Asynchronous inference endpoint with a large request queue',
      'SageMaker Serverless Inference with no minimum capacity',
    ],
    a: 1,
    exp: 'SageMaker Batch Transform is designed for offline batch inference: it creates compute instances, runs inference over the entire input dataset in S3, writes results back to S3, and terminates the instances automatically. You only pay for compute during the job runtime. A 24/7 real-time endpoint incurs continuous costs. Serverless inference has higher per-invocation latency and is not optimized for large batch workloads.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A team wants to use SageMaker Feature Store to share precomputed features across multiple ML models in a production environment. Which two stores does SageMaker Feature Store provide?',
    opts: [
      'Online store (low-latency, real-time access) and Offline store (historical, batch access via S3)',
      'Training store (for model training) and Serving store (for model inference)',
      'Primary store (read-write) and Replica store (read-only for redundancy)',
      'Structured store (tabular features) and Unstructured store (image/text features)',
    ],
    a: 0,
    exp: 'SageMaker Feature Store provides two complementary stores: the Online Store, backed by a fully managed low-latency database for real-time feature retrieval during inference, and the Offline Store, backed by S3 for historical feature data used in model training and batch inference. Both stores are synchronized when features are ingested, ensuring consistency between training and serving environments.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A company trains large SageMaker training jobs that each take 10 hours. They want to significantly reduce training costs without changing the model architecture. What is the most impactful cost optimization?',
    opts: [
      'Use Amazon S3 Intelligent-Tiering to reduce storage costs for training data',
      'Enable SageMaker Managed Spot Training to use EC2 Spot Instances with checkpointing',
      'Use smaller instance types (e.g., ml.m5.xlarge instead of ml.p3.8xlarge)',
      'Switch from multi-instance training to single-instance training to avoid network overhead',
    ],
    a: 1,
    exp: 'SageMaker Managed Spot Training uses EC2 Spot Instances, which cost up to 90% less than On-Demand instances. SageMaker handles Spot interruptions automatically, and checkpointing allows interrupted jobs to resume from the last checkpoint rather than restarting from scratch. This is the highest-impact cost optimization for long training jobs. Using smaller instances would increase training duration and may not reduce total cost.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A company needs to deploy a SageMaker model to IoT edge devices with limited memory and compute. Which SageMaker service optimizes and compiles the model for target hardware?',
    opts: [
      'SageMaker Model Monitor',
      'SageMaker Neo',
      'SageMaker JumpStart',
      'AWS IoT Greengrass with SageMaker components',
    ],
    a: 1,
    exp: 'SageMaker Neo compiles and optimizes ML models for specific target hardware platforms (including ARM, Intel, NVIDIA Jetson, and others) using TVM, reducing model size and increasing inference speed on edge devices. It also generates a runtime that can run the optimized model without a full SageMaker environment. AWS IoT Greengrass can host models but does not perform hardware-specific compilation and optimization.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A data scientist runs a SageMaker training job that accesses sensitive training data in S3. The security team requires that no traffic traverse the public internet. How should the training job be configured?',
    opts: [
      'Enable S3 server-side encryption (SSE-KMS) on the training data bucket',
      'Run the training job in VPC mode with a VPC endpoint for S3 and disable public internet access',
      'Use IAM resource-based policies to restrict S3 access to only the SageMaker service principal',
      'Enable SageMaker network isolation mode (NetworkIsolation=True) on the training container',
    ],
    a: 1,
    exp: 'Running SageMaker training jobs in VPC mode places the training instances inside a private VPC. Adding an S3 VPC endpoint (Gateway endpoint) routes all S3 traffic through the AWS private network without traversing the public internet. Disabling outbound internet access (no NAT gateway or internet gateway) ensures no traffic leaves AWS\'s private network. Encryption (SSE-KMS) protects data at rest but does not control network routing.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A company uses SageMaker Pipelines to orchestrate an ML workflow. They want to avoid re-running expensive preprocessing steps when only the hyperparameters change. Which SageMaker Pipelines feature supports this?',
    opts: [
      'SageMaker Pipelines step parallelism to run preprocessing and training simultaneously',
      'SageMaker Pipelines step caching, which reuses outputs of steps whose inputs have not changed',
      'SageMaker Experiments to track and compare previous run outputs',
      'SageMaker Model Registry to store and retrieve preprocessing artifacts',
    ],
    a: 1,
    exp: 'SageMaker Pipelines supports step-level caching: when a pipeline run is triggered and an upstream step\'s inputs (data, code, parameters) have not changed compared to a previous successful run, SageMaker reuses the cached output and skips re-execution. This significantly reduces pipeline runtime and cost when only downstream steps (like hyperparameters) change.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A SageMaker endpoint receives very spiky traffic: idle for hours, then receiving hundreds of requests per minute for short bursts. Which deployment option minimizes cost while handling the burst traffic?',
    opts: [
      'Real-time endpoint with minimum capacity set to 0 and Application Auto Scaling',
      'SageMaker Serverless Inference, which scales to zero when idle and provisions capacity on demand',
      'SageMaker Batch Transform triggered by an EventBridge rule when requests arrive',
      'SageMaker Async Inference with a large queue and small always-on instance fleet',
    ],
    a: 1,
    exp: 'SageMaker Serverless Inference is purpose-built for spiky, unpredictable traffic patterns. It scales to zero when idle (no charges when not in use) and automatically provisions compute capacity when requests arrive, billing only per invocation and for compute time used. Real-time endpoints cannot currently scale to zero. Batch Transform is for offline workloads, not interactive burst request handling.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A security team requires that all SageMaker notebook instances and training jobs use customer-managed KMS keys for encrypting notebook volumes and training data at rest. Which configuration achieves this?',
    opts: [
      'Enable SageMaker default encryption which uses AWS-managed keys automatically',
      'Specify a KMS key ARN in the KmsKeyId parameter when creating notebook instances and training jobs',
      'Enable S3 bucket default encryption with SSE-S3 on all SageMaker-related buckets',
      'Use VPC endpoints for KMS to route encryption operations through the private network',
    ],
    a: 1,
    exp: 'SageMaker supports customer-managed KMS keys (CMKs) for encrypting EBS volumes attached to notebook instances and training instances, as well as output artifacts in S3. The KMS key ARN is specified in the KmsKeyId parameter when creating notebook instances, training jobs, and processing jobs. This gives the security team full control over key rotation and access policies. AWS-managed keys do not provide the same level of customer control.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A company wants to use SageMaker Model Monitor to detect when model predictions diverge from ground-truth labels in production (model quality monitoring). What must be set up as a prerequisite?',
    opts: [
      'A SageMaker training job must be re-run weekly to generate a fresh baseline',
      'Data capture must be enabled on the endpoint and a ground-truth label merge job must feed actuals back to a monitoring baseline',
      'SageMaker Clarify must first run a bias detection job to establish a fairness baseline',
      'Amazon CloudWatch Logs must be enabled to capture endpoint invocation logs',
    ],
    a: 1,
    exp: 'Model quality monitoring requires: (1) enabling data capture on the endpoint to log inference requests and predictions, and (2) a mechanism to merge actual ground-truth labels with the logged predictions (via a merge job or labeling pipeline). The monitoring job then computes metrics like accuracy or AUC by comparing predictions to actuals. Without ground-truth labels merged with captured data, model quality monitoring cannot compute quality metrics.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A ML operations team wants to grant a SageMaker training job the minimum necessary permissions to read from a specific S3 bucket and write to a specific S3 prefix, following the principle of least privilege. How should they configure this?',
    opts: [
      'Attach the AmazonS3FullAccess managed policy to the SageMaker execution role',
      'Create a custom IAM policy granting s3:GetObject on the source bucket and s3:PutObject on the output prefix, and attach it to the SageMaker execution role',
      'Use S3 bucket ACLs to grant access to the SageMaker service account',
      'Enable SageMaker network isolation to prevent the training job from accessing unintended resources',
    ],
    a: 1,
    exp: 'The principle of least privilege requires granting only the specific permissions needed. A custom IAM policy that grants s3:GetObject on the source bucket (with a resource ARN scoped to that bucket) and s3:PutObject on the output prefix (scoped to that specific prefix) provides exactly the access needed without broader permissions. AmazonS3FullAccess grants access to all S3 buckets and all operations, violating least privilege.',
  },
  {
    domain: 'ML Implementation & Operations',
    q: 'A SageMaker Pipelines workflow includes a training step that runs for 6 hours. The team wants to receive a Slack notification when the pipeline execution succeeds or fails. Which AWS service orchestrates this notification?',
    opts: [
      'SageMaker Pipelines built-in notification feature in the SageMaker console',
      'Amazon EventBridge rule triggered by SageMaker Pipeline execution state changes, invoking an SNS topic that forwards to Slack via a Lambda or Chatbot integration',
      'AWS CloudTrail log monitoring with a CloudWatch alarm on API calls',
      'SageMaker Experiments notification hooks configured in the experiment config',
    ],
    a: 1,
    exp: 'SageMaker Pipelines publishes execution state change events (Succeeded, Failed, Stopped) to Amazon EventBridge. An EventBridge rule can match these events and route them to an SNS topic. AWS Chatbot can subscribe to the SNS topic and deliver formatted notifications to a Slack channel. This is the standard event-driven notification pattern for SageMaker Pipelines without requiring polling or custom monitoring code.',
  },
];
