import type { Question } from '../types';

export const BANK_DVA: Question[] = [
  // ── Lambda ──────────────────────────────────────────────────────────────────
  {
    domain: 'Development with AWS Services',
    q: 'A Lambda function processes S3 events but occasionally fails due to a downstream service being temporarily unavailable. The team wants failed events to be retried and, after exhausting retries, stored for manual inspection. Which configuration achieves this?',
    opts: [
      'Set the function reserved concurrency to 0 to pause processing, then resume manually.',
      'Configure a Dead Letter Queue (DLQ) on the Lambda function pointing to an SQS queue, and set the maximum retry attempts to 2.',
      'Enable Lambda Destinations with an SQS queue as the on-failure destination.',
      'Wrap the function code in a try/catch and publish failures to SNS.',
    ],
    a: 2,
    exp: 'Lambda Destinations (on-failure) are the modern replacement for DLQs. When configured, Lambda sends the event along with error details to the destination (SQS, SNS, EventBridge, or another Lambda) after all retries are exhausted. DLQs only receive the payload without the context/error details.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'You need to share a 50 MB custom library across 10 Lambda functions without packaging it in every deployment ZIP. What is the most operationally efficient approach?',
    opts: [
      "Upload the library to S3 and download it at runtime in each function's init code.",
      'Create a Lambda Layer containing the library and attach it to each function.',
      'Use a shared EFS mount point and reference the library path in each function.',
      'Publish the library as a Lambda function and invoke it synchronously.',
    ],
    a: 1,
    exp: 'Lambda Layers let you package shared code/libraries separately and attach them to multiple functions. This reduces deployment package size and allows independent versioning of shared dependencies. EFS works but adds network latency and operational overhead.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A Lambda function configured with 1024 MB memory and a 30-second timeout is invoked synchronously from API Gateway. During load testing, requests fail with a 429 error after the reserved concurrency (50) is reached. What happens to the excess requests?',
    opts: [
      'They are queued internally by Lambda until capacity is available.',
      'They are throttled and API Gateway returns a 429 Too Many Requests response immediately.',
      'Lambda automatically scales beyond reserved concurrency using burst concurrency.',
      "The requests are routed to the function's DLQ.",
    ],
    a: 1,
    exp: 'Reserved concurrency acts as both a floor and a ceiling. When all reserved concurrent executions are in use, Lambda throttles new synchronous invocations immediately. API Gateway translates this Lambda throttle into a 429 response. Burst concurrency does not apply when reserved concurrency is set — it caps the function.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'Which of the following correctly describes the order of the Lambda execution environment lifecycle?',
    opts: [
      'Init → Invoke → Shutdown',
      'Invoke → Init → Shutdown',
      'Init → Shutdown → Invoke',
      'Invoke → Shutdown → Init',
    ],
    a: 0,
    exp: 'Lambda execution environments go through Init (download code, start runtime, run static initialisation), then Invoke (run the handler), and optionally Shutdown (runtime hooks for cleanup). Code outside the handler runs during Init and is reused across warm invocations.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer wants to test a new Lambda function version in production with only 10% of traffic before fully promoting it. What feature enables this?',
    opts: [
      'Lambda aliases with a weighted routing configuration between two versions.',
      'Lambda versions with a canary deployment policy.',
      'API Gateway stage variables pointing to different function versions.',
      'AWS CodeDeploy Linear10PercentEvery1Minute deployment configuration.',
    ],
    a: 0,
    exp: 'Lambda aliases support traffic shifting by assigning weights to two function versions. For example, an alias can route 90% to v1 and 10% to v2. This is the native Lambda mechanism for canary/blue-green deployments. CodeDeploy can automate the shift but it configures the alias weights under the hood.',
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: 'A Lambda function reads a database password from an environment variable. After rotating the password in Secrets Manager, the function still uses the old password. What is the most likely cause?',
    opts: [
      'Lambda caches environment variables at the account level for 24 hours.',
      'Warm execution environments retain the old environment variable value until the environment is recycled.',
      'Secrets Manager and Lambda environment variables are not integrated.',
      'The Lambda execution role lacks the secretsmanager:GetSecretValue permission.',
    ],
    a: 1,
    exp: 'Environment variables are baked into the execution environment at creation time. If you store a secret directly as an env var, rotating the secret in Secrets Manager does not update existing warm environments — you must redeploy the function or force new environments. The recommended pattern is to call Secrets Manager at runtime (with caching) rather than storing secrets as env vars.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer enables AWS X-Ray active tracing on a Lambda function. The function calls DynamoDB and an external HTTP API. Which components appear automatically in the service map without additional instrumentation?',
    opts: [
      'Only the Lambda function node itself.',
      'The Lambda function and DynamoDB, but not the external HTTP call.',
      'The Lambda function, DynamoDB, and the external HTTP endpoint — all automatically traced via the X-Ray SDK daemon.',
      'The Lambda function and DynamoDB when the X-Ray SDK is included and used to create subsegments.',
    ],
    a: 3,
    exp: "Enabling active tracing adds the Lambda and its invocation metadata automatically. AWS SDK calls (like DynamoDB) are automatically traced when the X-Ray SDK is included. Outbound HTTP calls require wrapping with `xray.captureHTTPsGlobal()` or similar. The X-Ray daemon runs alongside your function — you don't deploy it.",
  },
  {
    domain: 'Development with AWS Services',
    q: 'You configure a Lambda function URL. Which statement about Lambda function URLs is correct?',
    opts: [
      'Function URLs support IAM authentication or no authentication (NONE); Cognito is not supported.',
      'Function URLs require API Gateway to be configured as a proxy.',
      'Function URLs automatically apply reserved concurrency of 100.',
      'Function URLs are only available for Lambda functions in a VPC.',
    ],
    a: 0,
    exp: 'Lambda function URLs support two auth types: AWS_IAM (uses SigV4 signing) or NONE (publicly accessible). Cognito, Lambda authorizers, and API keys are not supported — those require API Gateway. Function URLs provide a dedicated HTTPS endpoint directly on the function.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A Lambda function processes records from an SQS queue. The batch size is 10 and a partial failure occurs (3 out of 10 messages fail). What must the function return to prevent all 10 messages from being retried?',
    opts: [
      'Throw an exception so Lambda retries only the failed messages automatically.',
      'Return a `batchItemFailures` list containing the `itemIdentifier` of each failed message.',
      'Delete the successful messages manually from SQS within the function.',
      'Set the SQS visibility timeout to 0 for failed messages inside the function.',
    ],
    a: 1,
    exp: "Lambda's partial batch response feature (ReportBatchItemFailures) lets a function return a JSON object with a `batchItemFailures` array listing the `messageId` (as `itemIdentifier`) of failed records. Lambda puts only those messages back for retry. Without this, any exception causes the entire batch to retry.",
  },
  {
    domain: 'Development with AWS Services',
    q: 'What is the purpose of Lambda Provisioned Concurrency?',
    opts: [
      'It reserves a portion of regional concurrency quota so other functions cannot use it.',
      'It pre-initialises a specified number of execution environments so they are ready to handle requests with no cold start latency.',
      'It limits the maximum number of concurrent invocations to prevent runaway scaling.',
      'It automatically retries failed invocations up to the provisioned count.',
    ],
    a: 1,
    exp: 'Provisioned Concurrency initializes the specified number of execution environments ahead of time, eliminating cold starts for those slots. This is distinct from reserved concurrency, which caps the maximum concurrency. Provisioned Concurrency is billed even when idle.',
  },

  // ── API Gateway ─────────────────────────────────────────────────────────────
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to build a REST API that requires request payload transformation before hitting a Lambda backend, and response payload transformation before returning to clients. Which API Gateway feature provides this without custom Lambda logic?',
    opts: [
      'API Gateway resource policies',
      'Mapping templates using Apache Velocity (VTL)',
      'API Gateway response caching',
      'Lambda proxy integration',
    ],
    a: 1,
    exp: 'Mapping templates (written in Velocity Template Language) are applied at the Integration Request and Integration Response stages in API Gateway REST APIs. They transform request/response payloads without requiring separate Lambda logic. Lambda proxy integration passes the raw request through, bypassing transformation.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'What is the key difference between an API Gateway HTTP API and a REST API?',
    opts: [
      'HTTP APIs support Lambda proxy integration; REST APIs do not.',
      'HTTP APIs are lower latency and cost but lack features like API keys, usage plans, request validation, and WAF integration found in REST APIs.',
      'REST APIs support WebSocket connections; HTTP APIs do not.',
      'HTTP APIs automatically handle CORS; REST APIs require manual configuration in all cases.',
    ],
    a: 1,
    exp: 'HTTP APIs (v2) are up to 71% cheaper and have lower latency than REST APIs (v1). However, REST APIs offer features HTTP APIs lack: API keys, usage plans, per-client throttling, AWS WAF integration, request validation, and mapping templates. HTTP APIs do have built-in CORS and JWT authorizer support.',
  },
  {
    domain: 'Security',
    q: 'A REST API in API Gateway must only accept requests from authenticated users. The team uses Amazon Cognito. Which authorizer type is simplest to configure?',
    opts: [
      'Lambda authorizer (TOKEN type) that calls Cognito to validate tokens.',
      'Cognito User Pool authorizer attached to the API method.',
      'IAM authorization with Cognito Identity Pool credentials.',
      'API key combined with a usage plan.',
    ],
    a: 1,
    exp: 'API Gateway has native Cognito User Pool authorizer support. You configure it by pointing to the User Pool ARN and specifying the token source (e.g., Authorization header). API Gateway validates the JWT automatically. A Lambda authorizer can do the same but requires writing and maintaining code.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'API Gateway throttling is configured with a default rate of 1000 req/s and a burst of 2000. A client sends 5000 requests in one second. What happens?',
    opts: [
      'All 5000 requests are processed because Lambda scales automatically.',
      'The first 2000 requests are served (burst), then up to 1000/s afterward; excess requests receive a 429 response.',
      'Requests are queued until capacity is available; none are dropped.',
      'API Gateway returns a 503 Service Unavailable for all excess requests.',
    ],
    a: 1,
    exp: 'API Gateway uses a token bucket algorithm. The burst size determines how many requests can be served instantaneously (the bucket size), and the rate determines the refill speed. Requests exceeding both burst and sustained rate receive a 429 Too Many Requests response.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer enables API Gateway response caching on a stage with a TTL of 300 seconds. A client needs to bypass the cache for a specific request. What must the client include?',
    opts: [
      'A `Cache-Control: max-age=0` request header — provided the API is configured to allow cache invalidation.',
      'A query string parameter `?nocache=true`.',
      'An `X-Bypass-Cache: true` custom header.',
      'Cache bypass is not possible at the per-request level; the TTL must be changed on the stage.',
    ],
    a: 0,
    exp: 'Clients can invalidate the API Gateway cache for a specific request by sending the `Cache-Control: max-age=0` header. The API must be configured to allow cache invalidation (either requiring IAM authorization for it or allowing it for all callers). This is the standard mechanism.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'Which API Gateway integration type lets you expose an AWS service action (e.g., SQS SendMessage) directly without a Lambda intermediary?',
    opts: [
      'LAMBDA_PROXY',
      'HTTP_PROXY',
      'AWS (non-proxy)',
      'MOCK',
    ],
    a: 2,
    exp: 'The AWS integration type (non-proxy) lets API Gateway call AWS service APIs directly using mapping templates to construct the request. For example, you can have API Gateway call SQS:SendMessage, DynamoDB:PutItem, etc. LAMBDA_PROXY and HTTP_PROXY pass requests through to Lambda or HTTP endpoints respectively.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A web app hosted on `https://app.example.com` calls an API Gateway endpoint at `https://api.example.com`. The browser blocks the calls. What must be configured on API Gateway?',
    opts: [
      'An API Gateway resource policy allowing the web app origin.',
      'Cross-Origin Resource Sharing (CORS) with the appropriate allowed origins and methods.',
      'A custom domain name matching `app.example.com`.',
      'A Lambda authorizer that sets the Access-Control-Allow-Origin header.',
    ],
    a: 1,
    exp: 'Browsers enforce the Same-Origin Policy. Cross-origin requests require the server to include CORS headers (Access-Control-Allow-Origin, Access-Control-Allow-Methods, etc.). API Gateway has built-in CORS configuration (HTTP APIs) or requires OPTIONS method with correct headers (REST APIs). Resource policies control AWS-level access, not browser CORS.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'An API Gateway stage uses a Lambda function alias. A developer creates a new Lambda version and wants to shift 20% of production traffic to it for testing. What is the correct approach?',
    opts: [
      'Create a new API Gateway stage pointing to the new Lambda version.',
      'Update the Lambda alias to route 80% to the current version and 20% to the new version using alias routing configuration.',
      'Use API Gateway canary release deployment to split traffic at the stage level.',
      'Deploy a second API Gateway API with the new Lambda version.',
    ],
    a: 1,
    exp: 'Lambda alias traffic shifting (weighted routing) allows a single alias to distribute invocations across two function versions. The API Gateway stage points to the alias, so no API Gateway changes are needed. API Gateway canary releases split traffic between two stage configurations but do not provide Lambda version control.',
  },

  // ── DynamoDB ────────────────────────────────────────────────────────────────
  {
    domain: 'Development with AWS Services',
    q: 'A DynamoDB table has a partition key of `userId` and sort key of `timestamp`. A developer wants all items for `userId = "user123"` where `timestamp` is between `2024-01-01` and `2024-12-31`. Which operation is correct?',
    opts: [
      'Scan with a FilterExpression on userId and timestamp.',
      'GetItem with the userId and a timestamp range.',
      'Query with KeyConditionExpression `userId = :uid AND #ts BETWEEN :start AND :end`.',
      'BatchGetItem with all userId+timestamp combinations.',
    ],
    a: 2,
    exp: 'Query is the correct operation — it uses the partition key equality condition plus an optional sort key condition. BETWEEN is a valid sort key condition. Scan reads every item and filters, which is expensive. GetItem requires the full primary key. BatchGetItem requires exact keys.',
  },
  {
    domain: 'Development with AWS Services',
    q: "A developer uses the following Python code:\n```python\ntable.put_item(\n  Item={'userId': 'u1', 'email': 'a@b.com'},\n  ConditionExpression='attribute_not_exists(userId)'\n)\n```\nWhat is the purpose of the ConditionExpression?",
    opts: [
      'It validates the email format before writing.',
      'It prevents overwriting an existing item with the same partition key.',
      'It checks that the table has capacity before writing.',
      'It ensures the write is strongly consistent.',
    ],
    a: 1,
    exp: '`attribute_not_exists(userId)` makes the PutItem conditional — it only succeeds if no item with that partition key (userId) already exists. If an item exists, DynamoDB raises a ConditionalCheckFailedException. This implements optimistic locking / unique constraint behavior.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'What is the maximum item size in DynamoDB?',
    opts: ['64 KB', '256 KB', '400 KB', '1 MB'],
    a: 2,
    exp: 'DynamoDB items have a maximum size of 400 KB, including all attribute names and values. For larger objects, a common pattern is to store the large payload in S3 and store only the S3 reference in DynamoDB.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A DynamoDB table has TTL enabled on the `expiresAt` attribute. An item has `expiresAt = 1700000000` (Unix epoch). What happens after that timestamp?',
    opts: [
      'DynamoDB immediately deletes the item at exactly that timestamp.',
      'DynamoDB marks the item for deletion and removes it within a few minutes to 48 hours, at no cost.',
      'DynamoDB stops returning the item in reads immediately but deletes it after 7 days.',
      'The item is moved to a DynamoDB archive table.',
    ],
    a: 1,
    exp: 'TTL deletion is eventual — DynamoDB scans for expired items and deletes them typically within 48 hours of expiry, at no additional cost. Expired but not yet deleted items may still appear in reads (you can filter them with a FilterExpression comparing to current time). Items deleted by TTL also appear in DynamoDB Streams with a system event marker.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to write 500 items to DynamoDB as efficiently as possible. Which API call should be used?',
    opts: [
      'PutItem called 500 times in parallel threads.',
      'BatchWriteItem with up to 25 items per call.',
      'TransactWriteItems with all 500 items in one call.',
      'UpdateItem with a batch flag enabled.',
    ],
    a: 1,
    exp: 'BatchWriteItem allows up to 25 PutRequest or DeleteRequest operations per call, reducing API call overhead. For 500 items you make 20 batched calls. TransactWriteItems is for ACID transactions (max 100 items) and has higher cost. PutItem in parallel works but is less efficient.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'What is DynamoDB DAX and when should it be used?',
    opts: [
      'A DynamoDB audit extension that logs all API calls to CloudTrail.',
      'An in-memory caching cluster compatible with the DynamoDB API that reduces read latency to microseconds for eventually consistent reads.',
      'A DynamoDB feature that enables cross-region active-active replication.',
      'A data archival feature that moves cold data to S3 Glacier.',
    ],
    a: 1,
    exp: "DAX (DynamoDB Accelerator) is a fully managed, highly available in-memory cache for DynamoDB. It's API-compatible (minimal code changes) and provides microsecond latency for cached reads. It's ideal for read-heavy workloads with repeated reads of the same items. It does not help write-heavy workloads or strongly consistent reads.",
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer wants to react to every new item inserted into a DynamoDB table by triggering a Lambda function. Which feature enables this?',
    opts: [
      'DynamoDB point-in-time recovery (PITR) exports.',
      'DynamoDB Streams with a Lambda event source mapping.',
      'DynamoDB Kinesis Data Streams integration.',
      'DynamoDB TTL with a Lambda expiry hook.',
    ],
    a: 1,
    exp: 'DynamoDB Streams captures a time-ordered sequence of item-level changes. You can configure a Lambda event source mapping to poll the stream and invoke the function with batches of records. Kinesis integration is an alternative but uses a different consumer model. PITR is for backup/restore.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'In DynamoDB, what is the difference between a GSI (Global Secondary Index) and an LSI (Local Secondary Index)?',
    opts: [
      "GSIs can have a different partition key than the table; LSIs share the table's partition key but can have a different sort key.",
      "LSIs can have a different partition key; GSIs share the table's partition key.",
      'GSIs support strongly consistent reads; LSIs only support eventually consistent reads.',
      'LSIs are created after table creation; GSIs must be created at table creation time.',
    ],
    a: 0,
    exp: "A GSI can have any attribute as its partition key and sort key, enabling queries on non-primary-key attributes. An LSI must share the table's partition key but can use a different sort key — useful for different sort orders within a partition. LSIs must be created at table creation; GSIs can be added later.",
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: "A DynamoDB application receives frequent `ProvisionedThroughputExceededException` errors on reads even though consumed capacity is well below the provisioned limit. What is the most likely cause?",
    opts: [
      'The table does not have DAX enabled.',
      "Hot partitions — most read traffic is concentrated on a few partition key values, exhausting those partitions' capacity.",
      'The read requests are using strongly consistent reads, which consume double the RCUs.',
      'The DynamoDB table has TTL enabled, consuming extra RCUs.',
    ],
    a: 1,
    exp: 'DynamoDB distributes provisioned capacity across partitions. If traffic is unevenly distributed (hot partitions), individual partitions can be throttled even when the table-level consumed capacity appears low. Solutions include better key design, adding randomness to keys, or using on-demand capacity mode.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to atomically increment a numeric attribute `viewCount` in a DynamoDB item without reading the current value. Which operation achieves this?',
    opts: [
      'GetItem followed by PutItem in a transaction.',
      'UpdateItem with an UpdateExpression of `SET viewCount = viewCount + :inc`.',
      'PutItem with the new viewCount value using a ConditionExpression.',
      'BatchWriteItem with the updated viewCount.',
    ],
    a: 1,
    exp: 'UpdateItem with `SET viewCount = viewCount + :inc` performs an atomic increment. DynamoDB handles the read-modify-write internally, avoiding race conditions. This is more efficient than a read-then-write pattern and does not require a transaction.',
  },

  // ── S3 ──────────────────────────────────────────────────────────────────────
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to allow a user to upload a file directly to S3 from a browser without exposing AWS credentials. What is the recommended approach?',
    opts: [
      'Embed IAM access keys in the JavaScript bundle.',
      'Generate a pre-signed URL server-side and return it to the client for direct upload.',
      'Configure S3 public write access on the bucket.',
      'Use an API Gateway proxy that streams the upload to S3.',
    ],
    a: 1,
    exp: 'Pre-signed URLs encode temporary, scoped S3 operation permissions. The server generates a pre-signed PUT URL (using the AWS SDK with appropriate IAM permissions) and returns it to the client. The client uploads directly to S3 using that URL without needing AWS credentials. The URL expires after a configurable duration.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A file being uploaded to S3 is 1.2 GB. The upload fails at 800 MB due to a network error. What feature minimises the amount of data that must be re-uploaded?',
    opts: [
      'S3 Transfer Acceleration',
      'S3 Multipart Upload',
      'S3 Intelligent-Tiering',
      'S3 Cross-Region Replication',
    ],
    a: 1,
    exp: 'Multipart Upload splits large objects into parts (5 MB–5 GB each). If a part fails, only that part needs to be re-uploaded. AWS recommends multipart upload for objects over 100 MB and requires it for objects over 5 GB. S3 Transfer Acceleration speeds uploads via CloudFront edge but does not provide failure recovery at the part level.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer wants to query specific columns from a large CSV file stored in S3 without downloading the entire object. Which S3 feature enables this?',
    opts: [
      'S3 Batch Operations',
      'S3 Select',
      'S3 Inventory',
      'S3 Object Lambda',
    ],
    a: 1,
    exp: 'S3 Select allows you to use SQL expressions to retrieve a subset of data from an S3 object (CSV, JSON, or Parquet). Only the matching data is returned, reducing data transfer and processing time. S3 Object Lambda lets you transform data on the fly but requires a Lambda function.',
  },
  {
    domain: 'Development with AWS Services',
    q: "An application needs to trigger a Lambda function whenever a new object is created in an S3 bucket under the `uploads/` prefix. How should this be configured?",
    opts: [
      "Configure an S3 Event Notification on the bucket with a prefix filter for `uploads/` and an event type of `s3:ObjectCreated:*`, with Lambda as the destination.",
      'Create a CloudWatch Events rule that polls S3 for new objects every minute.',
      'Enable S3 versioning and configure a Lambda trigger on version creation.',
      'Use an SQS queue with S3 as the producer and Lambda as the consumer.',
    ],
    a: 0,
    exp: 'S3 Event Notifications support Lambda, SQS, and SNS as destinations. You configure the event type (e.g., s3:ObjectCreated:*) and optional prefix/suffix filters. Lambda must have a resource-based policy allowing S3 to invoke it. This is the simplest native integration for this use case.',
  },
  {
    domain: 'Security',
    q: "A developer generates a pre-signed URL for an S3 object using an IAM role. The URL is valid for 7 days. After 6 hours, the IAM role's permissions are revoked. What happens when the URL is accessed on day 2?",
    opts: [
      "The request succeeds because the URL was already signed with the role's credentials.",
      "The request fails because pre-signed URL validity depends on the signing credentials being valid at the time of access.",
      'The request succeeds for read operations but fails for write operations.',
      'The request fails only if the URL was generated with temporary credentials.',
    ],
    a: 1,
    exp: "Pre-signed URLs are validated at access time against the signer's current permissions. If the IAM role is revoked or the temporary credentials expire before the URL's expiry, the URL becomes invalid. This is an important security consideration — especially for URLs signed with STS temporary credentials where the session itself may expire sooner.",
  },
  {
    domain: 'Development with AWS Services',
    q: 'Which S3 consistency model applies after a successful PutObject or DeleteObject?',
    opts: [
      'Eventually consistent — changes propagate within 1 minute.',
      'Read-after-write consistent for new objects; eventually consistent for overwrites and deletes.',
      'Strongly consistent for all operations including new objects, overwrites, and deletes.',
      'Eventually consistent — all S3 operations are eventual.',
    ],
    a: 2,
    exp: 'Since December 2020, Amazon S3 provides strong read-after-write consistency for all operations — PutObject, DeleteObject, and overwrites. A successful write is immediately visible to all subsequent reads. This applies to all S3 regions and storage classes.',
  },

  // ── SQS ─────────────────────────────────────────────────────────────────────
  {
    domain: 'Development with AWS Services',
    q: 'A Lambda function polls an SQS queue and takes 45 seconds to process each message. The SQS visibility timeout is 30 seconds. What will happen?',
    opts: [
      'Lambda extends the visibility timeout automatically to match the function timeout.',
      'The message becomes visible again in the queue before processing completes, causing duplicate processing.',
      'SQS sends the message to the DLQ after 30 seconds.',
      'Lambda pauses processing and waits for the visibility timeout to reset.',
    ],
    a: 1,
    exp: 'The visibility timeout hides a message from other consumers while it is being processed. If processing takes longer than the visibility timeout, the message becomes visible again and can be picked up by another consumer (or the same one), causing duplicates. Best practice: set the visibility timeout to at least 6× the Lambda function timeout.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to ensure that related messages (e.g., all events for the same order) are processed in order by a single consumer. Which SQS feature provides this?',
    opts: [
      'Standard queue with a MessageGroupId attribute.',
      'FIFO queue with MessageGroupId set to the order ID.',
      'Standard queue with a SequenceNumber attribute.',
      'FIFO queue with the ContentBasedDeduplication feature.',
    ],
    a: 1,
    exp: 'SQS FIFO queues provide exactly-once, ordered processing per MessageGroupId. Messages with the same MessageGroupId are always processed in order and by a single consumer at a time. Standard queues offer at-least-once, best-effort ordering with no MessageGroupId concept.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'What is the maximum retention period for messages in an SQS queue?',
    opts: ['1 day', '4 days (default)', '14 days', '30 days'],
    a: 2,
    exp: 'SQS supports a message retention period of 60 seconds to 14 days. The default is 4 days. Messages older than the retention period are automatically deleted.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A consumer application calls ReceiveMessage on an SQS queue but receives no messages even though the queue has 5000 items. The application polls every second. What change would reduce empty responses and API costs?',
    opts: [
      'Increase the SQS queue visibility timeout.',
      'Enable long polling by setting WaitTimeSeconds to up to 20 seconds.',
      'Switch from a standard queue to a FIFO queue.',
      'Increase the SQS message size limit.',
    ],
    a: 1,
    exp: 'Short polling (default, WaitTimeSeconds=0) samples only a subset of servers and returns immediately, often with empty responses even when messages exist. Long polling (WaitTimeSeconds=1-20) queries all servers and waits for messages to arrive, reducing empty responses and lowering costs.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'An SQS queue is configured with a DLQ. A message fails processing and is moved to the DLQ after 3 delivery attempts. What is the attribute that controls how many times a message is tried before DLQ redirection?',
    opts: [
      'VisibilityTimeout',
      'MaxReceiveCount in the Redrive Policy',
      'MessageRetentionPeriod',
      'DelaySeconds',
    ],
    a: 1,
    exp: "The Redrive Policy on an SQS queue specifies the `deadLetterTargetArn` and `maxReceiveCount`. When a message's `ApproximateReceiveCount` exceeds `maxReceiveCount`, SQS moves it to the DLQ. The visibility timeout and retention period are independent settings.",
  },
  {
    domain: 'Development with AWS Services',
    q: 'Which SQS queue type guarantees exactly-once message delivery?',
    opts: [
      'Standard queue with deduplication enabled.',
      'FIFO queue with content-based deduplication or a MessageDeduplicationId.',
      'Standard queue with a DLQ configured.',
      'FIFO queue with a VisibilityTimeout of 0.',
    ],
    a: 1,
    exp: 'SQS FIFO queues provide exactly-once processing within a 5-minute deduplication window. Deduplication can be content-based (SHA-256 hash of body) or explicit (MessageDeduplicationId). Standard queues provide at-least-once delivery (duplicates possible).',
  },

  // ── SNS ─────────────────────────────────────────────────────────────────────
  {
    domain: 'Development with AWS Services',
    q: 'A developer wants a single S3 event to trigger both a Lambda function for processing and an SQS queue for auditing. What SNS pattern enables this?',
    opts: [
      'Configure two separate S3 event notifications, one for Lambda and one for SQS.',
      'Use the SNS fan-out pattern: S3 sends events to an SNS topic, which has Lambda and SQS as subscribers.',
      'Configure Lambda to forward events to SQS after processing.',
      'Use EventBridge with multiple targets on a single rule.',
    ],
    a: 1,
    exp: 'The SNS fan-out pattern allows a single message published to an SNS topic to be delivered to multiple subscribers simultaneously (Lambda, SQS, HTTP, email, etc.). S3 event notifications can target SNS, enabling fan-out to multiple downstream consumers. EventBridge is also a valid modern alternative.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'An SNS topic delivers messages to multiple SQS queues. A developer wants QueueA to receive only messages with `{"type": "order"}` and QueueB to receive only messages with `{"type": "inventory"}`. What feature enables this?',
    opts: [
      'SNS message routing policies',
      'SNS subscription filter policies',
      'SQS message attribute filters',
      'SNS topic policies',
    ],
    a: 1,
    exp: 'SNS subscription filter policies allow each subscriber to specify a JSON policy that filters messages based on message attributes. Only messages matching the filter are delivered to that subscriber. This enables selective fan-out without routing logic in the publisher.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'What is the maximum message size for an SNS notification?',
    opts: ['64 KB', '256 KB', '1 MB', '10 MB'],
    a: 1,
    exp: 'SNS supports message sizes up to 256 KB. For larger payloads, the recommended pattern is the SNS Extended Client Library (Java) or a manual pattern: store the payload in S3 and send only the S3 reference in the SNS message.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A mobile app uses SNS to send push notifications to iOS (APNs) and Android (FCM) devices. The app uses a single SNS topic. What is the recommended architecture?',
    opts: [
      'Publish directly to platform-specific ARNs using the SNS Publish API with platform application endpoints.',
      'Create separate topics for iOS and Android and publish to each.',
      'Use SNS mobile push: register each device to get an endpoint ARN, then publish to the endpoint ARN or to a topic with all endpoints subscribed.',
      'Use SES for mobile push notifications instead of SNS.',
    ],
    a: 2,
    exp: 'SNS Mobile Push: you create Platform Applications for APNs/FCM, register each device to get a unique endpoint ARN, and then publish either to the endpoint ARN directly or subscribe endpoints to a topic for broadcast. Publishing to the endpoint ARN lets SNS translate the SNS message into the platform-native format.',
  },

  // ── Kinesis ─────────────────────────────────────────────────────────────────
  {
    domain: 'Development with AWS Services',
    q: 'A Kinesis Data Stream has 4 shards. What is the maximum write throughput?',
    opts: [
      '4 MB/s and 4,000 records/s',
      '4 MB/s and 400 records/s',
      '40 MB/s and 40,000 records/s',
      '1 MB/s and 1,000 records/s per shard (4 MB/s and 4,000 records/s total)',
    ],
    a: 3,
    exp: 'Each Kinesis shard supports 1 MB/s or 1,000 records/s for writes. With 4 shards, the total write capacity is 4 MB/s and 4,000 records/s. Read capacity is 2 MB/s per shard per consumer (shared) or 2 MB/s per shard per Enhanced Fan-Out consumer.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to preserve the order of click-stream events for each user in a Kinesis Data Stream. What should be used as the partition key?',
    opts: [
      'A random UUID for each record to distribute load evenly.',
      'The user ID, so all records for a user go to the same shard.',
      'The event timestamp.',
      'The S3 bucket name where events are stored.',
    ],
    a: 1,
    exp: 'Kinesis guarantees order within a shard. By using the user ID as the partition key, all records for a user are hashed to the same shard and processed in order. A random UUID distributes evenly but breaks per-user ordering.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'What is the default data retention period for a Kinesis Data Stream, and what is the maximum?',
    opts: [
      'Default: 24 hours; Maximum: 7 days',
      'Default: 24 hours; Maximum: 365 days',
      'Default: 7 days; Maximum: 365 days',
      'Default: 1 hour; Maximum: 24 hours',
    ],
    a: 1,
    exp: 'Kinesis Data Streams default retention is 24 hours. It can be extended to up to 365 days (with additional cost). This is useful for replay and reprocessing scenarios.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'What is the key difference between Kinesis Data Streams and Kinesis Data Firehose?',
    opts: [
      'Firehose supports custom consumers; Data Streams can only deliver to S3.',
      'Data Streams is for real-time custom processing with multiple consumers; Firehose is a fully managed delivery service to destinations like S3, Redshift, and OpenSearch with built-in transformation.',
      'Firehose provides millisecond latency; Data Streams has 60-second minimum latency.',
      'Data Streams supports SQL queries; Firehose does not.',
    ],
    a: 1,
    exp: 'Kinesis Data Streams is a raw streaming service where you write custom consumers (KCL, Lambda) and control processing. Kinesis Data Firehose is fully managed: it buffers, optionally transforms (via Lambda), and delivers data to S3, Redshift, OpenSearch, Splunk, or HTTP endpoints. Firehose has near-real-time (60s buffer) latency vs. millisecond for Data Streams.',
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: 'A Kinesis consumer processes records but repeatedly sees the same records. The consumer is using the KCL. What is the most likely cause?',
    opts: [
      'The stream has too many shards for the number of consumers.',
      'The consumer is not checkpointing successfully, so after restart it reprocesses records from the last checkpoint.',
      'Kinesis Data Streams does not support exactly-once processing and duplicates are expected.',
      'The partition key is not set, causing records to replay.',
    ],
    a: 1,
    exp: 'KCL uses DynamoDB to store checkpoints (the sequence number of the last successfully processed record). If checkpointing fails or is skipped, the consumer re-reads from the last checkpoint on restart, causing duplicates. The fix is to ensure checkpoint() is called after successful processing.',
  },

  // ── CI/CD ────────────────────────────────────────────────────────────────────
  {
    domain: 'Deployment',
    q: 'A team uses AWS CodePipeline with CodeBuild for build and CodeDeploy for deployment. The build stage must run unit tests and fail the pipeline if any test fails. How should this be configured in the buildspec.yml?',
    opts: [
      'Put test commands in the `install` phase; failures in install stop the build.',
      'Put test commands in the `post_build` phase; set `on-failure: ABORT`.',
      'Put test commands in the `build` phase; a non-zero exit code from any command fails the build and stops the pipeline.',
      'Use a separate CodePipeline action of type `Test` with the test commands.',
    ],
    a: 2,
    exp: 'In buildspec.yml, commands in any phase (install, pre_build, build, post_build) that return a non-zero exit code fail the build. The `build` phase is the conventional place for compilation and tests. A failed CodeBuild action stops the pipeline from proceeding to deployment.',
  },
  {
    domain: 'Deployment',
    q: 'A CodeDeploy deployment to EC2 is using the `CodeDeployDefault.OneAtATime` deployment configuration. What does this mean?',
    opts: [
      'One deployment group is updated at a time across all regions.',
      'Instances are updated one at a time; the deployment stops if any instance fails.',
      'The first instance is updated, and if successful, the rest are updated in parallel.',
      'One percent of instances are updated per minute.',
    ],
    a: 1,
    exp: 'CodeDeployDefault.OneAtATime deploys to one instance at a time. If any instance fails deployment, the overall deployment fails (the remaining instances are not updated). Other built-in configs include AllAtOnce (fastest, least safe) and HalfAtATime.',
  },
  {
    domain: 'Deployment',
    q: 'A developer wants to deploy a new Lambda version with automatic rollback if error rates exceed 5% within 10 minutes. Which CodeDeploy deployment configuration supports this?',
    opts: [
      'CodeDeployDefault.LambdaAllAtOnce',
      'CodeDeployDefault.LambdaLinear10PercentEvery1Minute',
      'CodeDeployDefault.LambdaCanary10Percent5Minutes with CloudWatch alarms for rollback.',
      'CodeDeployDefault.LambdaCanary10Percent5Minutes is not valid; use Linear.',
    ],
    a: 2,
    exp: 'CodeDeploy Lambda deployment configurations support Canary, Linear, and AllAtOnce. Canary10Percent5Minutes shifts 10% of traffic initially, waits 5 minutes, then shifts the rest. By configuring CloudWatch alarms on the deployment group, CodeDeploy automatically rolls back if alarms trigger during the bake time.',
  },
  {
    domain: 'Deployment',
    q: 'Where should application artifacts produced by CodeBuild be stored for use by subsequent CodePipeline stages?',
    opts: [
      "In an EC2 instance's local filesystem.",
      "In an S3 bucket configured as the pipeline's artifact store.",
      'In a CodeCommit repository as a new commit.',
      'In DynamoDB as binary data.',
    ],
    a: 1,
    exp: "CodePipeline uses S3 as its artifact store. CodeBuild output artifacts are stored in S3, and subsequent stages (e.g., CodeDeploy) pull from S3. The artifact store bucket is configured when creating the pipeline.",
  },
  {
    domain: 'Deployment',
    q: 'A developer pushes a commit to a CodeCommit branch and wants CodePipeline to start automatically. How is this configured?',
    opts: [
      'CodePipeline polls CodeCommit every 5 minutes by default.',
      "Configure a CodeCommit trigger that calls CodePipeline's StartPipelineExecution API.",
      'Use an Amazon EventBridge rule that triggers on CodeCommit repository state change events and starts the pipeline.',
      'Both B and C are valid, but C (EventBridge) is the recommended approach.',
    ],
    a: 3,
    exp: 'CodePipeline supports two detection methods for CodeCommit sources: polling (legacy) and Amazon EventBridge (recommended). EventBridge provides near-instant detection of branch pushes and is the current best practice. Polling checks every minute and adds latency.',
  },
  {
    domain: 'Deployment',
    q: 'A CodeDeploy blue/green deployment for ECS is in progress. During the test traffic phase, the new task set is found to have a bug. What should the developer do?',
    opts: [
      'Manually update the ECS service to roll back the task definition.',
      'Stop the CodeDeploy deployment — CodeDeploy rolls back to the original (blue) task set automatically.',
      'Delete the new (green) task set from ECS and create a new one.',
      'Terminate the green EC2 instances and re-deploy.',
    ],
    a: 1,
    exp: 'In CodeDeploy blue/green for ECS, the original (blue) task set is maintained during the deployment window. If you stop/rollback the deployment, CodeDeploy shifts traffic back to the blue task set and terminates the green tasks. This is the key advantage of blue/green: instant rollback.',
  },

  // ── CloudFormation & SAM ─────────────────────────────────────────────────────
  {
    domain: 'Deployment',
    q: 'A CloudFormation template creates an S3 bucket and a Lambda function. The Lambda needs the bucket name as an environment variable. Which intrinsic function retrieves the bucket name after it is created?',
    opts: [
      '`!Ref MyBucket` — returns the bucket name for S3 buckets.',
      '`!GetAtt MyBucket.BucketName`',
      '`!Sub "${MyBucket.BucketName}"`',
      '`!ImportValue MyBucket`',
    ],
    a: 0,
    exp: 'For S3 buckets, `!Ref` returns the bucket name. `!GetAtt` is used for other attributes (e.g., `MyBucket.Arn` for the ARN). `!ImportValue` is for cross-stack references via Outputs/Exports. `!Sub` substitutes variables but still needs `!Ref` to get the name.',
  },
  {
    domain: 'Deployment',
    q: 'A CloudFormation stack update would delete and recreate a production RDS instance, causing data loss. How can a developer prevent this?',
    opts: [
      'Set the `DeletionPolicy: Retain` attribute on the RDS resource.',
      'Add an `UpdatePolicy` with `AutoScalingReplacingUpdate`.',
      'Use a `DependsOn` attribute to order the update after a backup.',
      'Set `UpdateReplacePolicy: Retain` on the RDS resource.',
    ],
    a: 3,
    exp: '`UpdateReplacePolicy` controls what happens to the old resource when CloudFormation replaces it during an update. `Retain` keeps the old resource (does not delete it). `DeletionPolicy: Retain` prevents deletion when the stack is deleted. Both can be set together for full protection.',
  },
  {
    domain: 'Deployment',
    q: 'A SAM template includes `Transform: AWS::Serverless-2016-10-31`. What does this line do?',
    opts: [
      'It imports the SAM CLI tool version.',
      'It tells CloudFormation to pass the template through the SAM transform (a CloudFormation macro) to expand SAM shorthand into standard CloudFormation resources.',
      'It enables CloudFormation drift detection.',
      'It specifies the AWS region for the deployment.',
    ],
    a: 1,
    exp: 'The `Transform` directive specifies a CloudFormation macro. `AWS::Serverless-2016-10-31` is the SAM transform macro. When CloudFormation processes the template, it sends it through the macro which expands SAM resource types (AWS::Serverless::Function, AWS::Serverless::Api, etc.) into standard CloudFormation resources.',
  },
  {
    domain: 'Deployment',
    q: 'A developer uses `sam local invoke` to test a Lambda function locally. What does this command do?',
    opts: [
      'It deploys the function to a Lambda development environment in the AWS account.',
      'It runs the Lambda function in a Docker container that emulates the Lambda execution environment locally.',
      'It uploads the function code to S3 for local testing via S3 Select.',
      'It generates test events from the Lambda console.',
    ],
    a: 1,
    exp: '`sam local invoke` uses Docker to run the Lambda function locally in a container that mirrors the Lambda execution environment (same runtime, same `/tmp` filesystem, same environment variables from env-vars.json or template). It does not deploy to AWS.',
  },
  {
    domain: 'Deployment',
    q: 'What does `Fn::GetAtt` do in a CloudFormation template?',
    opts: [
      'It retrieves a parameter value from the Parameters section.',
      'It returns the value of an attribute of a resource in the same template.',
      "It imports a value from another CloudFormation stack's Outputs.",
      'It retrieves a value from AWS Systems Manager Parameter Store.',
    ],
    a: 1,
    exp: '`Fn::GetAtt` (or `!GetAtt`) retrieves specific attributes of resources within the same template. For example, `!GetAtt MyLambda.Arn` returns the ARN of a Lambda function. Each resource type documents its available GetAtt attributes.',
  },
  {
    domain: 'Deployment',
    q: 'A CloudFormation template needs to use a different AMI ID depending on the deployment region. Which feature is best suited for this?',
    opts: [
      'Parameters with a default value that users override per region.',
      'Mappings section with a region-to-AMI map, accessed using `!FindInMap`.',
      'Outputs section that exports the AMI ID.',
      'Conditions section to select AMI based on a parameter.',
    ],
    a: 1,
    exp: 'CloudFormation Mappings are key-value maps. A common pattern is a RegionMap that maps each region to its AMI ID. `!FindInMap [RegionMap, !Ref AWS::Region, AMI]` retrieves the correct AMI for the current region without requiring user input.',
  },
  {
    domain: 'Deployment',
    q: 'A SAM application has a Lambda function that should be invoked on a schedule every 5 minutes. How is this defined in the SAM template?',
    opts: [
      'Add an EventBridge rule resource and use `DependsOn` to link it to the function.',
      'Add an `Events` property to the `AWS::Serverless::Function` with type `Schedule` and `Schedule: rate(5 minutes)`.',
      'Configure a CloudWatch alarm with a period of 300 seconds.',
      'Use a Step Functions state machine with a Wait state.',
    ],
    a: 1,
    exp: 'SAM supports event source definitions directly on the function. The `Schedule` event type creates an EventBridge (CloudWatch Events) rule. Using `rate(5 minutes)` or a cron expression, SAM automatically creates both the rule and the necessary Lambda permissions.',
  },

  // ── Elastic Beanstalk ────────────────────────────────────────────────────────
  {
    domain: 'Deployment',
    q: 'An Elastic Beanstalk environment is running 10 instances. A developer deploys a new version using the `Rolling` deployment policy with a batch size of 30%. What happens during deployment?',
    opts: [
      'All 10 instances are updated simultaneously.',
      'A new set of instances is launched, updated, and then replaces the old instances.',
      'Approximately 3 instances at a time are taken out of service, updated, and returned to service, cycling until all instances are updated.',
      'One instance is updated first; if healthy, the rest are updated all at once.',
    ],
    a: 2,
    exp: 'Rolling deployment updates instances in batches (here 30% ≈ 3 instances). Each batch is taken out of the load balancer, updated, and returned before the next batch starts. This causes brief capacity reduction but avoids downtime. Rolling with additional batch launches a new batch first to maintain capacity.',
  },
  {
    domain: 'Deployment',
    q: 'A developer needs to install a custom RPM package on all Elastic Beanstalk EC2 instances during environment creation. Where should this configuration be placed?',
    opts: [
      'In the `Procfile` at the root of the application bundle.',
      'In `.ebextensions/*.config` YAML/JSON files in the application bundle.',
      "In the Elastic Beanstalk environment's user data script via the console.",
      'In a `buildspec.yml` file in the application bundle.',
    ],
    a: 1,
    exp: '`.ebextensions` configuration files (*.config) allow you to customize and configure Elastic Beanstalk environments. They can install packages, create files, run commands, configure services, and set environment properties. They are processed during environment creation and updates.',
  },
  {
    domain: 'Deployment',
    q: 'Which Elastic Beanstalk deployment policy introduces ZERO downtime and maintains FULL capacity during deployment, but requires double the resources temporarily?',
    opts: [
      'Rolling',
      'Rolling with additional batch',
      'Immutable',
      'Blue/Green (swap URLs)',
    ],
    a: 2,
    exp: 'Immutable deployment launches a completely new set of instances with the new version in a new Auto Scaling group. Only after health checks pass are the new instances moved to the original ASG and old instances terminated. Full capacity is maintained throughout. Blue/Green is a separate environment swap, not a built-in Beanstalk deployment policy.',
  },
  {
    domain: 'Deployment',
    q: 'An Elastic Beanstalk worker environment is configured to process long-running jobs. Where does it read jobs from?',
    opts: [
      'An S3 bucket configured as the worker data source.',
      'An SQS queue that Elastic Beanstalk automatically creates and manages.',
      'A DynamoDB table with a stream trigger.',
      'An SNS topic subscribed to the web tier.',
    ],
    a: 1,
    exp: 'Elastic Beanstalk Worker environments include an sqsd daemon that polls an SQS queue and posts messages as HTTP POST requests to the application (localhost). Beanstalk can auto-create the SQS queue or you can specify an existing one. This decouples job submission from processing.',
  },
  {
    domain: 'Deployment',
    q: 'A developer wants to pass database connection string configuration to an Elastic Beanstalk application without hardcoding it. What is the recommended approach?',
    opts: [
      'Store it in a `.ebextensions/env.config` file checked into source control.',
      'Set it as an Elastic Beanstalk environment property; the application reads it as an OS environment variable.',
      "Embed it in the application's `application.properties` file.",
      'Store it in a publicly readable S3 bucket and download at startup.',
    ],
    a: 1,
    exp: 'Elastic Beanstalk environment properties are passed to EC2 instances as OS environment variables. Applications read them via `os.environ` (Python), `process.env` (Node.js), `System.getenv()` (Java), etc. This keeps sensitive config out of source code and allows environment-specific overrides.',
  },

  // ── ECS ──────────────────────────────────────────────────────────────────────
  {
    domain: 'Deployment',
    q: 'An ECS task definition specifies two containers: a web server and a logging sidecar. Both containers must be on the same host and share network interfaces. Which ECS networking mode enables this?',
    opts: ['bridge', 'host', 'awsvpc', 'none'],
    a: 2,
    exp: '`awsvpc` mode gives each ECS task its own Elastic Network Interface (ENI) and private IP. Containers within the same task share the task\'s network namespace and can communicate over localhost. This is required for Fargate and recommended for EC2 launch type for security group isolation.',
  },
  {
    domain: 'Deployment',
    q: 'A developer is performing a blue/green deployment of an ECS service using CodeDeploy. What does CodeDeploy manage in this scenario?',
    opts: [
      'CodeDeploy updates the ECS task definition and registers new tasks directly.',
      'CodeDeploy shifts traffic between the original task set (blue) and a new task set (green) via the Application Load Balancer target groups.',
      'CodeDeploy replaces EC2 container instances in the ECS cluster.',
      'CodeDeploy updates the ECR image and triggers ECS to redeploy.',
    ],
    a: 1,
    exp: 'ECS blue/green with CodeDeploy uses two ALB target groups. CodeDeploy creates a new task set (green), runs optional test traffic hooks, then shifts traffic from the blue target group to the green target group. The old task set is deleted after a configurable wait period.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'An ECS task running on Fargate needs to call AWS API (DynamoDB). How should credentials be provided?',
    opts: [
      'Embed IAM access keys in the container image.',
      'Set IAM_ACCESS_KEY and IAM_SECRET_KEY environment variables in the task definition.',
      'Assign a Task IAM Role to the ECS task definition; the ECS agent injects temporary credentials.',
      'Use an EC2 instance profile attached to the Fargate host.',
    ],
    a: 2,
    exp: "ECS Task IAM Roles allow each task to assume an IAM role via the ECS credential provider. The AWS SDK automatically retrieves temporary credentials from the task metadata endpoint. There is no EC2 instance profile for Fargate since you don't manage instances.",
  },
  {
    domain: 'Deployment',
    q: 'What is the difference between an ECS Task Definition and an ECS Service?',
    opts: [
      'A Task Definition defines the containers and resources; a Service ensures a desired number of tasks are running and handles scaling and load balancing.',
      'A Service defines the containers; a Task Definition manages the deployment strategy.',
      'Task Definitions are for Fargate; Services are for EC2 launch type.',
      'A Service is a one-off task; a Task Definition is a long-running service.',
    ],
    a: 0,
    exp: 'A Task Definition is a blueprint (container image, CPU/memory, env vars, ports). An ECS Service uses a task definition to run and maintain a specified number of task instances, integrate with a load balancer, perform rolling updates, and support auto scaling. A task is a running instance of a task definition.',
  },

  // ── X-Ray ────────────────────────────────────────────────────────────────────
  {
    domain: 'Troubleshooting and Optimization',
    q: 'A developer adds custom business data (user ID, order amount) to X-Ray traces. This data must be searchable in the X-Ray console filter expressions. Should the data be added as annotations or metadata?',
    opts: [
      'Metadata — it is indexed and searchable.',
      'Annotations — key-value pairs that are indexed and searchable in filter expressions.',
      'Both annotations and metadata are indexed; use either.',
      'Neither; use CloudWatch Logs Insights for searchable business data.',
    ],
    a: 1,
    exp: 'X-Ray annotations are indexed key-value pairs (string, number, or boolean) that appear in filter expressions (e.g., `annotation.userId = "u123"`). Metadata is arbitrary JSON data attached to segments/subsegments but is NOT indexed — it appears in the trace detail but cannot be filtered on.',
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: 'An application traces 100% of requests with X-Ray but costs are too high. What should the developer configure to reduce tracing volume?',
    opts: [
      'Disable X-Ray entirely and use CloudWatch metrics.',
      'Configure a sampling rule to trace a percentage of requests (e.g., 5%) with optional rules for specific paths.',
      'Reduce the number of subsegments created in the code.',
      'Switch from active tracing to passive tracing.',
    ],
    a: 1,
    exp: 'X-Ray sampling rules control what percentage of requests are traced. The default rule traces 5% + 1 per second. Custom sampling rules let you set reservoir size (absolute requests/second) and fixed rate (percentage). This reduces cost while retaining statistical visibility.',
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: 'In X-Ray terminology, what is a subsegment?',
    opts: [
      'A trace that spans multiple services.',
      'A child unit within a segment that represents a downstream call (e.g., a DynamoDB query or HTTP request).',
      'An X-Ray group that aggregates traces by service name.',
      'The root segment created when a request enters the application.',
    ],
    a: 1,
    exp: 'A segment is the top-level unit of work for one service. A subsegment is a child of a segment representing a downstream call — a DynamoDB operation, an HTTP call to another service, a SQL query, etc. Subsegments provide granular timing and error information within a service.',
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: 'An X-Ray trace shows a `Throttle` fault on a downstream DynamoDB call. What does this indicate and how should it be handled?',
    opts: [
      'DynamoDB rejected the request due to an authentication error; check IAM permissions.',
      'DynamoDB rate-limited the request (ProvisionedThroughputExceededException); implement exponential backoff with jitter and consider increasing provisioned capacity or switching to on-demand.',
      'The X-Ray segment was too large and was throttled by X-Ray.',
      "The Lambda function's reserved concurrency was exceeded.",
    ],
    a: 1,
    exp: 'A Throttle error in X-Ray on a DynamoDB subsegment indicates the request was throttled by DynamoDB (ProvisionedThroughputExceededException or request rate too high). The AWS SDK retries with exponential backoff by default, but applications should also implement application-level retry logic and consider capacity planning.',
  },

  // ── Secrets Manager vs Parameter Store ──────────────────────────────────────
  {
    domain: 'Security',
    q: 'A developer needs to store a database password that must be automatically rotated every 30 days using a Lambda function. Which service is designed for this use case?',
    opts: [
      'AWS Systems Manager Parameter Store (SecureString type).',
      'AWS Secrets Manager with automatic rotation configured.',
      'AWS KMS with a rotation policy.',
      'AWS IAM with credential rotation.',
    ],
    a: 1,
    exp: 'Secrets Manager natively supports automatic secret rotation using Lambda functions (AWS provides managed rotation functions for RDS, Redshift, etc., or you can use a custom Lambda). Parameter Store does not have built-in automatic rotation — you must build rotation yourself.',
  },
  {
    domain: 'Security',
    q: 'An application needs to retrieve a configuration value (non-sensitive) at startup and cache it for an hour. The value is shared across multiple services. Which service is most cost-effective?',
    opts: [
      'AWS Secrets Manager with caching SDK.',
      'AWS Systems Manager Parameter Store (Standard tier, no cost for standard parameters).',
      'Amazon S3 with a versioned config file.',
      'AWS AppConfig.',
    ],
    a: 1,
    exp: 'SSM Parameter Store Standard tier stores up to 10,000 parameters at no additional charge (API calls have standard rates). Secrets Manager has a per-secret monthly cost ($0.40/secret/month). For non-sensitive, non-rotating configuration, Parameter Store is the cost-effective choice.',
  },
  {
    domain: 'Security',
    q: 'An application retrieves a secret from Secrets Manager on every Lambda invocation. Cold starts are slow due to the API call. What is the best approach to reduce latency without sacrificing security?',
    opts: [
      'Store the secret in an environment variable.',
      'Use the AWS Parameters and Secrets Lambda Extension to cache secrets in the execution environment with a configurable TTL.',
      'Cache the secret in DynamoDB with a 1-hour TTL.',
      'Enable Secrets Manager VPC endpoint to reduce latency.',
    ],
    a: 1,
    exp: 'The AWS Parameters and Secrets Lambda Extension runs as a Lambda layer and caches Parameter Store and Secrets Manager values in the execution environment. It uses an HTTP endpoint on localhost, reducing API calls. The TTL controls cache freshness. This is the AWS-recommended approach for Lambda.',
  },

  // ── Cognito ──────────────────────────────────────────────────────────────────
  {
    domain: 'Security',
    q: 'A mobile app uses Cognito User Pools for authentication. After login, the app receives three tokens. Which token should be used to call an API Gateway endpoint protected by a Cognito User Pool authorizer?',
    opts: [
      'Refresh token',
      'Access token',
      'ID token',
      'Either the access or ID token; API Gateway accepts both.',
    ],
    a: 2,
    exp: 'The ID token contains user identity claims (email, name, custom attributes) and is a JWT. API Gateway Cognito User Pool authorizers validate the ID token by default (you can configure the token source). The access token is for calling Cognito APIs on behalf of the user. The refresh token is used to get new access/ID tokens.',
  },
  {
    domain: 'Security',
    q: 'What is the difference between Cognito User Pools and Cognito Identity Pools?',
    opts: [
      'User Pools handle authentication (sign-up/sign-in) and issue JWTs; Identity Pools exchange those JWTs (or other identity tokens) for temporary AWS credentials to access AWS services directly.',
      'Identity Pools handle authentication; User Pools provide temporary AWS credentials.',
      'User Pools are for web apps; Identity Pools are for mobile apps.',
      'User Pools store user data in DynamoDB; Identity Pools store data in S3.',
    ],
    a: 0,
    exp: 'User Pools are an identity provider — they manage user registration, authentication, and issue JWTs. Identity Pools (Federated Identities) are an authorization service — they accept tokens from User Pools, social IdPs, SAML, etc., and exchange them for short-lived AWS credentials (via STS AssumeRoleWithWebIdentity).',
  },
  {
    domain: 'Security',
    q: 'A developer wants to add social login (Google, Facebook) to a Cognito User Pool app without building a custom login UI. Which Cognito feature enables this?',
    opts: [
      'Cognito Identity Pools federation.',
      'Cognito User Pools Hosted UI with federated identity providers configured.',
      'Cognito triggers with a Lambda pre-authentication hook.',
      'Cognito User Pools resource servers.',
    ],
    a: 1,
    exp: 'Cognito User Pools Hosted UI is a pre-built, customizable web UI for sign-up/sign-in. When you configure third-party IdPs (Google, Facebook, OIDC, SAML), the Hosted UI handles the OAuth2 flow and federates the user into the User Pool. No custom UI code is needed.',
  },
  {
    domain: 'Security',
    q: 'A Cognito JWT token has expired. What should the client application do?',
    opts: [
      'Re-authenticate the user with their username and password.',
      'Use the refresh token to call the InitiateAuth or GetTokens API to obtain new access and ID tokens.',
      'Call the Cognito AdminResetUserPassword API.',
      'Increase the token expiry duration in the User Pool settings.',
    ],
    a: 1,
    exp: 'The refresh token (valid up to 3,650 days, configurable) can be exchanged for new access and ID tokens without re-entering credentials. The client calls `InitiateAuth` with `REFRESH_TOKEN_AUTH` flow. If the refresh token also expires, the user must re-authenticate.',
  },

  // ── IAM ──────────────────────────────────────────────────────────────────────
  {
    domain: 'Security',
    q: 'An application running on EC2 needs to call S3. In what order does the AWS SDK look for credentials?',
    opts: [
      'Environment variables → ~/.aws/credentials file → EC2 instance metadata (instance profile)',
      'EC2 instance metadata → environment variables → ~/.aws/credentials file',
      '~/.aws/credentials file → environment variables → EC2 instance metadata',
      'Environment variables → EC2 instance metadata → ~/.aws/credentials file',
    ],
    a: 0,
    exp: 'The AWS SDK credential chain (default for most SDKs): 1) Environment variables (AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY), 2) Java system properties (Java SDK), 3) ~/.aws/credentials file, 4) Container credentials (ECS task role), 5) EC2 instance metadata/IMDS (instance profile). The first found credentials are used.',
  },
  {
    domain: 'Security',
    q: 'A Lambda function needs to read from a DynamoDB table in a different AWS account. What is the correct IAM approach?',
    opts: [
      'Create an IAM user in the target account and embed the access keys in the Lambda environment variables.',
      'Configure the Lambda execution role to call STS AssumeRole on a role in the target account that has DynamoDB permissions.',
      "Use a resource-based policy on DynamoDB to allow the source account Lambda.",
      "Share the target account's root credentials with the source account.",
    ],
    a: 1,
    exp: "Cross-account access uses IAM role assumption: the Lambda execution role is granted `sts:AssumeRole` on a role in the target account. That role has a trust policy allowing the source account's Lambda role and has DynamoDB permissions. The Lambda calls `sts.assumeRole()` to get temporary credentials. Note: DynamoDB does not support resource-based policies.",
  },
  {
    domain: 'Security',
    q: 'An IAM policy has both an Allow and a Deny for the same action. What is the result?',
    opts: [
      'The Allow takes precedence.',
      'The Deny takes precedence — explicit Deny always overrides Allow.',
      'The most specific policy (resource-level vs. action-level) takes precedence.',
      'The result depends on the policy type: identity-based vs. resource-based.',
    ],
    a: 1,
    exp: 'IAM evaluation logic: first, check for explicit Deny — if found, the request is denied regardless of any Allow. Second, check for explicit Allow. Third, if neither, the default is implicit Deny. An explicit Deny always wins.',
  },
  {
    domain: 'Security',
    q: 'A developer uses `aws sts assume-role` to get temporary credentials. What three values does the response contain?',
    opts: [
      'AccessKeyId, SecretAccessKey, AccountId',
      'AccessKeyId, SecretAccessKey, SessionToken',
      'AccessKeyId, PublicKey, SessionToken',
      'AccessKeyId, SecretAccessKey, RoleArn',
    ],
    a: 1,
    exp: 'STS AssumeRole returns temporary security credentials: AccessKeyId, SecretAccessKey, and SessionToken. All three must be used together when making API calls. The SessionToken is passed as the `AWS_SESSION_TOKEN` environment variable or `x-amz-security-token` header.',
  },

  // ── CloudWatch ───────────────────────────────────────────────────────────────
  {
    domain: 'Troubleshooting and Optimization',
    q: 'A developer wants to publish the number of orders processed per second as a custom CloudWatch metric from a Lambda function. What is the most efficient approach for high-frequency metrics?',
    opts: [
      'Call `cloudwatch.putMetricData()` on every order processed.',
      'Use the CloudWatch Embedded Metric Format (EMF) — structured log events that CloudWatch automatically extracts as metrics.',
      'Write order counts to DynamoDB and use a CloudWatch metric filter on DynamoDB logs.',
      'Use X-Ray annotations to capture the order count.',
    ],
    a: 1,
    exp: 'EMF (Embedded Metric Format) lets you emit metric data as structured JSON log events. CloudWatch automatically extracts the metrics without additional API calls. This is efficient (batch metric extraction from logs), reduces Lambda duration spent on PutMetricData API calls, and enables detailed logging alongside metrics.',
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: 'A Lambda function occasionally times out. A developer wants to receive an alert when more than 5 timeouts occur in a 5-minute window. What is the correct approach?',
    opts: [
      'Create a CloudWatch Alarm on the Lambda `Errors` metric with a threshold of 5.',
      'Create a CloudWatch metric filter on `/aws/lambda/<function>` logs matching `"Task timed out"`, then create an alarm on the resulting custom metric.',
      'Use X-Ray to detect timeouts and trigger an SNS notification.',
      'Create a CloudWatch Alarm on the Lambda `Duration` metric with a threshold equal to the function timeout.',
    ],
    a: 1,
    exp: 'Lambda timeout events appear in the function logs as "Task timed out after X seconds". A CloudWatch Logs metric filter can match this pattern and emit a custom metric (count). A CloudWatch Alarm on this metric can trigger an SNS notification. The `Errors` metric counts exceptions, not timeouts specifically.',
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: 'A developer needs to run a query across several days of Lambda log data to find the top 10 most frequent error messages. Which service provides this capability?',
    opts: [
      'CloudWatch Dashboards',
      'CloudWatch Logs Insights',
      'AWS X-Ray Analytics',
      'Amazon Athena querying CloudWatch exports in S3',
    ],
    a: 1,
    exp: 'CloudWatch Logs Insights is an interactive query service for CloudWatch logs. You can write queries (using the CWL query language) to filter, aggregate, and visualize log data across log groups. For example: `filter @message like /ERROR/ | stats count(*) as cnt by errorMessage | sort cnt desc | limit 10`.',
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: 'A CloudWatch alarm is in `INSUFFICIENT_DATA` state. What does this mean?',
    opts: [
      'The alarm threshold has been exceeded.',
      'Not enough data points have been collected to determine the alarm state — the metric may be new or no data has been received in the evaluation period.',
      'The metric does not exist in CloudWatch.',
      'The alarm is disabled.',
    ],
    a: 1,
    exp: 'INSUFFICIENT_DATA is the initial alarm state and occurs when there is not enough metric data to evaluate the alarm condition. This happens when a metric is new, when data is sparse, or during evaluation periods with no data points. You can configure the alarm to treat missing data as good, bad, ignore, or breaching.',
  },

  // ── Step Functions ───────────────────────────────────────────────────────────
  {
    domain: 'Development with AWS Services',
    q: 'A workflow requires a human approval step — the Step Functions state machine must pause and wait for a callback from an external system before continuing. Which integration pattern enables this?',
    opts: [
      'Request-response integration — Lambda polls for the approval.',
      'waitForTaskToken — the state machine pauses until a callback is made with the task token.',
      'Synchronous integration — Step Functions calls the approval service and waits.',
      'Event-driven integration using EventBridge to resume the state machine.',
    ],
    a: 1,
    exp: 'The waitForTaskToken integration pattern pauses execution of a state and provides a unique task token. The external system (human approval UI, another service) calls `SendTaskSuccess` or `SendTaskFailure` with the token to resume the execution. This enables durable waiting without polling.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'What is the key difference between AWS Step Functions Standard and Express workflows?',
    opts: [
      'Standard workflows are for Lambda; Express workflows are for ECS.',
      'Standard workflows support exactly-once execution and can run up to 1 year; Express workflows support at-least-once execution and run up to 5 minutes.',
      'Express workflows are exactly-once; Standard workflows are at-least-once.',
      'Standard workflows have a lower cost per state transition than Express.',
    ],
    a: 1,
    exp: 'Standard Workflows: exactly-once execution, up to 1 year duration, charged per state transition — suited for long-running, durable workflows. Express Workflows: at-least-once execution, up to 5 minutes, charged by number of executions and duration — suited for high-volume, short-duration workflows like IoT data ingestion.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A Step Functions state machine needs to run three Lambda functions in parallel and continue only when all three complete. Which state type provides this?',
    opts: ['Choice state', 'Wait state', 'Parallel state', 'Map state'],
    a: 2,
    exp: 'The Parallel state runs multiple branches simultaneously. Each branch is an independent state machine fragment. The Parallel state completes when ALL branches complete (or any fails, by default). Map state iterates over a dynamic list; Parallel state runs a fixed set of branches.',
  },

  // ── EventBridge ──────────────────────────────────────────────────────────────
  {
    domain: 'Development with AWS Services',
    q: 'A developer wants to trigger a Lambda function whenever an EC2 instance state changes to "stopped". Which EventBridge configuration is required?',
    opts: [
      'A rule with a schedule expression of `rate(1 minute)` that polls EC2.',
      'A rule with an event pattern matching `{"source": ["aws.ec2"], "detail-type": ["EC2 Instance State-change Notification"], "detail": {"state": ["stopped"]}}`.',
      'A CloudWatch alarm on EC2 CPU utilization triggering Lambda.',
      'An SNS topic subscribed to EC2 state change notifications.',
    ],
    a: 1,
    exp: 'EventBridge rules use event patterns to match events from AWS services. EC2 publishes state change events to the default event bus. The pattern filters on source, detail-type, and specific detail fields. When matched, the rule invokes the specified targets (Lambda, SQS, Step Functions, etc.).',
  },
  {
    domain: 'Development with AWS Services',
    q: 'An application in Account A wants to receive events from Account B using EventBridge. What must be configured?',
    opts: [
      'Create a VPC peering connection between the accounts.',
      "In Account B, create an EventBridge rule that sends events to Account A's event bus as the target. In Account A, add a resource-based policy to the event bus allowing Account B to send events.",
      'Create an IAM cross-account role that allows EventBridge in Account B to invoke Account A resources.',
      'Use SNS cross-account subscriptions instead of EventBridge.',
    ],
    a: 1,
    exp: "EventBridge supports cross-account event delivery. The source account (B) creates a rule targeting the destination account's (A) event bus ARN. The destination account must add a resource-based policy (event bus policy) allowing the source account to send events. Account A then creates its own rules on its bus.",
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer creates an EventBridge rule with a cron expression `cron(0 12 * * ? *)`. When does this rule trigger?',
    opts: [
      'Every 12 hours',
      'At 12:00 PM UTC every day',
      'At midnight UTC on the 12th of every month',
      'Every minute for 12 hours',
    ],
    a: 1,
    exp: 'EventBridge cron syntax: `cron(minutes hours day-of-month month day-of-week year)`. `cron(0 12 * * ? *)` means: minute 0, hour 12, every day of month, every month, any day of week, every year — which is 12:00 PM UTC daily. The `?` is required for either day-of-month or day-of-week when the other is specified.',
  },

  // ── SDK Error Handling ───────────────────────────────────────────────────────
  {
    domain: 'Troubleshooting and Optimization',
    q: 'An application calls DynamoDB PutItem and receives a `ProvisionedThroughputExceededException`. The AWS SDK is configured with default retry settings. What does the SDK do by default?',
    opts: [
      'Immediately raises the exception to the application without retrying.',
      'Retries up to 3 times with fixed 100ms intervals.',
      'Retries up to 10 times using exponential backoff with jitter.',
      'Retries indefinitely until the request succeeds.',
    ],
    a: 2,
    exp: 'The AWS SDK automatically retries throttling errors (like ProvisionedThroughputExceededException) using exponential backoff. Default retry count is typically 3 (SDK v2) or configurable (SDK v3). The retry behavior uses exponential backoff with jitter to spread retry attempts and reduce thundering herd.',
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: 'Why is "jitter" added to exponential backoff retry logic?',
    opts: [
      'To ensure retries happen exactly at the calculated interval.',
      "To add randomness to retry intervals so that multiple clients don't retry simultaneously, reducing the chance of a synchronized thundering herd.",
      'To increase retry speed for time-sensitive operations.',
      'To comply with AWS API throttling policies.',
    ],
    a: 1,
    exp: "Exponential backoff without jitter can cause all clients to retry at the same moment (synchronized thundering herd), which re-saturates the service. Adding random jitter (e.g., `sleep = min(cap, base * 2^attempt) * random(0, 1)`) spreads retries over time, reducing peak load on the service.",
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: 'An application calls an AWS API and receives a `5xx` error. Should the application retry?',
    opts: [
      'No — 5xx errors indicate a permanent failure that retrying will not fix.',
      'Yes — 5xx errors indicate transient server-side issues and are safe to retry with exponential backoff.',
      'Only if the error is 503; 500 and 502 are permanent.',
      'Only retry if the Retry-After header is present in the response.',
    ],
    a: 1,
    exp: 'AWS recommends retrying 5xx (500, 502, 503, 504) errors as they typically indicate transient server-side issues. 4xx errors (except 429 Too Many Requests) generally indicate client errors that retrying will not fix. The SDK retries 5xx and throttling errors automatically.',
  },
  {
    domain: 'Troubleshooting and Optimization',
    q: "A developer writes the following Python code to call S3:\n```python\nimport boto3\nfrom botocore.config import Config\n\nconfig = Config(retries={'max_attempts': 10, 'mode': 'adaptive'})\ns3 = boto3.client('s3', config=config)\n```\nWhat does the `adaptive` retry mode do differently from `standard` mode?",
    opts: [
      'Adaptive mode retries only on network errors, not API errors.',
      'Adaptive mode dynamically adjusts retry rate based on throttling feedback from the service, reducing requests when throttling is detected.',
      'Adaptive mode uses a fixed 1-second interval between retries.',
      'Adaptive mode is identical to standard mode with max_attempts set to 10.',
    ],
    a: 1,
    exp: 'Botocore retry modes: `legacy` (3 retries, exponential), `standard` (3 retries, exponential with jitter, handles more error types), `adaptive` (like standard but additionally uses a client-side token bucket rate limiter that dynamically throttles outbound request rate based on throttling responses from the service).',
  },
];
