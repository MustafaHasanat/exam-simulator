import type { Question } from '../types';

export const BANK_DVA: Question[] = [
  // ── Development with AWS Services ──────────────────────────────────────────
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to process messages from an SQS queue exactly once, in order. Which queue type should be used?',
    opts: [
      'Standard Queue with deduplication',
      'FIFO Queue',
      'Standard Queue with long polling',
      'Dead Letter Queue',
    ],
    a: 1,
    exp: 'FIFO (First-In-First-Out) queues guarantee exactly-once processing and preserve message order. Standard queues offer at-least-once delivery and best-effort ordering, which is not suitable for order-sensitive workflows.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A Lambda function is experiencing cold starts that are impacting user experience. The function uses a 512 MB memory configuration and connects to a database on every invocation. Which approach BEST reduces cold start latency?',
    opts: [
      'Increase memory to 3008 MB',
      'Enable Lambda Provisioned Concurrency',
      'Move the database connection inside the handler function',
      'Switch the function runtime to Node.js',
    ],
    a: 1,
    exp: 'Provisioned Concurrency keeps a specified number of Lambda execution environments initialized and ready to respond immediately, eliminating cold starts entirely. Increasing memory may slightly speed up initialization but does not eliminate cold starts. Database connections should be moved outside the handler (not inside), and runtime choice has minimal cold-start impact.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer wants a Lambda function to share common dependencies across multiple functions without bundling them into each deployment package. What is the BEST approach?',
    opts: [
      'Upload a shared S3 bucket with the dependencies',
      'Use Lambda Layers to package and share common libraries',
      'Create a Lambda function that returns the libraries as a response',
      'Store the dependencies in an EFS file system',
    ],
    a: 1,
    exp: 'Lambda Layers let you package libraries, custom runtimes, and other function dependencies separately, and multiple functions can reference the same layer. EFS is also a valid shared storage option but adds networking complexity; Layers are the idiomatic solution for shared code.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A Lambda function invoked by API Gateway needs to return a custom HTTP status code. The function is configured with Lambda proxy integration. How should the function return the status code?',
    opts: [
      'Throw an exception with the status code as the message',
      'Return a JSON object with statusCode, headers, and body fields',
      'Set a response header named X-Status-Code in the response',
      'Configure a mapping template in API Gateway to set the status code',
    ],
    a: 1,
    exp: 'With Lambda proxy integration, API Gateway passes the entire request to Lambda and expects a JSON response with statusCode, headers, and body fields. API Gateway forwards these values directly to the client. Without proxy integration, mapping templates would be needed, but with proxy integration the function controls the full HTTP response.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A company runs a serverless API where the Lambda functions occasionally experience throttling errors. The Lambda function\'s reserved concurrency is set to 10. What happens to requests that exceed this limit?',
    opts: [
      'Lambda queues the excess requests and retries them automatically',
      'Lambda returns a 429 ThrottlingException to the caller',
      'Lambda scales up beyond the reserved concurrency limit',
      'API Gateway caches the response and returns it without invoking Lambda',
    ],
    a: 1,
    exp: 'When a Lambda function reaches its reserved concurrency limit, additional invocations are throttled and receive a 429 ThrottlingException. Reserved concurrency acts as both a ceiling and a guarantee — it caps the function at exactly that number of concurrent executions and does not auto-scale beyond it.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'An API Gateway REST API needs to cache responses for 300 seconds, but a specific client must always receive a fresh response. How can the developer allow the client to bypass the cache?',
    opts: [
      'Add a Cache-Control: max-age=0 header to the client request',
      'Set the X-Amz-No-Cache header in the client request',
      'Disable caching for that specific API method',
      'Use an HTTP API instead of a REST API',
    ],
    a: 0,
    exp: 'API Gateway respects the Cache-Control: max-age=0 header to allow clients to request a fresh response, bypassing the cache. This must be enabled in the API Gateway cache settings. HTTP APIs do not support caching, so switching API types would remove caching for all clients.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer is designing a DynamoDB table to store user orders. Queries will frequently filter on userId (partition key) and orderDate (sort key). Some queries also need to filter on orderStatus. Which DynamoDB feature supports the orderStatus filter efficiently without a full table scan?',
    opts: [
      'Local Secondary Index (LSI) on orderStatus',
      'Global Secondary Index (GSI) on orderStatus',
      'DynamoDB Streams with a Lambda filter',
      'DynamoDB DAX caching',
    ],
    a: 1,
    exp: 'A GSI allows queries on attributes other than the primary key, making it possible to query efficiently by orderStatus without scanning the entire table. An LSI must share the same partition key (userId) and only adds an alternate sort key — it cannot stand alone on orderStatus. DAX accelerates reads but does not add query flexibility.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A DynamoDB table is receiving a ProvisionedThroughputExceededException on writes. The access pattern shows that 90% of write traffic targets the same partition key. What is the BEST solution?',
    opts: [
      'Enable DynamoDB Auto Scaling',
      'Redesign the partition key to distribute writes more evenly',
      'Switch the table to on-demand billing mode',
      'Add a GSI with a different partition key',
    ],
    a: 1,
    exp: 'Hot partitions occur when too many requests target a single partition key, causing throttling even if overall provisioned capacity is sufficient. The root cause is a poor key design, and the best fix is to choose a high-cardinality partition key that evenly distributes traffic. Auto Scaling and on-demand mode help with capacity, but they cannot resolve a hot-partition design problem.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer wants to perform a write to a DynamoDB item only if an attribute named stock is greater than 0. Which DynamoDB feature should be used?',
    opts: [
      'DynamoDB Transactions',
      'Conditional Writes with a ConditionExpression',
      'DynamoDB Streams',
      'Optimistic locking with version numbers',
    ],
    a: 1,
    exp: 'DynamoDB Conditional Writes use a ConditionExpression to make a write operation succeed only when the specified condition is true (e.g., stock > :zero). If the condition fails, the write is rejected without consuming write capacity beyond the conditional check. Transactions are used for multi-item atomicity, not single-item conditions.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'An application uses DynamoDB and requires low single-digit millisecond read performance at peak load. The data is read frequently but updated rarely. Which solution provides the BEST read performance with the least operational effort?',
    opts: [
      'Enable DynamoDB read replicas',
      'Enable DynamoDB Accelerator (DAX)',
      'Cache responses in ElastiCache for Redis',
      'Use DynamoDB strongly consistent reads',
    ],
    a: 1,
    exp: 'DAX is a fully managed, in-memory cache for DynamoDB that delivers microsecond response times. It is API-compatible with DynamoDB, so minimal code changes are needed. ElastiCache requires more operational effort and code changes to integrate. Strongly consistent reads actually increase latency compared to eventually consistent reads.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer configures an SQS queue with a visibility timeout of 30 seconds. A consumer retrieves a message, but the processing takes 45 seconds. What happens to the message?',
    opts: [
      'The message is deleted after the visibility timeout expires',
      'The message becomes visible again in the queue after 30 seconds',
      'SQS automatically extends the visibility timeout by another 30 seconds',
      'SQS moves the message to the Dead Letter Queue',
    ],
    a: 1,
    exp: 'When the visibility timeout expires before a consumer explicitly deletes the message, SQS makes the message visible again so another consumer can process it. This can lead to duplicate processing. Consumers should extend the visibility timeout using ChangeMessageVisibility if processing takes longer than expected.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer wants to reduce the number of API calls made to SQS and reduce costs when the queue is mostly empty. Which approach should be used?',
    opts: [
      'Decrease the visibility timeout',
      'Enable long polling by setting WaitTimeSeconds to 20',
      'Enable short polling by setting WaitTimeSeconds to 0',
      'Increase the message retention period',
    ],
    a: 1,
    exp: 'Long polling (WaitTimeSeconds up to 20) causes SQS to wait for a message to arrive before responding, reducing empty responses and API calls. Short polling (WaitTimeSeconds = 0) returns immediately even if the queue is empty, increasing unnecessary API calls and costs.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'An application publishes notifications to multiple downstream services using SNS. Each subscriber only wants to receive messages that match a specific event type attribute. Which SNS feature enables this?',
    opts: [
      'SNS FIFO topics',
      'SNS message filtering with subscription filter policies',
      'SNS message archiving',
      'SNS dead-letter queues',
    ],
    a: 1,
    exp: 'SNS subscription filter policies allow each subscriber to specify attribute-based filters so they only receive messages that match the policy. Without filters, every subscriber receives every message. FIFO topics provide ordering, not attribute-based filtering.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to fan out an event to multiple SQS queues simultaneously. What is the BEST architecture?',
    opts: [
      'Publish the event directly to each SQS queue using multiple API calls',
      'Publish to an SNS topic and subscribe each SQS queue to the topic',
      'Use EventBridge to schedule the delivery to each SQS queue',
      'Use a Lambda function to forward the event to each SQS queue',
    ],
    a: 1,
    exp: 'The SNS-to-SQS fan-out pattern is the standard approach: publish once to an SNS topic and have multiple SQS queues subscribe. Each queue receives its own copy of the message. This decouples the publisher from the consumers and scales easily without code changes.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A streaming application ingests clickstream data at 5 MB/s and needs to process each record within 1 second. Which Kinesis Data Streams configuration is sufficient?',
    opts: [
      '1 shard with enhanced fan-out',
      '5 shards using the standard iterator',
      '10 shards with standard iterator',
      '1 shard with increased retention period',
    ],
    a: 1,
    exp: 'Each Kinesis shard supports 1 MB/s write and 2 MB/s read throughput. At 5 MB/s of ingest, at least 5 shards are needed to absorb the writes. Standard iterator read capacity (2 MB/s per shard) is adequate for a single consumer at this throughput. Enhanced fan-out is needed only when multiple consumer applications each need the full 2 MB/s per shard.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to orchestrate a multi-step order processing workflow where each step depends on the result of the previous step, with retry logic and error handling. Which AWS service is BEST suited?',
    opts: [
      'Amazon SQS with Lambda',
      'AWS Step Functions',
      'Amazon SNS with Lambda',
      'Amazon EventBridge with Lambda',
    ],
    a: 1,
    exp: 'Step Functions provides a visual workflow engine with built-in support for sequential and parallel states, retries with exponential backoff, error handling, and timeouts. SQS and SNS are messaging services that require custom orchestration logic, making complex multi-step workflows harder to manage and observe.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A mobile app allows users to sign in and then access AWS services directly. Which Cognito component issues AWS temporary credentials to authenticated users?',
    opts: [
      'Cognito User Pool',
      'Cognito Identity Pool',
      'Cognito Sync',
      'Cognito User Pool with hosted UI',
    ],
    a: 1,
    exp: 'Cognito Identity Pools (Federated Identities) exchange a token from a trusted identity provider (such as a Cognito User Pool, Google, or SAML) for temporary AWS credentials via STS. User Pools handle authentication and directory management but do not themselves vend AWS credentials.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer is building a REST API where users authenticate using Cognito User Pools. The API Gateway should reject unauthenticated requests before they reach Lambda. Which API Gateway authorizer type should be used?',
    opts: [
      'Lambda authorizer (TOKEN type)',
      'Cognito User Pool authorizer',
      'IAM authorization',
      'API key authorization',
    ],
    a: 1,
    exp: 'API Gateway natively integrates with Cognito User Pools via a Cognito authorizer. It validates the JWT token from the Authorization header with no custom Lambda code required. A Lambda authorizer is used for custom or third-party token validation. IAM authorization requires SigV4 signing, which is typically used for service-to-service calls.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'An application uses ElastiCache for Redis to cache database query results. After deploying a new version, stale cached data is returned to users. What is the BEST approach to handle cache invalidation?',
    opts: [
      'Restart the ElastiCache cluster on every deployment',
      'Set an appropriate TTL on cache keys and use cache-aside pattern with explicit invalidation on writes',
      'Disable caching during deployments',
      'Use ElastiCache Read Replicas to serve fresh data',
    ],
    a: 1,
    exp: 'The cache-aside pattern reads from cache, falls back to the database on a miss, and explicitly deletes or updates cache keys when underlying data changes. Combined with a TTL, this bounds staleness. Restarting the cluster on every deployment causes cache stampedes and is operationally disruptive.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to allow an external partner to download a private S3 object for 15 minutes without making the bucket public. What is the BEST approach?',
    opts: [
      'Temporarily set the S3 object ACL to public-read',
      'Generate a pre-signed URL with a 15-minute expiration',
      'Create an S3 bucket policy that allows the partner\'s IP address',
      'Share the AWS access key with the partner',
    ],
    a: 1,
    exp: 'A pre-signed URL encodes the uploader\'s credentials and an expiration time, granting time-limited access to a specific S3 object without requiring the requester to have AWS credentials. Making the object public, sharing access keys, or IP-based policies are security anti-patterns.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'An S3 object upload triggers a Lambda function via an S3 event notification. The Lambda function is failing for objects larger than 5 GB. The upload itself succeeds. What is the MOST likely cause?',
    opts: [
      'S3 event notifications are not supported for objects larger than 5 GB',
      'The Lambda function\'s /tmp storage is too small for large files',
      'S3 multipart upload events are not emitted for objects over 5 GB',
      'The Lambda function timeout is too short to process large objects',
    ],
    a: 3,
    exp: 'Lambda has a maximum execution timeout of 15 minutes and /tmp storage of up to 10 GB. For large objects, the Lambda function is most likely hitting the execution timeout before processing completes. The S3 event notification itself fires normally after a multipart upload completes, and the event contains only metadata, not the object content.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer wants to query a subset of columns from a large CSV file stored in S3 without downloading the entire file. Which S3 feature enables this?',
    opts: [
      'S3 Batch Operations',
      'S3 Select',
      'S3 Intelligent-Tiering',
      'S3 Transfer Acceleration',
    ],
    a: 1,
    exp: 'S3 Select allows you to use SQL expressions to retrieve a subset of data from an S3 object, reducing the amount of data transferred. This is useful for large CSV, JSON, or Parquet files when only a fraction of the data is needed. S3 Batch Operations performs bulk operations on existing objects but does not provide query capability.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to upload a 10 GB file to S3 reliably. The upload occasionally fails partway through. Which S3 feature should be used to handle this efficiently?',
    opts: [
      'S3 Transfer Acceleration',
      'S3 Multipart Upload',
      'S3 Cross-Region Replication',
      'S3 pre-signed POST',
    ],
    a: 1,
    exp: 'S3 Multipart Upload splits large files into parts that are uploaded independently and reassembled in S3. If one part fails, only that part needs to be retried, not the entire file. It is required for objects over 5 GB and recommended for objects over 100 MB. Transfer Acceleration speeds up uploads but does not provide fault tolerance for partial failures.',
  },

  // ── Security ───────────────────────────────────────────────────────────────
  {
    domain: 'Security',
    q: 'A Lambda function needs read access to an S3 bucket. How should permissions be granted following AWS best practices?',
    opts: [
      'Embed AWS access keys in the Lambda environment variables',
      'Attach an IAM execution role to the Lambda function with an S3 read policy',
      'Make the S3 bucket public and access it without credentials',
      'Store credentials in AWS Systems Manager Parameter Store and read them at runtime',
    ],
    a: 1,
    exp: 'The best practice is to attach an IAM execution role to the Lambda function granting only the necessary S3 permissions. Lambda automatically provides temporary credentials via the execution role. Embedding static credentials in environment variables is a security risk because they can be exposed in logs or configuration dumps.',
  },
  {
    domain: 'Security',
    q: 'An application running on EC2 needs to access resources in another AWS account. Which approach should be used?',
    opts: [
      'Create an IAM user in the target account and embed its access keys in the application',
      'Use STS AssumeRole to assume a cross-account IAM role in the target account',
      'Share the root account credentials of the target account',
      'Use VPC Peering to gain access to the other account\'s resources',
    ],
    a: 1,
    exp: 'STS AssumeRole is the standard way to obtain temporary credentials to act in another account. The EC2 instance assumes a role in the target account that has the necessary permissions. This avoids long-lived credentials and follows the principle of least privilege. VPC Peering addresses network connectivity, not IAM authorization.',
  },
  {
    domain: 'Security',
    q: 'A developer is using the AWS CLI and wants to assume a different IAM role for a specific command. Which CLI option allows this without changing the default profile?',
    opts: [
      'Use the --role-arn flag directly in the CLI command',
      'Configure a named profile with role_arn in ~/.aws/config and use --profile',
      'Export AWS_ROLE_ARN as an environment variable before running the command',
      'Use aws iam switch-role to temporarily switch roles',
    ],
    a: 1,
    exp: 'AWS CLI supports named profiles that specify a role_arn and source_profile. Passing --profile <name> to a command causes the CLI to assume the specified role automatically. This avoids modifying the default profile and allows easy role switching per command.',
  },
  {
    domain: 'Security',
    q: 'An application needs to encrypt a 4 MB file using AWS KMS. What is the MOST cost-effective and secure approach?',
    opts: [
      'Call the KMS Encrypt API directly with the 4 MB file',
      'Use envelope encryption: generate a data key with KMS and encrypt the file locally',
      'Store the file in S3 with server-side encryption using SSE-S3',
      'Use AWS Secrets Manager to encrypt the file',
    ],
    a: 1,
    exp: 'KMS Encrypt API has a 4 KB limit on plaintext size. For larger data, envelope encryption is required: generate a plaintext data key using GenerateDataKey, encrypt the data locally with the plaintext key, then discard the plaintext key and store the encrypted data key alongside the ciphertext. This is the standard pattern for KMS with large payloads.',
  },
  {
    domain: 'Security',
    q: 'A developer needs to rotate database passwords automatically and make them available to application code securely. Which AWS service is BEST suited?',
    opts: [
      'AWS Systems Manager Parameter Store (Standard tier)',
      'AWS Secrets Manager',
      'AWS KMS with a customer-managed key',
      'Amazon S3 with server-side encryption',
    ],
    a: 1,
    exp: 'Secrets Manager supports automatic rotation of secrets on a schedule, integrates with supported databases to update credentials, and encrypts secrets using KMS. Parameter Store supports secrets storage and can integrate with KMS, but does not provide native automatic rotation of database passwords.',
  },
  {
    domain: 'Security',
    q: 'An application stores configuration values in AWS Systems Manager Parameter Store. Some parameters contain database passwords. Which parameter type encrypts the value at rest?',
    opts: [
      'String',
      'StringList',
      'SecureString',
      'EncryptedString',
    ],
    a: 2,
    exp: 'SecureString parameters are encrypted at rest using an AWS KMS key. String and StringList parameters are stored in plaintext. There is no "EncryptedString" type in Parameter Store.',
  },
  {
    domain: 'Security',
    q: 'A developer wants to allow a browser-based JavaScript application running on https://app.example.com to call an API Gateway endpoint at https://api.example.com. The browser blocks the requests. What must be configured?',
    opts: [
      'Enable VPC peering between the client and API Gateway',
      'Enable CORS on the API Gateway endpoint',
      'Add a bucket policy to the S3 static hosting bucket',
      'Add an IP whitelist in API Gateway for the client\'s IP address',
    ],
    a: 1,
    exp: 'Browsers enforce the Same-Origin Policy and block cross-origin requests unless the server includes CORS headers. API Gateway must be configured to respond to OPTIONS preflight requests and include Access-Control-Allow-Origin and related headers. VPC peering and IP whitelisting address network access, not browser-enforced CORS restrictions.',
  },
  {
    domain: 'Security',
    q: 'An API Gateway endpoint is publicly accessible but should only be callable by authenticated users in a specific Cognito User Pool group. Which combination achieves this?',
    opts: [
      'Cognito User Pool authorizer + API Gateway resource policy',
      'Cognito User Pool authorizer + Lambda authorizer for group check',
      'IAM authorization + Cognito Identity Pool',
      'API key + Usage Plan',
    ],
    a: 1,
    exp: 'A Cognito User Pool authorizer validates the JWT but does not check group membership by default. A Lambda authorizer can extract group claims from the decoded JWT and allow or deny access based on group membership. This combination provides both authentication (Cognito) and fine-grained authorization (Lambda authorizer).',
  },
  {
    domain: 'Security',
    q: 'A developer wants to generate a pre-signed URL for an S3 object that expires in 7 days. The developer\'s IAM user is used to sign the URL. After 6 hours, users report the URL is no longer working. What is the MOST likely cause?',
    opts: [
      'S3 pre-signed URLs have a maximum lifetime of 6 hours',
      'The IAM user credentials used to sign the URL have been rotated or the session expired',
      'The S3 object was moved to Glacier after 6 hours',
      'Pre-signed URLs cannot be generated by IAM users, only by IAM roles',
    ],
    a: 1,
    exp: 'Pre-signed URLs generated with temporary credentials (such as from an assumed IAM role or SSO session) expire when the underlying credentials expire, regardless of the specified expiration time. If the developer used temporary credentials that expired after 6 hours, the URL becomes invalid. IAM user long-term credentials do not have this issue, but session tokens do.',
  },
  {
    domain: 'Security',
    q: 'An application needs to authenticate users with their corporate Active Directory credentials and then call AWS APIs on their behalf. Which service combination enables this?',
    opts: [
      'Cognito User Pools with a SAML identity provider + Cognito Identity Pools',
      'Cognito User Pools with username/password only + STS',
      'IAM Identity Center with direct IAM role assumption',
      'API Gateway Cognito authorizer + Lambda',
    ],
    a: 0,
    exp: 'Cognito User Pools can federate with a SAML 2.0 identity provider (such as Active Directory via ADFS). After authentication, the token can be exchanged in a Cognito Identity Pool for temporary AWS credentials using STS. This allows corporate users to authenticate with AD and obtain AWS credentials without creating separate IAM users.',
  },
  {
    domain: 'Security',
    q: 'A developer needs to grant an S3 bucket access to objects owned by other AWS accounts that upload to the bucket. What should be configured?',
    opts: [
      'Enable S3 Object Ownership with Bucket owner enforced setting',
      'Add an S3 bucket ACL that grants the other account READ permission',
      'Create a cross-account IAM role in the bucket\'s account',
      'Enable S3 Requester Pays',
    ],
    a: 0,
    exp: 'With the Bucket owner enforced setting for S3 Object Ownership, the bucket owner automatically owns all objects regardless of who uploaded them, and ACLs are disabled. This eliminates the "object owned by another account" problem. ACLs can solve this but are the legacy approach and are being phased out.',
  },
  {
    domain: 'Security',
    q: 'A Lambda function must access a secret stored in Secrets Manager. The function is failing with AccessDeniedException. The execution role has SecretsManagerReadWrite policy attached. What is the MOST likely cause?',
    opts: [
      'The Lambda function is in a VPC without a Secrets Manager VPC endpoint or NAT Gateway',
      'Secrets Manager requires a Lambda authorizer to grant access',
      'Lambda cannot access Secrets Manager; Parameter Store must be used instead',
      'The execution role policy has an explicit Deny for the secretsmanager:GetSecretValue action',
    ],
    a: 3,
    exp: 'An explicit Deny in any attached policy always overrides an Allow. Even though SecretsManagerReadWrite is attached, if another policy (e.g., a Service Control Policy or permission boundary) contains an explicit Deny for secretsmanager:GetSecretValue, the call will fail. Network connectivity (VPC endpoint) is a common but separate issue that returns a connection timeout, not an AccessDeniedException.',
  },
  {
    domain: 'Security',
    q: 'A developer is writing a policy that should allow an IAM user to list all S3 buckets but deny access to a specific bucket named sensitive-data. Which policy structure achieves this?',
    opts: [
      'A single Allow statement for s3:ListAllMyBuckets and s3:GetObject with a NotResource for sensitive-data',
      'An Allow for s3:ListAllMyBuckets on * and a separate explicit Deny for all S3 actions on the sensitive-data bucket ARN',
      'An Allow for s3:ListAllMyBuckets on * only; S3 denies other buckets by default',
      'Remove all S3 permissions and re-add them per bucket except sensitive-data',
    ],
    a: 1,
    exp: 'The correct approach is an Allow for s3:ListAllMyBuckets on all resources and an explicit Deny for all s3:* actions on the arn:aws:s3:::sensitive-data/* ARN. Explicit Denies always win over Allows. Simply not including the bucket in an Allow statement is insufficient if another Allow statement grants access to all buckets.',
  },
  {
    domain: 'Security',
    q: 'An API Gateway Lambda authorizer returns an IAM policy. The policy denies access, but the client still receives a 200 OK response. What is the MOST likely cause?',
    opts: [
      'Lambda authorizer caching is returning a previously cached ALLOW policy',
      'The Lambda authorizer function is returning a malformed policy',
      'API Gateway ignores Lambda authorizer policies for HTTP APIs',
      'The method has IAM authorization enabled, which overrides the Lambda authorizer',
    ],
    a: 0,
    exp: 'API Gateway caches Lambda authorizer responses based on the incoming token. If a previous request with the same token received an ALLOW policy, that cached result will be returned for subsequent requests within the TTL, even if the authorizer would now return DENY. Reducing the TTL or invalidating the cache resolves the issue.',
  },
  {
    domain: 'Security',
    q: 'A company wants to enforce that all new IAM users must enable MFA before they can perform any action. Which IAM feature enforces this?',
    opts: [
      'IAM password policy requiring MFA',
      'An IAM policy with a Deny condition using aws:MultiFactorAuthPresent = false',
      'AWS Config rule for MFA compliance',
      'An SCP at the organization level denying all actions without MFA',
    ],
    a: 1,
    exp: 'An IAM deny policy with a condition on aws:MultiFactorAuthPresent forces MFA before allowing any actions. The policy allows only the actions needed to set up MFA when this condition is false and denies everything else. AWS Config checks compliance but does not enforce access control. SCPs apply at the organization level and can be used similarly, but for per-user enforcement, an IAM policy with the condition is the standard approach.',
  },
  {
    domain: 'Security',
    q: 'A developer generates a KMS data key using GenerateDataKey. What should the developer do with the plaintext data key after encrypting the data?',
    opts: [
      'Store the plaintext data key in S3 for future decryption',
      'Immediately discard the plaintext data key from memory',
      'Encrypt the plaintext data key again with KMS and store both',
      'Store the plaintext data key in DynamoDB with encryption at rest',
    ],
    a: 1,
    exp: 'In envelope encryption, the plaintext data key is used only for the encryption operation and must be immediately discarded from memory afterward. The encrypted data key (returned alongside the plaintext key by GenerateDataKey) is stored alongside the ciphertext. To decrypt, call KMS Decrypt on the encrypted data key to retrieve the plaintext data key, decrypt the data, then discard the plaintext key again.',
  },
  {
    domain: 'Security',
    q: 'A serverless application stores API secrets in environment variables of a Lambda function. A security review flags this as a risk. What is the RECOMMENDED improvement?',
    opts: [
      'Base64-encode the secrets before storing them in environment variables',
      'Store the secrets in Secrets Manager and retrieve them at runtime using the SDK',
      'Move the secrets to Lambda Layers',
      'Encrypt the Lambda function code package with KMS',
    ],
    a: 1,
    exp: 'Environment variables in Lambda are visible in the console and in logs. The recommended practice is to store secrets in Secrets Manager (or Parameter Store SecureString) and retrieve them at Lambda startup, caching the value in memory for the lifetime of the execution environment. Secrets Manager also supports automatic rotation.',
  },
  {
    domain: 'Security',
    q: 'An application uses AWS SDK v2 for Java. The developer needs to call STS to assume a role in another account and use the resulting credentials for subsequent API calls. Which SDK feature handles automatic credential refresh?',
    opts: [
      'StaticCredentialsProvider',
      'STSAssumeRoleSessionCredentialsProvider',
      'ProfileCredentialsProvider',
      'EnvironmentVariableCredentialsProvider',
    ],
    a: 1,
    exp: 'STSAssumeRoleSessionCredentialsProvider automatically refreshes the assumed role credentials before they expire, so the application does not need to manage credential lifecycle manually. StaticCredentialsProvider uses long-lived credentials. ProfileCredentialsProvider reads from ~/.aws/credentials and does not handle role assumption.',
  },

  // ── Deployment ─────────────────────────────────────────────────────────────
  {
    domain: 'Deployment',
    q: 'A developer needs to define build commands for a CodeBuild project. Where are the build commands specified?',
    opts: [
      'In the CodeBuild project environment variables',
      'In the buildspec.yml file at the root of the source repository',
      'In the CodePipeline pipeline definition',
      'In the CloudFormation template that creates the CodeBuild project',
    ],
    a: 1,
    exp: 'buildspec.yml is the file CodeBuild looks for to determine the phases (install, pre_build, build, post_build), environment variables, artifacts, and cache settings. It must be placed at the root of the source code or specified as inline YAML in the CodeBuild project configuration. CodePipeline orchestrates CodeBuild but does not define build commands.',
  },
  {
    domain: 'Deployment',
    q: 'A CodeDeploy deployment to EC2 instances needs to shift traffic gradually: 10% immediately, then 100% after 15 minutes, rolling back if CloudWatch alarms trigger. Which deployment configuration achieves this?',
    opts: [
      'In-place deployment with AllAtOnce configuration',
      'Blue/green deployment with Canary10Percent15Minutes configuration',
      'In-place deployment with HalfAtATime configuration',
      'Blue/green deployment with Linear10PercentEvery1Minute configuration',
    ],
    a: 1,
    exp: 'A canary deployment configuration such as Canary10Percent15Minutes shifts 10% of traffic to the new version immediately and the remaining 90% after 15 minutes. Combined with CodeDeploy CloudWatch alarm-based rollback, this provides gradual rollout with automatic rollback. Linear configurations shift traffic in equal increments at regular intervals.',
  },
  {
    domain: 'Deployment',
    q: 'A CodeDeploy appspec.yml for an EC2 deployment defines lifecycle event hooks. Which hook runs AFTER traffic is shifted to the new application revision?',
    opts: [
      'BeforeInstall',
      'AfterInstall',
      'ApplicationStart',
      'ValidateService',
    ],
    a: 3,
    exp: 'ValidateService is the final lifecycle hook in CodeDeploy and runs after traffic has been shifted to the new revision. It is used to run smoke tests and verify the deployment was successful. BeforeInstall and AfterInstall run before and after the application files are copied; ApplicationStart starts the application process.',
  },
  {
    domain: 'Deployment',
    q: 'A developer uses AWS SAM to define a serverless application. Which command packages and deploys the application to AWS, prompting for configuration parameters on the first run?',
    opts: [
      'sam build',
      'sam deploy --guided',
      'sam package',
      'sam publish',
    ],
    a: 1,
    exp: 'sam deploy --guided walks the developer through specifying the S3 bucket, stack name, region, parameter overrides, and other options on the first run, saving them to samconfig.toml for future deployments. sam build compiles the application locally; sam package uploads artifacts but does not deploy; sam publish publishes to the Serverless Application Repository.',
  },
  {
    domain: 'Deployment',
    q: 'A developer wants to test a Lambda function defined in a SAM template locally before deploying. Which command should be used?',
    opts: [
      'sam deploy --local',
      'sam local invoke',
      'sam test --local',
      'aws lambda invoke --local',
    ],
    a: 1,
    exp: 'sam local invoke runs a Lambda function locally in a Docker container that emulates the Lambda execution environment. The developer can pass a test event JSON file with -e. This allows testing business logic and integration points without deploying to AWS.',
  },
  {
    domain: 'Deployment',
    q: 'A team wants to use infrastructure as code with familiar programming languages (TypeScript, Python) instead of YAML or JSON. Which AWS service supports this?',
    opts: [
      'AWS CloudFormation with custom transforms',
      'AWS CDK (Cloud Development Kit)',
      'AWS SAM',
      'AWS Elastic Beanstalk',
    ],
    a: 1,
    exp: 'AWS CDK allows developers to define cloud infrastructure using TypeScript, Python, Java, C#, or Go. CDK synthesizes the code into CloudFormation templates for deployment. SAM uses YAML/JSON templates (a superset of CloudFormation). CloudFormation natively supports only YAML and JSON.',
  },
  {
    domain: 'Deployment',
    q: 'A CodePipeline pipeline has three stages: Source (CodeCommit), Build (CodeBuild), and Deploy (CodeDeploy). A build failure occurs. What happens to the Deploy stage?',
    opts: [
      'The Deploy stage runs with the last successful build artifact',
      'The Deploy stage is skipped; the pipeline stops at the failed Build stage',
      'CodePipeline rolls back the previous deployment automatically',
      'CodePipeline sends a notification but continues to the Deploy stage',
    ],
    a: 1,
    exp: 'CodePipeline stages run sequentially. If a stage fails, the pipeline stops and does not proceed to subsequent stages. The Deploy stage will not run if the Build stage fails. Manual or automated retries can restart the pipeline from the failed stage.',
  },
  {
    domain: 'Deployment',
    q: 'An Elastic Beanstalk application must be updated with zero downtime. New application instances should be launched alongside old instances before old instances are terminated. Which deployment policy achieves this?',
    opts: [
      'All at once',
      'Rolling',
      'Rolling with additional batch',
      'Immutable',
    ],
    a: 3,
    exp: 'Immutable deployments launch an entirely new set of instances in a new Auto Scaling Group with the new version. Only after the new instances pass health checks does Elastic Beanstalk swap them in and terminate the old instances. This provides zero downtime and easy rollback by simply terminating the new Auto Scaling Group if issues arise.',
  },
  {
    domain: 'Deployment',
    q: 'A developer needs to run a custom script on Elastic Beanstalk EC2 instances during environment creation. Where should the script configuration be placed?',
    opts: [
      'In a buildspec.yml file at the root of the project',
      'In configuration files under the .ebextensions/ directory',
      'In the Elastic Beanstalk environment variables',
      'In the Procfile at the root of the project',
    ],
    a: 1,
    exp: '.ebextensions/ contains YAML configuration files that Elastic Beanstalk processes during environment creation and updates. These files can define commands, container_commands, packages, files, and services. buildspec.yml is used by CodeBuild; the Procfile defines the process type for the application server.',
  },
  {
    domain: 'Deployment',
    q: 'A developer pushes a Docker image to ECR. The subsequent CodeDeploy deployment fails with "CannotPullContainerError". What is the MOST likely cause?',
    opts: [
      'The ECR repository is in a different region than the ECS cluster',
      'The ECS task execution role does not have permission to pull from ECR',
      'Docker images in ECR must be re-tagged before deployment',
      'ECR requires a VPC endpoint for ECS to pull images',
    ],
    a: 1,
    exp: 'ECS tasks pull Docker images from ECR using the task execution role. If this role does not have ecr:GetDownloadUrlForLayer, ecr:BatchGetImage, and ecr:GetAuthorizationToken permissions, the pull fails with CannotPullContainerError. The AmazonECSTaskExecutionRolePolicy managed policy includes these permissions.',
  },
  {
    domain: 'Deployment',
    q: 'A SAM template defines an API Gateway and Lambda function. The developer wants to test the API locally with sam local start-api. The Lambda function needs to call DynamoDB. What is required for the local test to reach DynamoDB?',
    opts: [
      'A local DynamoDB instance must be running on the developer\'s machine',
      'The developer\'s machine must have AWS credentials configured with access to DynamoDB',
      'The SAM template must include a LocalDynamoDB resource',
      'sam local start-api does not support DynamoDB integrations',
    ],
    a: 1,
    exp: 'sam local start-api invokes Lambda functions locally in Docker containers but they make real AWS SDK calls. As long as the developer has AWS credentials configured (via profile, environment variables, or IAM role), the locally running Lambda code will call the real DynamoDB service in AWS. A local DynamoDB instance is optional if using the real service.',
  },
  {
    domain: 'Deployment',
    q: 'A CodeBuild project builds a Docker image and pushes it to ECR. The build fails with "no basic auth credentials". What is the MOST likely cause?',
    opts: [
      'The buildspec.yml is missing the docker login command for ECR',
      'The CodeBuild service role does not have ecr:GetAuthorizationToken permission',
      'Docker is not installed in the CodeBuild build environment',
      'The ECR repository does not exist yet',
    ],
    a: 1,
    exp: 'To push to ECR, the build must first authenticate with aws ecr get-login-password | docker login. This command calls ecr:GetAuthorizationToken, which requires the CodeBuild service role to have this permission. Without it, the Docker daemon has no credentials for ECR and the push fails with an authentication error.',
  },
  {
    domain: 'Deployment',
    q: 'A developer needs to implement a blue/green deployment for a Lambda function using CodeDeploy. Traffic should shift 10% per minute until 100% is on the new version, with automatic rollback on errors. Which CodeDeploy configuration achieves this?',
    opts: [
      'Canary10Percent5Minutes',
      'Linear10PercentEvery1Minute',
      'AllAtOnce',
      'Linear10PercentEvery10Minutes',
    ],
    a: 1,
    exp: 'Linear10PercentEvery1Minute shifts exactly 10% of Lambda traffic to the new version every minute, reaching 100% after 10 minutes. Combined with CodeDeploy CloudWatch alarms and rollback hooks, errors detected during the shift trigger an automatic rollback to the original version. Canary shifts a fixed initial batch and then the remainder all at once.',
  },
  {
    domain: 'Deployment',
    q: 'A developer wants to store source code securely in AWS and trigger a CodePipeline when new commits are pushed. Which service should be used as the source stage?',
    opts: [
      'Amazon S3 with versioning',
      'AWS CodeCommit',
      'AWS CodeArtifact',
      'Amazon ECR',
    ],
    a: 1,
    exp: 'CodeCommit is a fully managed Git repository service that integrates natively with CodePipeline as a source provider. Pushing to a CodeCommit repository triggers the pipeline automatically via EventBridge. S3 with versioning can also be a source but is typically used for artifact storage, not source code. CodeArtifact stores packages; ECR stores Docker images.',
  },
  {
    domain: 'Deployment',
    q: 'A developer needs to run an Elastic Beanstalk environment as a worker that processes jobs from an SQS queue. Which Elastic Beanstalk environment tier should be used?',
    opts: [
      'Web Server tier with an SQS trigger',
      'Worker tier',
      'Web Server tier with a scheduled task',
      'Worker tier with an HTTP load balancer',
    ],
    a: 1,
    exp: 'The Elastic Beanstalk Worker environment tier is designed for background job processing. It pulls messages from an SQS queue using a daemon and delivers them to the application via HTTP POST to localhost. The Web Server tier is for internet-facing applications with an Elastic Load Balancer.',
  },
  {
    domain: 'Deployment',
    q: 'A CloudFormation stack deployment fails and the stack rolls back, deleting newly created resources. The developer wants to debug the failure without the rollback deleting resources. What option should be set?',
    opts: [
      'Set DeletionPolicy: Retain on all resources',
      'Disable rollback using the --disable-rollback flag during stack creation',
      'Enable stack termination protection',
      'Use a CloudFormation change set instead of a direct update',
    ],
    a: 1,
    exp: 'The --disable-rollback flag (or OnFailure=DO_NOTHING in the API) prevents CloudFormation from rolling back on failure, leaving the partially created resources intact for debugging. DeletionPolicy: Retain only affects resources on stack deletion, not on rollback. Termination protection prevents accidental deletion of the stack itself.',
  },
  {
    domain: 'Deployment',
    q: 'A developer is using CodeArtifact to manage npm packages. How should the developer configure npm to use CodeArtifact as the package registry?',
    opts: [
      'Update package.json with the CodeArtifact endpoint as the main field',
      'Run aws codeartifact login --tool npm to configure npm credentials and registry URL',
      'Manually edit ~/.npmrc with the CodeArtifact URL',
      'Set the NPM_REGISTRY environment variable to the CodeArtifact endpoint',
    ],
    a: 1,
    exp: 'The aws codeartifact login --tool npm command fetches a temporary authorization token and writes the registry URL and auth token to ~/.npmrc automatically. This token is valid for 12 hours by default. Manually editing ~/.npmrc works but requires managing the auth token manually.',
  },

  // ── Troubleshooting & Optimization ─────────────────────────────────────────
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A developer wants to find all Lambda function invocations that took longer than 3 seconds in the past hour using CloudWatch. Which tool provides the MOST efficient query capability?',
    opts: [
      'CloudWatch Metrics with a custom filter',
      'CloudWatch Logs Insights with a query on the @duration field',
      'CloudWatch Contributor Insights',
      'AWS X-Ray Service Map',
    ],
    a: 1,
    exp: 'CloudWatch Logs Insights allows ad-hoc SQL-like queries against CloudWatch Logs groups. Lambda automatically emits a REPORT log line with @duration, @billedDuration, @maxMemoryUsed, and other fields. A query like filter @duration > 3000 instantly returns matching invocations. X-Ray provides distributed tracing but requires more setup to filter by duration.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A Lambda function is configured with X-Ray tracing enabled. The developer wants to add custom metadata to a trace to record the user ID making the request. Which X-Ray concept should be used?',
    opts: [
      'X-Ray Segments',
      'X-Ray Annotations',
      'X-Ray Subsegments',
      'X-Ray Sampling Rules',
    ],
    a: 1,
    exp: 'X-Ray Annotations are key-value pairs indexed for filtering and searching in the X-Ray console. Adding the user ID as an annotation allows filtering traces by user. Metadata is similar but not indexed for search. Subsegments represent downstream calls within a segment. Sampling rules control which requests are traced.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A Lambda function is failing intermittently with no error logs in CloudWatch. The function processes SQS messages. What should the developer configure to capture failed messages for later investigation?',
    opts: [
      'Enable SQS long polling',
      'Configure a Dead Letter Queue (DLQ) on the SQS event source mapping',
      'Increase the Lambda function timeout',
      'Enable X-Ray tracing on the Lambda function',
    ],
    a: 1,
    exp: 'A Dead Letter Queue on the Lambda event source mapping captures messages that failed processing after the maximum number of retries. This allows developers to inspect failed messages, replay them after fixing the bug, and avoid message loss. X-Ray tracing helps diagnose failures but does not preserve failed messages.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A developer receives a ThrottlingException when calling the DynamoDB PutItem API. The table has 5 write capacity units (WCUs). Each item being written is 3 KB. What is happening?',
    opts: [
      'DynamoDB does not support PutItem for items larger than 1 KB',
      'The application is consuming more than 5 WCUs per second; each 1 KB write costs 1 WCU',
      'PutItem is a read operation and is consuming the read capacity instead',
      'The table needs a GSI to support the write pattern',
    ],
    a: 1,
    exp: 'DynamoDB charges 1 WCU per 1 KB of data written (rounded up) for provisioned mode. A 3 KB item costs 3 WCUs per write. With 5 WCUs provisioned, only about 1.6 writes per second can be sustained before throttling. Solutions include increasing WCUs, using on-demand mode, or implementing exponential backoff and retry.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'An API Gateway endpoint returns a 502 Bad Gateway error when invoked. The Lambda function exists and has the correct permissions. What is the MOST likely cause?',
    opts: [
      'The client request is malformed',
      'The Lambda function returned an invalid response format for proxy integration',
      'API Gateway throttled the request',
      'The Lambda function exceeded its concurrency limit',
    ],
    a: 1,
    exp: 'A 502 Bad Gateway from API Gateway typically means the Lambda function returned a response that API Gateway could not parse. With Lambda proxy integration, the function must return a JSON object with statusCode (integer), headers (object), and body (string) fields. A missing or malformed statusCode field causes a 502. A 429 indicates throttling; a 503 indicates concurrency exhaustion.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A developer notices that a Lambda function is consuming far more memory than expected. CloudWatch shows @maxMemoryUsed is near the configured limit, causing out-of-memory errors. Which CloudWatch Logs Insights query would help identify the most memory-intensive invocations?',
    opts: [
      'filter @type = "START" | stats max(@maxMemoryUsed) by @requestId',
      'filter @type = "REPORT" | sort @maxMemoryUsed desc | limit 20',
      'filter @message like "Out of memory" | count(*) by bin(1h)',
      'filter @duration > 5000 | stats avg(@maxMemoryUsed) by bin(5m)',
    ],
    a: 1,
    exp: 'REPORT log lines contain @maxMemoryUsed. Querying filter @type = "REPORT" | sort @maxMemoryUsed desc | limit 20 returns the top 20 invocations ordered by memory usage, helping identify outlier requests. Sorting by @duration does not directly identify memory-intensive invocations.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'An X-Ray trace shows a large gap between a Lambda function\'s initialization and the first downstream call. The function connects to RDS during initialization. What does this most likely indicate?',
    opts: [
      'A cold start where the Lambda execution environment is being initialized',
      'The RDS instance is experiencing high CPU utilization',
      'The VPC NAT Gateway is throttling outbound connections',
      'The Lambda function is retrying a failed connection',
    ],
    a: 0,
    exp: 'In X-Ray, Lambda cold starts appear as initialization time before the handler begins. When a new execution environment is created, Lambda runs the initialization code (outside the handler), which includes establishing database connections. This initialization gap is characteristic of a cold start and can be mitigated with Provisioned Concurrency.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A developer sets a CloudWatch alarm on a custom metric published by a Lambda function. The alarm enters INSUFFICIENT_DATA state and never transitions to OK or ALARM. What is the MOST likely cause?',
    opts: [
      'CloudWatch does not support alarms on custom metrics',
      'The Lambda function is not publishing the metric to the correct namespace or metric name',
      'The alarm threshold is set too high',
      'The alarm evaluation period is longer than the metric retention period',
    ],
    a: 1,
    exp: 'INSUFFICIENT_DATA means CloudWatch has not received enough data points to evaluate the alarm. The most common cause is that the metric is being published to a different namespace, metric name, or with different dimensions than the alarm is monitoring. Verifying the exact namespace, metric name, and dimensions in the CloudWatch Metrics console resolves this.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'An SQS-triggered Lambda function is processing messages but some messages are being processed multiple times. The function deletes the message after processing. What is the MOST likely cause?',
    opts: [
      'The SQS queue type is FIFO, which sends duplicates by design',
      'The function\'s processing time occasionally exceeds the visibility timeout, making the message visible again before deletion',
      'Lambda automatically retries all SQS messages three times',
      'The Dead Letter Queue is re-delivering failed messages to the source queue',
    ],
    a: 1,
    exp: 'If a Lambda function takes longer than the SQS visibility timeout to process a message, the message becomes visible in the queue again and can be picked up by another Lambda invocation before the first one deletes it. The fix is to set the visibility timeout to at least 6x the Lambda function timeout, or to extend the visibility timeout programmatically during processing.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A developer wants to track the number of items processed per Lambda invocation as a custom CloudWatch metric. Which approach is MOST cost-effective for high-throughput functions?',
    opts: [
      'Call PutMetricData once per item processed',
      'Aggregate counts within the function and call PutMetricData once per invocation with the total',
      'Write counts to DynamoDB and use a separate Lambda to publish metrics',
      'Use CloudWatch Logs metric filters to extract the count from log output',
    ],
    a: 1,
    exp: 'PutMetricData is charged per API call and has a rate limit. Aggregating all counts within the Lambda function and making a single PutMetricData call per invocation minimizes API costs and avoids throttling. CloudWatch Logs metric filters are also cost-effective but add latency and require structured log output.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'An API Gateway returns a 403 Forbidden error for all requests even though the endpoint is publicly accessible. The developer recently added a resource policy. What is the MOST likely cause?',
    opts: [
      'The API has not been redeployed after the resource policy change',
      'Resource policies cannot be used with public API Gateway endpoints',
      'The resource policy has an explicit Deny for all principals',
      'API Gateway requires a VPC endpoint when resource policies are used',
    ],
    a: 0,
    exp: 'Changes to API Gateway resource policies take effect only after the API is redeployed to a stage. If the API was not redeployed after adding the resource policy, the old policy (or no policy) remains in effect. After adding or modifying a resource policy, always redeploy the API.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A DynamoDB table receives bursts of read traffic that exceed provisioned read capacity, causing throttling. The data being read is the same set of popular items. Which solution reduces throttling with the LEAST application code changes?',
    opts: [
      'Increase provisioned read capacity units permanently',
      'Enable DynamoDB Accelerator (DAX)',
      'Implement client-side caching in the application code',
      'Use DynamoDB adaptive capacity',
    ],
    a: 1,
    exp: 'DAX is a fully managed, API-compatible in-memory cache for DynamoDB. Enabling DAX requires only changing the DynamoDB client endpoint in the application — no query logic changes. It absorbs repeated reads of popular items and dramatically reduces the load on DynamoDB. Client-side caching requires more significant code changes.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A developer wants to instrument an existing Node.js Lambda function with X-Ray to capture all downstream AWS SDK calls automatically. What is the minimum change required?',
    opts: [
      'Enable X-Ray Active Tracing on the Lambda function and add aws-xray-sdk to instrument the AWS SDK',
      'Add an X-Ray daemon sidecar container to the Lambda function',
      'Enable X-Ray Passive Tracing in the Lambda configuration',
      'Add X-Ray annotations to every SDK call manually',
    ],
    a: 0,
    exp: 'Enabling Active Tracing in the Lambda configuration causes Lambda to run the X-Ray daemon automatically. Wrapping the AWS SDK with the X-Ray SDK (const AWS = AWSXRay.captureAWS(require("aws-sdk"))) automatically creates subsegments for all SDK calls with no per-call instrumentation. Passive tracing only traces requests that already have a trace header.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A Lambda function configured with an SQS trigger is consistently failing. After 3 retry attempts, the messages are lost. The developer wants failed messages to be preserved for analysis. What should be configured?',
    opts: [
      'Set the SQS queue message retention period to 14 days',
      'Configure a Dead Letter Queue on the SQS queue and set maxReceiveCount to 3',
      'Increase the Lambda function timeout to prevent failures',
      'Enable Lambda Destinations with an on-failure destination',
    ],
    a: 1,
    exp: 'Configuring a Dead Letter Queue on the SQS queue with a maxReceiveCount of 3 moves messages to the DLQ after 3 failed receive attempts. This preserves failed messages for investigation and replay. Lambda Destinations work for asynchronous (non-SQS) invocations. Message retention extends how long messages stay in the queue but does not redirect failures.',
  },

  // ── Additional Development with AWS Services ───────────────────────────────
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to implement a Step Functions workflow that runs two Lambda functions in parallel and waits for both to complete before proceeding. Which state type should be used?',
    opts: [
      'Choice state',
      'Parallel state',
      'Map state',
      'Wait state',
    ],
    a: 1,
    exp: 'The Parallel state in Step Functions executes multiple branches concurrently and waits for all branches to complete before transitioning to the next state. The Map state iterates over a collection and runs states for each item. Choice states handle conditional branching; Wait states introduce delays.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer is implementing an API that returns paginated results from DynamoDB. After the first page of results, DynamoDB returns a LastEvaluatedKey. How should the developer retrieve the next page?',
    opts: [
      'Use the LastEvaluatedKey as the KeyConditionExpression in the next query',
      'Set ExclusiveStartKey to the LastEvaluatedKey value in the next query',
      'Increment the page number by 1 in the next query request',
      'Use the LastEvaluatedKey as the FilterExpression in the next scan',
    ],
    a: 1,
    exp: 'DynamoDB pagination uses a cursor-based approach. When a Query or Scan returns a LastEvaluatedKey, pass it as the ExclusiveStartKey in the next request to continue reading from where the previous query left off. DynamoDB does not support offset-based pagination.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer creates an API Gateway HTTP API with a Lambda integration. The developer notices that the Lambda function is not receiving the original request headers. What is the MOST likely cause?',
    opts: [
      'HTTP APIs do not support custom headers',
      'The integration is not configured as a proxy integration, so headers are not forwarded',
      'Lambda functions cannot access request headers',
      'The API Gateway is stripping headers for security',
    ],
    a: 1,
    exp: 'HTTP API Lambda integrations use Lambda proxy integration by default and forward all request headers in the event object. If the integration was explicitly configured as a non-proxy integration without mapping templates, headers are not automatically forwarded. Verifying the integration type is the first troubleshooting step.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer is using the AWS SDK for Python (boto3) and wants to handle transient errors with exponential backoff automatically. Which SDK feature provides this?',
    opts: [
      'boto3 does not support automatic retries',
      'Configure the botocore retry configuration with max_attempts and retry mode',
      'Use a try/except block with time.sleep for manual backoff',
      'Enable SDK automatic retry via the AWS_RETRY_MODE environment variable only',
    ],
    a: 1,
    exp: 'boto3 uses botocore\'s retry configuration, which can be customized by setting the retry parameter in the Config object with max_attempts and mode (legacy, standard, or adaptive). Standard mode uses exponential backoff with jitter. The AWS_RETRY_MODE environment variable also configures this globally, and both approaches are valid.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A Kinesis Data Stream consumer is falling behind in processing. The stream has 4 shards and 2 consumer applications. Which approach increases read throughput without resharding?',
    opts: [
      'Increase the GetRecords limit per call',
      'Use Enhanced Fan-Out to give each consumer application dedicated throughput per shard',
      'Increase the stream retention period',
      'Switch to Kinesis Data Firehose',
    ],
    a: 1,
    exp: 'Enhanced Fan-Out gives each registered consumer its own 2 MB/s per-shard read throughput, independent of other consumers. Without Enhanced Fan-Out, all consumers share the 2 MB/s standard read throughput per shard. With 2 consumer applications on 4 shards, Enhanced Fan-Out effectively doubles the available read bandwidth without resharding.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer uses DynamoDB Streams to trigger a Lambda function whenever items are updated. The function needs to receive only the new image of the item after the update. Which DynamoDB Streams view type should be configured?',
    opts: [
      'KEYS_ONLY',
      'NEW_IMAGE',
      'OLD_AND_NEW_IMAGES',
      'NEW_AND_OLD_IMAGES',
    ],
    a: 1,
    exp: 'DynamoDB Streams supports four view types: KEYS_ONLY (only key attributes), NEW_IMAGE (entire item after change), OLD_IMAGE (entire item before change), and NEW_AND_OLD_IMAGES (both). NEW_IMAGE provides the complete updated item without the overhead of transmitting the old image.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A developer needs to implement a rate-limited API where each client is allowed 100 requests per second. Which API Gateway feature provides per-client rate limiting?',
    opts: [
      'Stage-level throttling settings',
      'Usage Plans with API Keys',
      'Lambda authorizer with rate limiting logic',
      'API Gateway WAF integration',
    ],
    a: 1,
    exp: 'API Gateway Usage Plans define throttle (requests per second and burst) and quota (requests per day/week/month) limits per API key. Each client is assigned an API key associated with a Usage Plan, enabling per-client rate limiting. Stage-level throttling applies globally to all clients.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A serverless application uses S3 to store user-uploaded images. A Lambda function must be triggered immediately when a new image is uploaded. Which S3 feature enables this?',
    opts: [
      'S3 Lifecycle policies',
      'S3 Event Notifications',
      'S3 Replication',
      'S3 Inventory',
    ],
    a: 1,
    exp: 'S3 Event Notifications can invoke a Lambda function, send a message to SQS, or publish to SNS when specific events occur, such as s3:ObjectCreated:*. This triggers the Lambda function immediately (near-real-time) when a new object is uploaded. Lifecycle policies manage object transitions and expiration, not event-driven invocations.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'A Lambda function needs to write to DynamoDB transactionally: either both writes succeed or neither does. Which DynamoDB API should be used?',
    opts: [
      'BatchWriteItem',
      'TransactWriteItems',
      'PutItem with a ConditionExpression',
      'UpdateItem with a ConditionExpression',
    ],
    a: 1,
    exp: 'TransactWriteItems provides ACID transactions across up to 100 items and multiple tables. Either all operations in the transaction succeed or all fail, ensuring atomicity. BatchWriteItem executes operations in parallel but is not transactional — some writes can fail independently.',
  },
  {
    domain: 'Development with AWS Services',
    q: 'An API Gateway REST API uses a Lambda authorizer. The developer wants to reduce latency and Lambda invocations for the authorizer. Which configuration should be adjusted?',
    opts: [
      'Enable API Gateway caching for the endpoint methods',
      'Increase the Lambda authorizer result cache TTL',
      'Switch to a Cognito User Pool authorizer',
      'Enable X-Ray tracing on the Lambda authorizer',
    ],
    a: 1,
    exp: 'Lambda authorizer results can be cached by API Gateway based on the incoming token. Increasing the cache TTL (up to 3600 seconds) means the authorizer Lambda is invoked less frequently, reducing latency for subsequent requests with the same token and lowering Lambda invocation costs.',
  },

  // ── Additional Security ─────────────────────────────────────────────────────
  {
    domain: 'Security',
    q: 'A company wants all API requests to be signed with AWS SigV4 to ensure only authenticated AWS principals can call the API. Which API Gateway authorization method should be used?',
    opts: [
      'Cognito User Pool authorizer',
      'Lambda authorizer',
      'IAM authorization',
      'API Key authorization',
    ],
    a: 2,
    exp: 'IAM authorization requires clients to sign requests using AWS Signature Version 4 with valid IAM credentials. API Gateway validates the signature against IAM policies. This is suitable for service-to-service communication. Cognito and Lambda authorizers use JWT tokens; API Keys are not tied to IAM identities.',
  },

  // ── Additional Deployment ──────────────────────────────────────────────────
  {
    domain: 'Deployment',
    q: 'A developer wants to preview the changes a CloudFormation stack update will make before applying them. Which CloudFormation feature enables this?',
    opts: [
      'CloudFormation drift detection',
      'CloudFormation change sets',
      'CloudFormation stack policies',
      'CloudFormation rollback triggers',
    ],
    a: 1,
    exp: 'CloudFormation change sets show a preview of what resources will be added, modified, or deleted by a proposed stack update before executing it. Drift detection identifies differences between the stack template and the actual deployed resources. Stack policies protect resources from unintended updates.',
  },
  {
    domain: 'Deployment',
    q: 'A developer is deploying a Lambda function using CloudFormation. The function code is stored in an S3 bucket. After updating the code in S3 without changing the S3 key, the CloudFormation update does not deploy the new code. What is the cause?',
    opts: [
      'CloudFormation does not support Lambda deployments from S3',
      'CloudFormation detects changes only by comparing template properties; if the S3 key is unchanged, no update is triggered',
      'The Lambda function must be deleted and recreated for code changes',
      'CloudFormation caches Lambda function code for 24 hours',
    ],
    a: 1,
    exp: 'CloudFormation tracks changes to stack resources by comparing template property values. If the S3 key and version remain unchanged, CloudFormation sees no change to the AWS::Lambda::Function resource and does not update it. The solution is to use S3 object versioning and reference the S3ObjectVersion in the template, or use SAM/CDK which handle this automatically.',
  },

  // ── Additional Troubleshooting & Optimization ──────────────────────────────
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A Lambda function occasionally throws a timeout error. The function timeout is set to 5 seconds. CloudWatch Logs show the function is making an HTTP call to a third-party API that sometimes takes 8 seconds. What is the BEST solution?',
    opts: [
      'Increase the Lambda timeout to 10 seconds',
      'Implement an asynchronous pattern: queue the request in SQS and process it in a separate Lambda with a longer timeout',
      'Retry the HTTP call with exponential backoff inside the same Lambda invocation',
      'Use Lambda Provisioned Concurrency to reduce latency',
    ],
    a: 1,
    exp: 'If the third-party API can take longer than the acceptable Lambda timeout, the best pattern is to decouple the request from the response: accept the request synchronously, queue it in SQS, and process it asynchronously with a Lambda that has an appropriate timeout. Provisioned Concurrency reduces cold starts but does not affect downstream API latency.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A developer notices that a CloudWatch alarm is in ALARM state but no notification was sent via SNS. The alarm has an SNS action configured. What is the MOST likely cause?',
    opts: [
      'CloudWatch cannot publish to SNS directly',
      'The SNS topic does not exist or the CloudWatch service does not have permission to publish to it',
      'The alarm must be in ALARM state for at least 5 minutes before notifications are sent',
      'SNS notifications require a confirmed subscription to the topic',
    ],
    a: 3,
    exp: 'SNS subscriptions require the endpoint (email, HTTP, etc.) to confirm the subscription before messages are delivered. If the subscription is not confirmed, messages are published to the topic but not delivered to the endpoint. Unconfirmed email subscriptions are the most common cause of missing CloudWatch alarm notifications.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'X-Ray shows a service in the Service Map with a high error rate (red). The error annotation shows "Error". What does this indicate?',
    opts: [
      'The service returned a 5xx server error response',
      'The service returned a 4xx client error response',
      'The service was throttled by AWS',
      'The service experienced a network fault',
    ],
    a: 1,
    exp: 'In X-Ray, "Error" (shown in red) indicates a 4xx client-side error. "Fault" indicates a 5xx server-side error. "Throttle" indicates a 429 throttling response. Understanding this distinction helps developers quickly identify whether the issue is with client requests (4xx) or the service itself (5xx).',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'A developer wants to reduce the cost of CloudWatch Logs for a high-volume Lambda function. The function logs every input event as JSON. Which approach reduces cost without losing critical diagnostic information?',
    opts: [
      'Disable CloudWatch Logs for the Lambda function',
      'Log only errors and warnings; remove debug and info-level event logging in production',
      'Switch to CloudWatch Metrics instead of Logs',
      'Compress log entries before writing them to CloudWatch Logs',
    ],
    a: 1,
    exp: 'CloudWatch Logs charges are based on data ingested. Logging every input event at debug level in production generates unnecessary data. The recommended approach is to implement log levels and log only warnings and errors in production, significantly reducing ingested log volume and cost while preserving critical diagnostic information.',
  },
  {
    domain: 'Troubleshooting & Optimization',
    q: 'An API Gateway endpoint returns 504 Gateway Timeout errors during peak traffic. The backend is a Lambda function with a 29-second timeout. What is MOST likely causing the 504?',
    opts: [
      'API Gateway has a maximum integration timeout of 29 seconds',
      'The Lambda function is exceeding the 29-second API Gateway maximum integration timeout',
      'API Gateway throttling is causing requests to queue and timeout',
      'The Lambda function is running out of memory',
    ],
    a: 1,
    exp: 'API Gateway has a maximum integration timeout of 29 seconds. If the Lambda function takes longer than 29 seconds to respond, API Gateway returns a 504 Gateway Timeout. The Lambda function may still be running, but API Gateway will not wait. Solutions include optimizing the function to complete within 29 seconds or switching to an asynchronous pattern.',
  },
];
